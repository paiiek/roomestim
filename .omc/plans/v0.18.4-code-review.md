# Code Review — roomestim v0.18.4 (v0.19-cycle OQ re-examination, doc-only)

**Reviewer:** code-reviewer (read-only, separate lane)
**Date:** 2026-05-25
**Base:** HEAD `c05c34e` (v0.18.3) → uncommitted working tree (v0.18.4)
**Scope:** `git diff` (decisions.md, open-questions.md, ADR 0030/0034/0035/0036, pyproject.toml, roomestim/__init__.py) + untracked `RELEASE_NOTES_v0.18.4.md`, `.omc/plans/v0.18.4-patch.md`
**Verdict: APPROVE-WITH-NITS → both nits applied (see Resolution) → APPROVE**
(0 CRITICAL / 0 HIGH / 0 MEDIUM / 1 LOW / 1 NIT — both resolved before commit)

---

## Summary

| Metric | Value |
|---|---|
| Files changed | 8 (1 version code-line, 1 config, 4 ADR, 2 plan-state) + 1 new RELEASE_NOTES |
| Functional code | 1 line — `roomestim/__init__.py:3` `__version__` bump only |
| New decisions | D57 (OQ-36 WONTFIX) + D58/D59/D60/D61 (re-defers) |
| New tests | none (doc-only cycle — no behavior change to test) |
| Doc diff | append-only — exactly 2 deletion lines in whole diff, both version strings |

---

## Criteria results

1. **Scope discipline — PASS.** `git diff -- roomestim/ roomestim_web/ tests/` shows ONLY `roomestim/__init__.py:3` (`0.18.3`→`0.18.4`). No functional code touched; `roomestim_web/` byte-equal (`0.15-web.0` unchanged).
2. **Append-only audit trail (D22 / D28-P1) — PASS.** Every ADR/OQ/decisions edit is a new block (inserted above prior status-update for ADR 0030/0036; appended at EOF elsewhere). No prior prose body modified. OQ-36's original body (`open-questions.md:489-499`) untouched; the `[x] CLOSED (WONTFIX)` flip lives only in the new append block.
3. **Naming consistency — PASS.** All this-cycle blocks VERSION-named (`§Status-update-v0.18.4`, `## v0.18.4 OQ status-updates`, `## v0.18.4 D-decision allocation`); 0 stray `Status-update-v0.19` headers ("v0.19-cycle" only in prose). ADR 0030 Item lettering = **Item Z** (follows Item Y at v0.18.3).
4. **Decision numbering — PASS.** Prior max D56; new D57–D61 sequential, no collisions. Every decision carries an explicit reverse-criterion (D26 forbidden-indefinite-deferral satisfied): OQ-34/35→v0.21, OQ-37/38→v0.20 (concrete cadences); OQ-36→WONTFIX with two-pronged reverse-criterion. No cadence-only re-defer.
5. **Honesty discipline — PASS.** Load-bearing claims verified against repo: "spatial_engine consumes only layout.yaml" (external consumer — accurate; `room.yaml` is read internally by `roomestim/io/room_yaml_reader.py`, but the claim is scoped to the external consumer); "library write-path already exists" verified (`roomestim/export/room_yaml.py` accepts `schema_version="0.1"`, omits `objects`); "0 reports / trigger unmet" confirmed via git log + docs grep; `model.py:247 notes: str = ""` exact; `roomestim/io/placement_yaml_reader.py:67-76` WFS-vs-VBAP inference / DBAP·AMBISONICS→"VBAP" collapse verified.
6. **Cross-ref integrity — PASS.** D57↔OQ-36↔ADR 0034 §v0.18.4↔ADR 0030 Item Z consistent. RELEASE_NOTES_v0.18.4.md cadence table matches decisions.md and the ADR blocks.
7. **Consistency with design doc — PASS.** Applied edits match `.omc/plans/v0.18.4-patch.md` §9 Steps 1–9 intent.

---

## Findings & Resolution

- **[LOW] ADR 0030 §Status-update-v0.18.4 date** read `(2026-05-24)` while all sibling v0.18.4 blocks read `(2026-05-25)`; plan §9 Step 6 specified `(2026-05-25)`. Likely copy-paste from the adjacent v0.18.3 block. **RESOLVED:** `docs/adr/0030-predictor-default-switch.md:414` → `(2026-05-25)`. (Verifier gate 8 confirms.)
- **[NIT] `placement_yaml_reader.py:67-76` citations** in `decisions.md` (D61) and ADR 0036 lacked the `roomestim/io/` directory prefix used by sibling citations. **RESOLVED:** both now `roomestim/io/placement_yaml_reader.py:67-76`; 0 bare occurrences. (Verifier gate 9 confirms.)

Both fixes are single-token edits to blocks authored THIS cycle (append-discipline-neutral; not retroactive edits of prior content).

---

## Positive observations

- Append-only discipline exemplary: exactly 2 deletion lines in the entire diff, both expected version strings.
- The plan's version-naming note proactively resolved the "v0.19-cycle label vs v0.18.4 emitted version" trap; executor honored it (0 stray `v0.19` headers).
- Every deferral names a concrete cadence AND a bidirectional reverse-criterion, fully honoring D26.
- Honesty claims precise — the "external spatial_engine" qualifier correctly avoids overclaiming that no internal `room.yaml` reader exists.

---

## Recommendation: **APPROVE** (post-fix)

No Critical/High/Medium. The 1 LOW + 1 NIT were corrected before commit and independently re-confirmed by the verifier pass. Hand to verifier (done — see `v0.18.4-verifier.md`).
