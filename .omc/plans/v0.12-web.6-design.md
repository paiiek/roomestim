# roomestim v0.12-web.6 — Design Plan (mini)

**Routing**: D29 — `.omc/plans/v0.12-web.6-design.md`. **Versioning**: D30 —
`pyproject.toml` `0.14.0` 불변; `roomestim_web.__version__` `"0.12-web.5"` →
`"0.12-web.6"`. **Predecessor**: v0.12-web.5 (`a054add`).

## 1. 목표 — 약한점·위험 모두 해결 (9 정돈 + 2 위험 + OQ-28/29 NEW)

| # | 항목 | Source | 처리 |
|---|---|---|---|
| 1 | HF Spaces ffmpeg 미설치 위험 | 위험 list item 1 | `packages.txt` NEW: `ffmpeg` |
| 2 | 첫 클릭 전 빈 탭 UX 위험 | 위험 list item 3 | `_ensure_web_data()` → `bool` + `build_demo()` initial Markdown |
| 3 | KEMAR/LibriVox URL rotation 모니터링 | 위험 list item 2 | OQ-28 NEW (GitHub Action stub 계획) |
| 4 | MEDIUM-1 `gr.update` test contract | v0.12-web.5 code-review | `tests/web/_md_helpers.py:get_md_payload()` 헬퍼 추출 |
| 5 | MINOR-1 `fetch_kemar` mismatch integration | v0.12-web.5 code-review | `test_fetch_kemar_integration_sha_mismatch_raises_and_unlinks` NEW |
| 6 | MINOR-2 binaural-status 코멘트 | v0.12-web.5 code-review | `gr.Markdown` 위 4-line 코멘트 |
| 7 | MINOR-2 `print()` → log + quiet flag | v0.12-web.4 code-review | `_progress_quiet()` + `ROOMESTIM_WEB_QUIET_FETCH=1` |
| 8 | MINOR-3 `_BINAURAL_DATA_ROOT` 함수화 | v0.12-web.4 code-review | 부분 — `_binaural_data_present()` 함수 NEW (현재 mod-global 유지: 테스트 patch 호환) |
| 9 | MINOR-4 env-gate DRY | v0.12-web.4 code-review | `auto_fetch_enabled()` 헬퍼 NEW (3-callsite 통합) |
| 10 | MINOR-5 zip-slip docstring | v0.12-web.4 code-review | `extract_hutubs` docstring 4-line 노트 |
| 11 | MINOR-3 drift root-cause 식별 | v0.12-web.5 code-review | `test_mypy_strict_baseline.py:30` skipif 식별 → RELEASE_NOTES 명시 |
| 12 | OQ-26 후속 — pp1 SOFA mirror | P3 | OQ-29 NEW (HUTUBS pp1 GitHub mirror) |

## 2. Phase별 구현

### Phase 1 — HF Spaces 위험 (item 1 + 2)
- `packages.txt` NEW: `ffmpeg` (HF Spaces auto-detects).
- `app.py:_ensure_web_data() -> bool` (반환 True = fetch 시작됨).
- `build_demo()` 분기: `fetch_started` → "데이터 다운로드 중", `not data_present` → "데이터 미준비", else → hidden.

### Phase 2 — Web 정돈 (items 4-10)
- `scripts/fetch_web_data.py`: `auto_fetch_enabled()` + `_progress_quiet()` 헬퍼; `_download_file` 진행률을 daemon 모드에서 억제; `extract_hutubs` zip-slip docstring; `auto_fetch` / `main()` 에서 신규 헬퍼 사용.
- `app.py`: `_binaural_data_present()` 함수 추출; `_ensure_web_data()` 가 daemon thread 시작 시 `ROOMESTIM_WEB_QUIET_FETCH=1` 설정; `binaural_status_md` Markdown 위 코멘트.
- `tests/web/_md_helpers.py` NEW: `get_md_payload(maybe_update)` → (value, visible).
- `tests/web/test_binaural_fallback.py`: 신규 헬퍼 사용으로 refactor.
- `tests/web/test_fetch_web_data.py`: `test_fetch_kemar_integration_sha_mismatch_raises_and_unlinks` NEW.

### Phase 3 — OQ 신규 (items 3, 12)
- OQ-28 NEW: KEMAR/LibriVox/HUTUBS URL stability monitoring (GitHub Action stub).
- OQ-29 NEW: HUTUBS pp1 SOFA GitHub mirror (OQ-26 후속).

### Phase 4 — 문서 (item 11)
- ADR 0029 §A §Status-update-v0.12-web.6 (packages.txt + UX + DRY + quiet flag).
- D37 NEW.
- RELEASE_NOTES_v0.12-web.6.md NEW.
- drift root-cause: `test_mypy_strict_baseline.py:30 pytest.mark.skipif(not _MYPY_AVAILABLE)` → mypy 미설치 환경에서 1 skip 증가 — RELEASE_NOTES에 명시.

## 3. 수용 기준

| # | Check | Expected |
|---|---|---|
| 1 | `pytest tests/web/` | 48 passed + 1 skipped (+1 SHA mismatch integration) |
| 2 | `pytest -m "not lab and not web"` | 150 passed + 4 skipped (mypy 설치 env) / 149+5 (mypy 없는 env) |
| 3 | `ruff check` | All checks passed |
| 4 | `mypy --strict roomestim/` | 0 errors |
| 5 | `__version__` | `0.12-web.6` |
| 6 | `pyproject.toml` | `0.14.0` 불변 |
| 7 | `git diff roomestim/` | empty (byte-equal) |
| 8 | `build_demo()` smoke | 29 components, Markdown 초기값 분기 작동 |
| 9 | `_binaural_data_present()` smoke | True/False 정확 |

## 4. 버전

**v0.12-web.6** — 약한점·위험 정돈 패치.
