# roomestim v0.18.2 — Code Review (doc-only re-defer + regression lock)

**Reviewer**: code-reviewer (separate read-only pass)
**Date**: 2026-05-24
**Base**: HEAD `2ad9ece` (v0.18.1) vs uncommitted working tree
**Change type**: DOC-ONLY re-defer (OQ-33 / D54) + 1 new executable test file + PATCH version bump
**Verdict**: **APPROVE**

---

## Scope verified
`git diff 2ad9ece --stat`: 6 tracked files (2 plan docs, 2 ADRs, pyproject, roomestim/__init__.py)
\+ 3 untracked (RELEASE_NOTES_v0.18.2.md, tests/test_oq33_residual_lock.py, .omc/plans/v0.18.2-patch.md).
The only `roomestim/**.py` code change is the `__init__.py` version-string line (1 ins / 1 del).
No acoustic/schema/web code touched — matches the DOC-ONLY claim.

---

## Stage 1 — Spec compliance (re-defer integrity)

| # | Requirement | Result |
|---|-------------|--------|
| 1 | D22 append-only: all 4 docs APPEND, no retroactive edits | PASS — every diff hunk is a pure tail-append (`@@ -1547,3 +1547,53`, `@@ -525,3 +525,68`, `@@ -410,3 +410,29`, `@@ -177,3 +177,61`); 3 context lines unchanged, zero deletions above the new section. |
| 2 | Numbering no-collision | PASS — pre-patch max: D53→D54/D55, OQ-38→OQ-39, ADR0030 Item W→X. Confirmed against `git show 2ad9ece:` (D54/D55 absent, OQ-39 absent pre-patch). No new ADR number (ADR 0037 correctly deferred to v0.20). |
| 3 | Version-string consistency | PASS — pyproject + `roomestim.__version__` = `0.18.2`; RELEASE_NOTES filename + ADR §Status-update-v0.18.2 all 0.18.2. `roomestim_web.__version__` stays `0.15-web.0`; `__schema_version__` stays `0.2-draft`. Plan renamed to `v0.18.2-patch.md`; NO `v0.19-design.md` in `.omc/plans/`. `git diff 2ad9ece -- roomestim_web/` = 0 bytes (web byte-equal claim verified). |
| 4 | Regression-lock test NON-vacuous | PASS — all 4 cases EXECUTE (none skip): MeshAdapter parses real `tests/fixtures/lab_room.obj`, ACE `load_room` parses real rooms `['Meeting_1','Office_1']`, both assert `objects==[]` on genuine adapter output (not mocked constants). Round-trip tests genuinely `evolve_room_add_object` → `write_room_yaml` → `read_room_yaml` and assert kind/material/width_m survive. ADR adapter line-refs (mesh.py:154, ace_challenge.py:721) accurate. |
| 5 | D54 honesty | PASS — manual path genuinely DONE (Object/evolve helpers exported in `roomestim.__init__`, model.py defines them, predictor fold pre-exists). Both D26 triggers genuinely unmet (0 reports, no mesh-GT fixture in repo). Concrete v0.20 hard wall + 3-part Reverse-criterion present. No overclaim found. |

## Stage 2 — Code quality / gates (project miniforge interpreter)

- `ruff check`: All checks passed.
- `mypy --strict roomestim/`: Success, no issues, 39 source files.
- `pytest tests/test_oq33_residual_lock.py`: 4 passed.
- `pytest -m 'not lab and not web'`: 263 passed, 12 skipped, 53 deselected, exit 0 — no regression; +4 new tests fold in cleanly.

---

## Findings

| Severity | Location | Finding | Suggested fix |
|----------|----------|---------|---------------|
| LOW | `docs/adr/0030-...md:OQ-39 block` & RELEASE_NOTES:46 | ADR 0030 size stated "~430 / 412-line"; actual working-tree size is **438 lines**. Still well under the 600-line OQ-39 trigger, so the deferral logic is unaffected. | Optionally state ">430" or exact 438. Non-blocking. |
| LOW | `.omc/plans/v0.18.2-patch.md:170,201` | Stale planner-draft remnants: "§Status-update-v0.19 Item X" / "§What v0.19 decides" — the shipped ADR/RELEASE_NOTES correctly say **v0.18.2**, not v0.19. Cosmetic inconsistency confined to the historical planner artifact (D29 READ-ONLY record). | Optionally s/v0.19/v0.18.2/ in those 2 lines for the audit trail. Non-blocking. |
| LOW (test completeness) | `tests/test_oq33_residual_lock.py:106-138` | Round-trip tests assert YAML survival but do NOT exercise the acoustic path (no `predict_rt60`/`_objects_to_surfaces` call), though the review focus mentioned "acoustic path consumes it". Mitigated: the acoustic fold is already locked by the pre-existing `tests/test_objects_acoustic_invariant.py` (6 tests, D47 150-instance lock). | None required — coverage exists elsewhere. Could add one assertion that an object-bearing room yields a column-surface mention in `rationale` if desired. |

No CRITICAL / HIGH / MEDIUM issues.

## Positive observations
- Textbook D22 append-only discipline: every doc edit is a verifiable tail-append with byte-unchanged prior content.
- The regression-lock test is a genuine guard, not a tautology: it instantiates real adapters against real fixtures and will fail loudly if auto-detection is silently added — exactly the "deliberate-decision-forcing" intent D54 describes.
- D54 is honest about reality (manual path DONE, candidate 3 already shipped) and corrects the stale OQ-33 premise rather than papering over it.
- Hard v0.20 wall + concrete 3-part Reverse-criterion + explicit WONTFIX path satisfy D26 forbidden-indefinite-deferral with no escape hatch.
- Version hygiene is exact: web byte-equal (0 bytes), schema unchanged, PATCH bump justified by executable tests (not the re-defer itself).

## Env note
`/usr/bin/python3` lacks ruff/mypy/ty; the project interpreter `/home/seung/miniforge3/bin/python` has ruff 0.15.12 + mypy 1.20.2 and all gates pass there. LSP (`ty`) unavailable — substituted by mypy --strict (clean).

## Recommendation
**APPROVE.** Three LOW cosmetic/coverage notes, all non-blocking. Re-defer integrity, numbering, version consistency, append-only discipline, and test non-vacuity all verified GREEN.
