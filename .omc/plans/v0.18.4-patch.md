# roomestim v0.18.4 — Patch Plan (v0.19-cycle OQ re-examination: OQ-34/35/36/37/38)

> **Version-naming note**: the deferral cadence on record literally names "v0.19
> cycle 시작 시 재검토" for OQ-34/35/36/37/38, and OQ-36 carries a D26 **hard wall
> forcing a decision by the v0.19 cycle**. This cycle SATISFIES that cadence
> commitment. It is shipped as **PATCH `0.18.4`** (a re-defer / forced-WONTFIX of
> externally-gated OQs does NOT justify a MINOR bump — see §6). **"v0.19 cycle" is
> the cadence LABEL (which OQs are due), not the emitted version** — the emitted
> version string is `0.18.4`, the plan file is `v0.18.4-patch.md` (per the
> `v<version>-patch.md` convention: `v0.18.2-patch.md`, `v0.18.3-patch.md`), and
> all audit-trail artifacts are **VERSION-named `v0.18.4`**, NOT cadence-named
> `v0.19`. This is the EXACT pattern the project used at v0.18.2: the "v0.19-cycle"
> OQ-33 decision shipped as PATCH `0.18.2`, plan file `v0.18.2-patch.md`, and its
> ADR blocks were headed `§Status-update-v0.18.2` (verified at
> `docs/adr/0034-object-schema.md:183`, `docs/adr/0030-...:433`) — version-named,
> with the prose noting the v0.19-cycle cadence. We mirror that exactly: ADR blocks
> are headed **`§Status-update-v0.18.4`**, with prose noting they fulfill the
> v0.19-cycle OQ re-examination commitment.

**Routing**: D29 — `.omc/plans/v0.18.4-patch.md` (planner artifact; READ-ONLY
planning, no code edits in this cycle). **Versioning**: D30 — see §6; recommended
path = **doc-only re-examination** → `pyproject.toml` / `roomestim.__version__`
`0.18.3` → `0.18.4` (PATCH); `roomestim_web.__version__` **불변** `0.15-web.0`
(web byte-equal — no web touch); `__schema_version__` **불변** `"0.2-draft"`
(no new `Object`/`RoomModel` field). **Predecessor**: v0.18.3 (`c05c34e` — D56
`write_layout_yaml` float normalization; working tree clean, 12 gates GREEN).
**Anchor**: **OQ-36** — D26 forbidden-indefinite-deferral forces a DECISION at
v0.19 (cadence on record: `open-questions.md:496`; the hard-wall trigger
"외부 consumer fail 보고 ≥ 1건" is **0** — verified). **Mode**: **SHORT** (every
OQ is externally-gated with verified 0 reports; there is no feature to build —
this is the project's established doc-only re-examination cadence).

> **Scope flag for the user**: This plan does **NOT** recommend any feature
> implementation. It is a **doc-only OQ re-examination** (ADR §Status-update +
> decisions + OQ status appends + PATCH bump). The single genuine open decision
> is **MINOR vs PATCH** (§6) — recommendation: **PATCH `0.18.4`**, flagged for
> your confirmation.

---

## 0. Scope statement (why this cycle exists, what it touches)

The v0.18.x sprint closed the entire "사용자 4-제안" roadmap (v0.18.0 nudge +
round-trip, v0.18.1 el-bound, v0.18.2 OQ-33 re-defer, v0.18.3 float-normalization
PATCH). The **next natural unit** is the v0.19 OQ re-examination: five open
questions carry the cadence **"v0.19 cycle 시작 시 재검토"**, and one of them
(OQ-36) is a **D26 hard wall** that forces a decision this cycle.

**This is NOT a feature cycle.** Every one of OQ-34/35/36/37/38 is externally
gated (user-request / external-standard / external-consumer-fail triggers), and
**all triggers are verified UNMET (0 reports)**. The correct, honest disposition
is therefore:

- **OQ-36 (hard wall)** → **forced decision = permanent deferral / WONTFIX** per
  the OQ's own resolution candidate (3) "영구 deferral (보고 0건이면 YAGNI)",
  with an explicit reverse-criterion. (Justified rigorously in §2.)
- **OQ-34/35/37/38 (soft cadence)** → **re-defer with an EXPLICIT new concrete
  cadence** (NOT open-ended — D26 forbids indefinite deferral). New cadences
  assigned per-OQ in §3.

**Touched (doc-only):** `decisions.md` (D57–D61), `open-questions.md` (5 OQ
status-updates, no new OQ number), `docs/adr/0034` / `0035` / `0036`
(§Status-update-v0.18.4 appends), `docs/adr/0030` (§Status-update-v0.18.4 Item Z),
`pyproject.toml` + `roomestim/__init__.py` (version line), `RELEASE_NOTES_v0.18.4.md`
(new). **No `roomestim/`, `roomestim_web/`, or `tests/` functional change.**

Unlike v0.18.2 (which added one executable regression-lock test), **this cycle
adds NO test** — every OQ disposition is a policy statement about externally-gated
work, and the relevant invariants (adapter `objects=[]`, ObjectKind closed Literal,
sidecar schema, notes/algorithm round-trip exclusion) are **already locked** by
existing tests (see §5 negative-control gates). This makes the PATCH case for
v0.19 even cleaner than for v0.18.2.

---

## 1. Ground truth — verified trigger states (read at `c05c34e`, v0.18.3)

I independently verified each OQ's premise and D26 trigger against the live repo.
**All five premises are confirmed accurate; all five triggers are UNMET.**

| OQ | Premise claim | Verified code reality | Trigger state |
|---|---|---|---|
| **OQ-34** cylinder/curved column | `ObjectKind` = box-only `"column"` | `roomestim/model.py:133` → `ObjectKind = Literal["column", "door", "window"]`; **no** `shape` field, **no** `cylinder`/`CurvedObject` anywhere | (a) user cylinder/arch request ≥1 — **0**; (b) acoustic model swapped to non-rectilinear — **NO** (still shoebox ISM) → **UNMET** |
| **OQ-35** USDZ/gLTF acoustic metadata std | sidecar schema = `"v0.1-internal"` (non-standard) | `roomestim/export/gltf.py:191` `_build_acoustics_sidecar()`; `cli.py:124` `--with-acoustics-sidecar`; schema string non-standard, confirmed | (a) Apple/Khronos acoustic-metadata standard published — **NO** standard exists; (b) external tool (Unreal/SPARTA) import request ≥1 — **0** → **UNMET** |
| **OQ-36** `--schema 0.1` downgrade CLI flag | external consumer fails on `objects` unknown field; spatial_engine consumes only `layout.yaml` | **Write-side library machinery already exists**: `room_yaml.py:105` `room_model_to_dict(room, schema_version="0.1"\|"0.1-draft")` + `:167` `write_room_yaml(schema_version=...)` already write a downgraded file (objects omitted on legacy). **No CLI `--schema` flag** (`cli.py` grep → 0 hits). spatial_engine consumes **only** `layout.yaml` — confirmed `docs/adr/0034-object-schema.md:142` ("외부 spatial_engine은 layout.yaml만 소비, room.yaml 미사용") | external consumer fail report ≥1 — **0** (the sole real consumer never reads `room.yaml`) → **UNMET** |
| **OQ-37** `PlacedSpeaker.notes` round-trip (`x_notes`) | `notes` not serialized by writer; reader fills `""` | `roomestim/model.py:247` `notes: str = ""`; `placement_yaml_reader.py` has **no** `notes` handling (reader never restores it); `write_layout_yaml` does not emit it. ADR 0036 §C explicitly excludes `notes` from the Level-1 contract | per-speaker note-preservation request ≥1 OR nudge notes-loss report — **0** → **UNMET** |
| **OQ-38** `target_algorithm` full round-trip (`x_target_algorithm`) | reader infers only WFS-vs-VBAP; DBAP/AMBISONICS collapse to "VBAP" | `placement_yaml_reader.py:67-76` — comment "Infer target_algorithm from regularity_hint (best-effort; not stored in YAML)"; only `x_wfs_f_alias_hz` presence + `LINEAR` → WFS, else VBAP. DBAP/AMBISONICS **collapse to "VBAP" on read** — exactly as ADR 0036 §C documents (D50 Level-1 explicit exclusion) | DBAP/AMBISONICS nudge round-trip label-loss report ≥1 OR engine algorithm-aware validation — **0** (no DBAP/AMBISONICS edit workflow exercised; engine validation is geometry-only) → **UNMET** |

**Search for ANY user-report signal** (`git log --all --grep`, `docs/`, `.omc/`,
issue/feedback files) for cylinder / x_notes / x_target_algorithm / `--schema 0.1`
/ acoustic-metadata-standard / external-import: **0 hits.** No commit, doc, or
artifact records a fired trigger for any of the five.

---

## 2. OQ-36 — the hard wall — **D57 (forced decision: permanent deferral / WONTFIX)**

### 2.1 Why OQ-36 is the anchor (D26 mechanics)

`open-questions.md:489-499` records OQ-36 with **"Evaluation cadence: v0.19 cycle
시작"** AND it is the only v0.19-queued OQ originally forward-referenced as a
**hard wall** (ADR 0030 §Status-update-v0.17 + ADR 0034 §B + D44 forward-ref).
D26 forbids indefinite deferral; v0.19 must **decide**, not silently re-defer.

### 2.2 Decision matrix

| # | Option | Effort | Risk | YAGNI/D26 read |
|---|---|---|---|---|
| 1 | Add `roomestim export --schema 0.1` CLI flag (objects omitted, `version: 0.1-draft` write) | LOW (library write-path already exists) | LOW-MED | Builds a downgrade path **no consumer needs** — spatial_engine reads only `layout.yaml`; 0 fail reports |
| 2 | Emit `objects` under an `x_objects` extension key in the legacy schema | MED | MED | Pollutes the locked 0.1 schema with a forward-compat hack for a non-existent consumer |
| 3 | **Permanent deferral / WONTFIX** (the OQ's own candidate 3 "영구 deferral — 보고 0건이면 YAGNI") with an explicit reverse-criterion | doc-only | LOW | **Honest with verified facts**: 0 fail reports; sole consumer never reads `room.yaml`; library can already downgrade if a programmatic caller ever needs it |

### 2.3 D57 (recommended — forced WONTFIX)

> **D57** — OQ-36 (`room.yaml --schema 0.1` 다운그레이드 export flag) 의 D26
> **hard-wall forced decision**: **영구 deferral / WONTFIX**. 근거:
> (1) 유일한 실제 consumer 인 `spatial_engine` 은 `layout.yaml` **만** 소비하고
> `room.yaml` 은 읽지 않는다 (`docs/adr/0034-object-schema.md:142` 명시). 따라서
> "0.2-draft `objects` unknown field 로 외부 consumer fail" 이라는 trigger 의
> 전제가 성립하는 consumer 가 **존재하지 않는다**. (2) trigger ("외부 consumer
> fail 보고 ≥ 1건") = **0건** (git log / docs / artifacts 전수 grep, 0 hits).
> (3) **다운그레이드 기능 자체는 이미 라이브러리 레벨에 존재**한다 —
> `room_model_to_dict(room, schema_version="0.1"|"0.1-draft")` + `write_room_yaml(
> schema_version=...)` 가 legacy schema (objects 생략) 를 이미 write 한다
> (`roomestim/export/room_yaml.py:105/167`). 미존재 항목은 **CLI 노출 (`--schema`
> 플래그)** 뿐이며, 프로그래매틱 caller 가 다운그레이드가 필요하면 오늘도 가능하다.
> 따라서 CLI 플래그를 추가하는 것은 0-consumer 기능에 대한 surface-area 증가 =
> YAGNI. **이것은 무기한 deferral 이 아니라 D26 hard-wall 에서의 정식 close
> (WONTFIX) 이다** — cadence 가 다시 미정으로 돌아가지 않는다.
>
> **Rejected alternatives:**
> - **옵션 1 (CLI `--schema 0.1` 플래그)** — 0 consumer / 0 fail report 에
>   사용자-노출 표면을 추가. 라이브러리 write-path 가 이미 존재하므로 긴급
>   프로그래매틱 needs 는 충족 가능. premature.
> - **옵션 2 (`x_objects` extension key in 0.1)** — locked 0.1 schema 를 존재하지
>   않는 consumer 를 위해 오염. ADR 0004 schema lock-in 위반 방향.
> - **무기한 재연기 (cadence 만 갱신)** — D26 hard-wall 직접 위반 (이번 사이클이
>   바로 그 wall).
>
> **Reverse-criterion (WONTFIX 해제 조건):** 다음 중 하나라도 충족되면 v0.20+ 에서
> `roomestim export --schema 0.1` CLI 플래그를 신설하고 (라이브러리 write-path 를
> CLI 로 노출만; 신규 ADR 불필요, ADR 0034 §Status-update 로 기록) 회귀 테스트를
> 추가한다 — (a) `room.yaml` 을 직접 소비하는 외부 consumer 가 0.2-draft `objects`
> unknown field 로 fail 보고 ≥ 1건, OR (b) spatial_engine (또는 신규 consumer) 이
> `room.yaml` 소비를 도입하면서 0.1-draft 만 지원. 이 두 경우 전까지 OQ-36 은
> **CLOSED (WONTFIX)** 이다.
> **Scope**: policy (no code; library write-path 불변). **ADR ref**: ADR 0034
> §Status-update-v0.18.4 + ADR 0030 §Status-update-v0.18.4 Item Z.

**OQ-36 disposition: CLOSED (WONTFIX).** This is the one OQ that LEAVES the open
list this cycle (mark `[x]` with WONTFIX + reverse-criterion; D26 hard-wall is
satisfied by a real decision, not another re-defer).

---

## 3. OQ-34/35/37/38 — soft re-defer with EXPLICIT new cadences (D58–D61)

D26 forbids open-ended deferral: each re-defer **must name a concrete next
cadence**. None of these four has a hard wall yet, but each gets a specific
version cadence so the audit trail never goes indefinite.

### 3.1 D58 — OQ-34 (cylinder/curved column) → re-defer to **v0.21 cycle**

> **D58** — OQ-34 (곡선/원형 column 지원) 재연기. 두 trigger 모두 미충족 (사용자
> cylinder/arch 요청 0건; acoustic 모델은 여전히 rectilinear shoebox ISM). 곡선
> 근사는 ISM order 50 에서 surface 수 n배 → 계산비용 n배 (실측 needs 없는
> 성능회귀). **신규 cadence: v0.21 cycle 시작 시 재검토** (v0.20 은 OQ-33 잔여
> auto-detection 의 hard wall 에 예약되어 있어 object-schema 변경과 충돌 회피;
> OQ-34 는 그 한 사이클 뒤). **Reverse if (조기 escalate):** 사용자 cylinder/arch
> column 요청 ≥ 1건 OR acoustic 모델이 non-rectilinear surface 버전으로 교체 →
> 그 시점에 ADR 0034 §Status-update + `shape: Literal["box","cylinder"]` 필드
> (resolution candidate 1) 검토. **Scope**: policy (no code). **ADR ref**: ADR
> 0034 §Status-update-v0.18.4.

### 3.2 D59 — OQ-35 (USDZ/gLTF acoustic metadata standard) → re-defer to **v0.21 cycle**

> **D59** — OQ-35 (USDZ/gLTF acoustic metadata 표준) 재연기. 두 trigger 모두
> 미충족 (Apple/Khronos acoustic-metadata 표준 **미공개**; Unreal/SPARTA 등 외부
> 도구 import 요청 0건). sidecar 는 `"v0.1-internal"` 로 동작 중이며 외부 표준이
> 없는 상태에서 roomestim 자체 spec 을 동결하는 것은 premature (표준 등장 시
> 재작업 비용). **신규 cadence: v0.21 cycle 시작 시 재검토** (외부 표준 의존이므로
> OQ-34 와 동일 사이클로 묶어 재검토 효율화). **Reverse if (조기 escalate):**
> Vision Pro/Apple RoomPlan acoustic metadata 표준 공개 OR 외부 도구 acoustic
> import 요청 ≥ 1건 → ADR 0035 §E 확장 (§G reverse-criterion (iv) = OQ-35
> closure 경로). **Scope**: policy (no code). **ADR ref**: ADR 0035
> §Status-update-v0.18.4 (신규 §Status-update 블록 — ADR 0035 최초 status-update).

### 3.3 D60 — OQ-37 (`PlacedSpeaker.notes` round-trip / `x_notes`) → re-defer to **v0.20 cycle**

> **D60** — OQ-37 (`notes` round-trip via engine `x_notes` extension) 재연기.
> trigger 미충족 (per-speaker 메모 보존 요청 0건; nudge notes-loss 보고 0건).
> `notes` 는 현재 in-memory annotation 전용 (`model.py:247` `notes: str = ""`;
> reader 가 복원 안 함; ADR 0036 §C 가 Level-1 계약에서 명시 제외). engine
> `geometry_schema.json` 이 per-speaker `additionalProperties: true` 이므로
> `x_notes` 추가는 기술적으로 가능하나 engine 측 소비/무시 정책 협의 필요.
> **신규 cadence: v0.20 cycle 시작 시 재검토** (notes 와 algorithm round-trip 은
> 둘 다 `layout.yaml` round-trip extension 이므로 OQ-38 과 같은 사이클로 묶음 —
> 한 번의 engine-schema 협의로 둘 다 평가). **Reverse if (조기 escalate):**
> per-speaker note 보존 요청 ≥ 1건 OR nudge 워크플로 notes 손실 보고 ≥ 1건 →
> `x_notes` per-speaker extension key (resolution candidate 1) + engine 협의.
> **Scope**: policy (no code). **ADR ref**: ADR 0036 §Status-update-v0.18.4
> (§C/§G(iv) 보강).

### 3.4 D61 — OQ-38 (`target_algorithm` full round-trip / `x_target_algorithm`) → re-defer to **v0.20 cycle**

> **D61** — OQ-38 (`target_algorithm` 전체 round-trip via `x_target_algorithm`)
> 재연기. trigger 미충족 (DBAP/AMBISONICS nudge round-trip 라벨-손실 보고 0건;
> engine algorithm-aware 검증 미도입). 현재 `placement_yaml_reader.py:67-76` 은
> `regularity_hint` + `x_wfs_f_alias_hz` 존재로 WFS-vs-VBAP 만 추론 → DBAP/AMBISONICS
> layout 은 read 시 "VBAP" 로 붕괴 (D50 Level-1 계약에서 명시 제외). 편집은 좌표만
> 다루고 알고리즘은 `place` 재실행으로 결정한다는 D50 설계가 의도적. **신규 cadence:
> v0.20 cycle 시작 시 재검토** (OQ-37 과 동일 — 둘 다 layout extension key,
> 한 번의 engine 협의로 묶음 평가). **Reverse if (조기 escalate):** DBAP/AMBISONICS
> layout nudge round-trip 후 알고리즘 라벨 손실 보고 ≥ 1건 OR engine 이
> algorithm-aware 검증 도입 → top-level `x_target_algorithm` extension key
> (writer emit + reader 복원, WFS 추론보다 우선; resolution candidate 1).
> **Scope**: policy (no code). **ADR ref**: ADR 0036 §Status-update-v0.18.4
> (§C 보강).

### 3.5 Cadence summary (no indefinite deferral — D26 compliant)

| OQ | Disposition | New cadence | Hard wall? |
|---|---|---|---|
| OQ-36 | **CLOSED (WONTFIX)** — forced decision | n/a (closed) | was the v0.19 hard wall — SATISFIED |
| OQ-37 | re-defer | **v0.20** | no (soft) |
| OQ-38 | re-defer | **v0.20** | no (soft) |
| OQ-34 | re-defer | **v0.21** | no (soft) |
| OQ-35 | re-defer | **v0.21** | no (soft) |

> **Cadence-collision note (D34 discipline):** v0.20 already hosts a hard wall —
> OQ-33's adapter auto-detection residual (D54). Grouping OQ-37/OQ-38 (both
> `layout.yaml` extension keys) at v0.20 is deliberate: they are evaluated
> alongside any engine-schema consultation that v0.20 may already need, NOT bundled
> into the OQ-33 decision. OQ-34/OQ-35 (both externally-gated, object/format
> schema) sit at v0.21 to avoid colliding with v0.20's object-schema hard wall.

---

## 4. Scope (IN / OUT / 영구거부)

### IN (default — doc-only)
- `.omc/plans/decisions.md`: **D57** (OQ-36 WONTFIX) + **D58** (OQ-34) + **D59**
  (OQ-35) + **D60** (OQ-37) + **D61** (OQ-38) + a "New ADR this cycle: none" /
  "New OQ: none" trailer block (per the D56 / D54 precedent format).
- `.omc/plans/open-questions.md`: **5 status-update blocks** under one **VERSION-named**
  section header `## v0.18.4 OQ status-updates (2026-05-25)` (mirrors the v0.18.2
  precedent `## v0.18.2 OQ status-updates` at `open-questions.md:531`; per-block
  headers like `### OQ-36 status-update (v0.18.4 — CLOSED/WONTFIX)` — version-named,
  with "v0.19-cycle cadence" noted in the block prose, NOT cadence-named in the
  heading). OQ-36 marked CLOSED (WONTFIX) with reverse-criterion; OQ-34/35/37/38
  marked re-deferred with the new concrete v0.20/v0.21 cadence. **No new OQ number**
  (no new deferral is created; the existing five are dispositioned). Append-only —
  do NOT retroactively edit the existing OQ-34/35/36/37/38 bodies; append a dated
  v0.18.4 status section at EOF.
- `docs/adr/0034-object-schema.md`: **§Status-update-v0.18.4** append — OQ-36 WONTFIX
  (room.yaml downgrade) + OQ-34 re-defer to v0.21. (Above the existing
  §Status-update-v0.18.2 body; no retroactive edit.)
- `docs/adr/0035-mesh-export-policy.md`: **§Status-update-v0.18.4** append (ADR 0035's
  FIRST status-update) — OQ-35 re-defer to v0.21; cite §G reverse-criterion (iv).
- `docs/adr/0036-layout-round-trip-nudge.md`: **§Status-update-v0.18.4** append — OQ-37
  + OQ-38 re-defer to v0.20; cite §C / §G(iv). (Above the existing
  §Status-update-v0.18.3 body; no retroactive edit.)
- `docs/adr/0030-predictor-default-switch.md`: **§Status-update-v0.18.4 Item Z** append
  (the running per-version index; last item was "Item Y" at v0.18.3). One-paragraph
  cross-ref that v0.18.4 is the v0.19-cycle doc-only OQ re-examination, predictor
  cascade §A–§E byte-equal, web byte-equal, schema unchanged.
- `pyproject.toml` + `roomestim/__init__.py`: version `0.18.3` → **`0.18.4`** (PATCH).
- `RELEASE_NOTES_v0.18.4.md` (new) — §What v0.18.4 decides (OQ-36 WONTFIX + 4
  re-defers), §What stays the same (RoomModel frozen, ObjectKind closed Literal,
  ADR 0009 invariant, schema `0.2-draft`, web byte-equal, all code untouched).

### OUT (this cycle — explicit)
- **Any CLI `--schema 0.1` flag** (OQ-36 option 1) — D57 WONTFIX; library write-path
  stays as-is, unexposed.
- **`shape`/cylinder field, `CurvedObject`** (OQ-34) — re-deferred to v0.21.
- **sidecar schema bump / KHR extension** (OQ-35) — re-deferred to v0.21.
- **`x_notes` / `x_target_algorithm` extension keys, reader/writer changes**
  (OQ-37/38) — re-deferred to v0.20.
- **Any new ADR** — re-examination/re-defer is recorded via §Status-update appends
  per project pattern (D54/D56 precedent); no ADR 0037.
- **Any new test** — no functional change to lock; existing tests already cover the
  invariants this cycle re-affirms (§5 negative-control gates).
- **OQ-33 (v0.20 wall) / OQ-39 (v0.21 ADR-split)** — untouched; their cadences
  (v0.20 / v0.21) are unaffected.

### 영구거부 (this cycle)
- Reopening OQ-36 as anything but WONTFIX without a fired reverse-criterion.
- A MINOR `0.19.0` bump (no feature, no schema, no API change ships — §6).

---

## 5. Acceptance gates (interpreter `/home/seung/miniforge3/bin/python`; `python` not on PATH)

> **Baseline note (env drift, non-blocking):** assert "no regression vs the count
> observed at execution start," not a hardcoded integer (v0.15.x–v0.18.3 precedent
> for optional-dep skip drift). Re-baseline at execution start. v0.18.3 verifier
> baseline: default 271 passed / 6 skipped; web 70 passed / 1 skipped.

1. **G1 — default lane no-regression**: `/home/seung/miniforge3/bin/python -m pytest -m 'not lab and not web' -q` → all pass, count == start-of-cycle baseline (~271; **doc-only ⇒ ZERO new/removed tests** — count must be byte-equal to baseline, not just ≥).
2. **G2 — web lane no-regression**: `/home/seung/miniforge3/bin/python -m pytest tests/web/ -q` → all pass, count == baseline (~70).
3. **G3 — ruff clean**: `/home/seung/miniforge3/bin/python -m ruff check roomestim/ roomestim_web/ tests/` → All checks passed.
4. **G4 — mypy strict**: `/home/seung/miniforge3/bin/python -m mypy --strict roomestim/` → 0 errors (39 source files).
5. **G5 — D29 direction invariant**: `grep -rn 'import roomestim_web' roomestim/` → 0 hits.
6. **G6 — NO functional code change (the core doc-only assertion)**: `git diff <predecessor c05c34e> -- roomestim/ roomestim_web/ tests/` shows **ONLY** the single `roomestim/__init__.py` version line (1 ins / 1 del). Any other `.py` hunk FAILS the gate.
7. **G7 — D57 present (OQ-36 WONTFIX)**: `grep -n 'D57' .omc/plans/decisions.md` → ≥ 1 hit.
8. **G8 — D58–D61 present**: `grep -nE 'D58|D59|D60|D61' .omc/plans/decisions.md` → 4 hits.
9. **G9 — OQ statuses updated**: `grep -n 'v0.18.4 OQ status-updates' .omc/plans/open-questions.md` → 1 hit (the new VERSION-named section); the 5 sub-blocks show the new disposition; OQ-36 marked CLOSED/WONTFIX; OQ-34/35/37/38 show explicit v0.20/v0.21 cadence; **no new `OQ-4x` number introduced** (`grep -oE 'OQ-[0-9]+' .omc/plans/open-questions.md | sort -t- -k2 -n | tail -1` stays `OQ-39`).
10. **G10 — ADR §Status-update-v0.18.4 appends (append-only, prior body byte-equal)**: `grep -n 'Status-update-v0.18.4' docs/adr/0030-*.md docs/adr/0034-*.md docs/adr/0035-*.md docs/adr/0036-*.md` → ≥ 1 hit each (4 files); for each, `git diff <pred>` shows ONLY an appended block (zero deletions above the new section).
11. **G11 — RT60 negative control byte-equal** (acoustic path provably untouched): RT60 invariant value `1.9190766987173207` still asserted; `/home/seung/miniforge3/bin/python -m pytest tests/test_objects_acoustic_invariant.py tests/test_predict_rt60_default.py -q` → pass.
12. **G12 — adapter/object invariants still locked** (re-affirmed dispositions are real): existing `tests/test_oq33_residual_lock.py` + `tests/test_objects.py` (or equivalents) still pass — confirms ObjectKind closed Literal + adapter `objects=[]` unchanged (covers OQ-34/OQ-36 premises). Run: `/home/seung/miniforge3/bin/python -m pytest tests/test_oq33_residual_lock.py -q` → pass.
13. **G13 — web byte-equal (D30)**: `git diff <predecessor c05c34e> -- roomestim_web/` → 0 bytes.
14. **G14 — versions**: `roomestim.__version__ == "0.18.4"`, `roomestim_web.__version__ == "0.15-web.0"` (unchanged), `__schema_version__ == "0.2-draft"` (unchanged), `pyproject.toml version == "0.18.4"`. `grep -n '__version__\|^version' roomestim/__init__.py roomestim_web/__init__.py pyproject.toml`.

**Gate count: 14.** All gates must stay GREEN; G6 is the load-bearing "doc-only,
no functional code change" assertion.

---

## 6. Versioning (D30) — the one genuine open decision

| Path | `roomestim` / `pyproject` | `roomestim_web` | `__schema_version__` | Release? |
|---|---|---|---|---|
| **Recommended: doc-only OQ re-examination** | `0.18.3` → **`0.18.4`** (PATCH) | **불변** `0.15-web.0` | **불변** `"0.2-draft"` | PATCH tag, `RELEASE_NOTES_v0.18.4.md` |
| ~~Alt A: design-doc-only, NO release~~ (REJECTED by user) | unchanged `0.18.3` | unchanged | unchanged | none (decisions/ADR/OQ appends only) |
| Alt B: MINOR `0.19.0` | `0.18.3` → `0.19.0` | unchanged | unchanged | MINOR tag |

**Recommendation: PATCH `0.18.4`.** Rigorous justification:

1. **SemVer policy on record (D30 + project precedent).** The project's only
   written versioning rule is D30 (web parallel-track string; core stays on the
   acoustics SemVer). The de-facto cadence across v0.15.1 / v0.16.1 / v0.18.1 /
   v0.18.2 / v0.18.3 is: **MINOR = new user-facing feature/schema/API; PATCH =
   fix / doc / re-defer / precision-tighten with no new feature.** A MINOR is
   reserved for shipped capability (v0.18.0 nudge MINOR; v0.17.0 Object schema
   MINOR).
2. **This cycle ships ZERO new capability.** It is a re-examination: one forced
   WONTFIX + four re-defers. No CLI flag, no schema field, no API, no behavior
   change. By the project's own rule that is a PATCH.
3. **It is even *more* clearly PATCH than v0.18.2 was.** v0.18.2 (the prior
   "v0.19-cycle" OQ resolution) added one executable regression-lock test and
   still chose PATCH `0.18.2`. **This cycle adds no test at all** (existing tests
   already lock the invariants). The precedent therefore strictly dominates: if
   v0.18.2 was PATCH, v0.19's pure-doc cycle is unambiguously PATCH.
4. **The "v0.19 cycle" label is a cadence name, not a version mandate.** The same
   thing happened at v0.18.2 (the cadence was literally "v0.19까지 결정 강제" yet
   shipped as PATCH `0.18.2`). The cadence label tracks *which OQs are due*, not
   *what version they bump*.

**A MINOR (`0.19.0`) is NOT justified** unless the user opts to actually BUILD one
of the deferred features (e.g. the OQ-36 CLI flag) — which D57 rejects. **Alt A
(no release)** was offered but **REJECTED by the user (2026-05-25)**: this ships as
a full **PATCH `0.18.4` release commit** per the v0.18.1/0.18.2/0.18.3 cadence, so
the full gate set (G1–G14, including G14 version assertions and a
`RELEASE_NOTES_v0.18.4.md`) applies. Alt A's "skip G14, doc gates only" path is
recorded for completeness only and is NOT the chosen path.

---

## 7. Numbering allocation (confirmed by reading the files)

| Registry | Current top (verified at `c05c34e`) | This cycle allocates |
|---|---|---|
| `.omc/plans/decisions.md` | **D56** (line 1629 tail) | **D57** (OQ-36 WONTFIX) + **D58** (OQ-34) + **D59** (OQ-35) + **D60** (OQ-37) + **D61** (OQ-38) |
| `.omc/plans/open-questions.md` | **OQ-39** (max number; OQ-36/37/38 are the v0.19-queued) | **No new OQ number** — 5 status-updates on existing OQ-34/35/36/37/38 |
| `docs/adr/` | **0036** | **No new ADR** (re-examination = §Status-update appends; ADR 0037 stays reserved for v0.20 OQ-33 auto-detection per D54) |

**New ADR needed? No.** Per the project's own pattern (D54 re-defer, D56 PATCH),
re-examination/re-defer is recorded via ADR §Status-update + decisions + OQ status,
not a fresh ADR. ADR 0037 remains reserved for v0.20 (OQ-33 auto-recognition).

**New OQ needed? No.** This cycle dispositions the five already-open OQs; it does
not spawn new deferred work. (Contrast v0.18.2, which spawned OQ-39 for the ADR
0030 split — that was a *newly surfaced* concern, not a disposition of an existing
OQ. No analogous new concern surfaces here.)

---

## 8. Risk / rollback

| Risk | Likelihood | Mitigation / Rollback |
|---|---|---|
| OQ-36 WONTFIX mistaken as "feature shipped" | Low | D57 + RELEASE_NOTES state CLOSED-WONTFIX explicitly; reverse-criterion enumerated; G7 asserts D57 present |
| A re-defer drifts to indefinite (D26 violation) | Low | D58–D61 each name a concrete v0.20/v0.21 cadence; §3.5 table + G9 assert the cadence text is present |
| Functional code accidentally touched | Very low | **G6** = `git diff -- roomestim/ roomestim_web/ tests/` shows ONLY the version line; doc-only scope means no module edits |
| Acoustic path drift | Very low | G11 RT60 byte-equal `1.9190766987173207`; no `roomestim/reconstruct/` edit |
| Append-only discipline broken (retroactive ADR/OQ edit) | Low | G10 asserts every ADR diff is a pure tail-append (zero deletions above new section); same for OQ status |
| Web byte-equal violated | Very low | G13 `git diff -- roomestim_web/` = 0 bytes |
| Cadence collision with v0.20 OQ-33 wall | Low | §3.5 note: OQ-37/38 grouped at v0.20 are evaluated *alongside* (not bundled into) OQ-33; OQ-34/35 sit at v0.21 to avoid the object-schema wall |
| Rollback | n/a | Doc + version line only — `git revert` of the single commit fully restores `0.18.3` |

---

## 9. Concrete edit plan (ordered; mechanical for the executor)

> All edits are **append-only** except the two version-line replacements. Read each
> target file's tail before appending. Do NOT retroactively edit prior ADR/OQ/D
> bodies (D22 / D28-P1).

**Step 1 — `.omc/plans/decisions.md`** (append at EOF, after line 1629 D56 block):
add a new **VERSION-named** section header `## v0.18.4 D-decision allocation
(2026-05-25)` (mirrors the precedent `## v0.18.2 D-decision allocation` at
`decisions.md:1553`; the "v0.19-cycle" cadence is noted in the D57 block prose, not
the header) then,
in order, the **D57** (OQ-36 WONTFIX), **D58** (OQ-34), **D59** (OQ-35), **D60**
(OQ-37), **D61** (OQ-38) blocks verbatim from §2.3 / §3.1–§3.4, followed by a
trailer: `**New ADR this cycle**: none (re-examination = §Status-update appends).
ADR 0030/0034/0035/0036 §Status-update-v0.18.4 append. 신규 OQ: none.`

**Step 2 — `.omc/plans/open-questions.md`** (append at EOF, after line 592): add a
new **VERSION-named** section `## v0.18.4 OQ status-updates (2026-05-25)` (mirrors
the precedent `## v0.18.2 OQ status-updates` at `open-questions.md:531`) with FIVE
sub-blocks (version-named headers; "v0.19-cycle cadence" noted in block prose):
- `### OQ-36 status-update (v0.18.4 — CLOSED / WONTFIX, D57)` — D26 hard-wall forced
  decision = WONTFIX (fulfills the v0.19-cycle hard-wall cadence); cite the 0-report
  + spatial_engine-`layout.yaml`-only evidence + library-write-path-already-exists;
  reverse-criterion (a)/(b); mark OQ-36 `[x]` CLOSED.
- `### OQ-37 status-update (v0.18.4 — re-deferred to v0.20, D60)`.
- `### OQ-38 status-update (v0.18.4 — re-deferred to v0.20, D61)`.
- `### OQ-34 status-update (v0.18.4 — re-deferred to v0.21, D58)`.
- `### OQ-35 status-update (v0.18.4 — re-deferred to v0.21, D59)`.
  Each re-defer block: trigger-still-unmet evidence + new explicit cadence +
  reverse-if escalation. Do NOT edit the original OQ-34..38 bodies (lines 442-527).

**Step 3 — `docs/adr/0034-object-schema.md`** (append a new `## §Status-update-v0.18.4
(2026-05-25)` block ABOVE the existing `## §Status-update-v0.18.2` at line 183 —
follow the "위 §Status-update-… 본문 위에 append" convention): record OQ-36 WONTFIX
(room.yaml downgrade; library write-path already exists; CLI flag = YAGNI) + OQ-34
re-defer to v0.21 (§D "곡선/원형 객체" line). Cross-ref D57/D58. State predictor
cascade / schema unchanged.

**Step 4 — `docs/adr/0035-mesh-export-policy.md`** (append the FIRST
`## §Status-update-v0.18.4 (2026-05-25)` block after the References section / EOF,
line ~194): record OQ-35 re-defer to v0.21; cite §G reverse-criterion (iv) as the
closure path; D59 cross-ref.

**Step 5 — `docs/adr/0036-layout-round-trip-nudge.md`** (append a new
`## §Status-update-v0.18.4 (2026-05-25)` block ABOVE the existing
`## §Status-update-v0.18.3` at line 145): record OQ-37 + OQ-38 re-defer to v0.20;
cite §C (Level-1 exclusion of notes + DBAP/AMBISONICS) and §G(iv); D60/D61 cross-ref.

**Step 6 — `docs/adr/0030-predictor-default-switch.md`** (append a new
`## §Status-update-v0.18.4 (2026-05-25)` block ABOVE the existing
`## §Status-update-v0.18.3` at line 414, with header note "위 §Status-update-v0.18.3
본문 위에 append; retroactive 수정 없음"): add **Item Z** (the running per-version
index: W=v0.18.1 → X=v0.18.2 → Y=v0.18.3 → **Z=v0.18.4**; verified last letter
allocated is Y at `0030:420`) — header **"Item Z — v0.19-cycle OQ 재검토 (doc-only
PATCH)"**; body: this is the v0.19-cycle OQ re-examination cadence commitment
landing as PATCH 0.18.4 (OQ-36 WONTFIX + OQ-34/35/37/38 re-defer, D57–D61);
predictor cascade §A–§E byte-equal (doc-only — acoustic/schema code 미접촉); web
byte-equal `0.15-web.0` (D30); `__schema_version__` `0.2-draft` 불변;
`roomestim.__version__` `0.18.3` → `0.18.4` (PATCH); New ADR: none; 신규 OQ: none.

**Step 7 — `pyproject.toml`**: replace `version = "0.18.3"` → `version = "0.18.4"`
(line 7).

**Step 8 — `roomestim/__init__.py`**: replace `__version__ = "0.18.3"` →
`__version__ = "0.18.4"` (line 3). Leave `__schema_version__ = "0.2-draft"` (line 4)
untouched. Do NOT touch `roomestim_web/__init__.py`.

**Step 9 — `RELEASE_NOTES_v0.18.4.md`** (new file at repo root, mirror
`RELEASE_NOTES_v0.18.3.md` structure): §Summary (v0.19-cycle OQ re-examination,
PATCH); §What v0.18.4 decides (OQ-36 CLOSED/WONTFIX + OQ-34/35/37/38 re-deferred
with v0.20/v0.21 cadences; D57–D61); §What stays the same (RoomModel frozen,
ObjectKind closed Literal, schema 0.2-draft, web byte-equal, all functional code
untouched); §Versioning note (PATCH rationale per §6).

**Step 10 — gate run** (§5 G1–G14). Confirm G6 (no functional code diff except
version line) and G10 (append-only) explicitly. Then hand to code-reviewer +
verifier (separate passes; do NOT self-approve — per project MEMORY.md).

---

## 10. Proposed commit message (project format — mirrors recent git log)

```
roomestim v0.18.4 — v0.19-cycle OQ 재검토: OQ-36 WONTFIX (room.yaml --schema 0.1 다운그레이드 hard-wall 정식 close) + OQ-34/35/37/38 명시-cadence 재연기 (D57 NEW; D58/D59/D60/D61 NEW; OQ-36 closed; ADR 0030 §Status-update-v0.18.4 Item Z; ADR 0034/0035/0036 §Status-update-v0.18.4; 신규 ADR/OQ 없음): externally-gated OQ 전부 trigger 미충족 (0 reports) — doc-only PATCH bump
```

> **Format verified against `git log --oneline -3`** (v0.18.1/0.18.2/0.18.3):
> `roomestim v{X} — {headline} ({D-allocations}; {OQ status}; {ADR
> §Status-update-v{X} [Item _ for ADR 0030]}; 신규 ADR/OQ status): {one-line
> rationale} — {bump} bump`. This mirrors v0.18.2's commit (`aae5514`:
> "...재연기 (D54 NEW; D55 OPTIONAL/deferred; OQ-39 NEW; OQ-33 status-update; ADR
> 0034 §Status-update-v0.18.2; ADR 0030 §Status-update-v0.18.2 Item X; 신규 ADR
> 없음): v0.19-cycle OQ-33 결정 — doc-only PATCH bump") and v0.18.3's
> (`c05c34e`: "...(D56 NEW; ADR 0036 §Status-update-v0.18.3; ADR 0030
> §Status-update-v0.18.3 Item Y; 신규 ADR/OQ 없음): ... — PATCH bump"). Key
> points: ADR refs are **version-named `v0.18.4`** (NOT `v0.19`); ADR 0030 carries
> the **Item Z** index letter (W=v0.18.1 → X=v0.18.2 → Y=v0.18.3 → **Z=v0.18.4**);
> OQ-36 is the headline (the hard wall); D57 leads (NEW); `OQ-36 closed` flags the
> one OQ that leaves the open list. Per the cwm:push protocol, append the
> `Co-Authored-By` trailer the project uses if committing via that flow.

---

## 11. Handoff note

This is a **doc-only OQ re-examination** plan (SHORT). It builds no feature.
**User-confirmed (2026-05-25):** ship as **PATCH `0.18.4`** with a release commit
per the v0.18.1/0.18.2/0.18.3 cadence (Alt A "no release" REJECTED). It is ready
for `/oh-my-claudecode:start-work v0.18.4-patch`. If a future request instead wants
any deferred feature BUILT (e.g. the OQ-36 CLI `--schema 0.1` flag), that
**overrides D57** and becomes a separate feature cycle (likely MINOR `0.19.0`)
requiring a fresh interview and possibly ADR 0037 — flag that as a scope change
before any execution.

Per project MEMORY.md: this must go through planner → executor → code-reviewer →
verifier with no self-approval; the executor pass applies §9 mechanically, then a
separate code-reviewer pass and a separate verifier pass run the §5 gates.
