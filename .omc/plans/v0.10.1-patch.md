# roomestim v0.10.1 Patch — Design Plan (factual-integrity-only fast follow-up)

> **PATCH NATURE NOTE (2026-05-10b)**: v0.10.1 is a **factual-integrity-only**
> patch on top of v0.10.0 (`90aa6f1`). It addresses three v0.10 critic +
> architect findings that are concrete, scope-bounded, and shippable within
> 24h: (i) fabricated `1.121 s` quote in ADR 0018 §Drivers item 2 (no
> citation, contradicts v0.10's own §A.2 "no authoritative dims" claim);
> (ii) `tests/fixtures/soundcam_synthesized/README.md` body lines 27/31/41-43
> still describe v0.9 placeholder regime in present tense beneath a v0.10
> §Honesty-correction prepend; (iii) audit-trail discipline pattern for
> same-week-old ADR corrections is not yet codified in `decisions.md`.
> v0.10.1 fixes (i) + (ii) and codifies (iii) in D22; everything else
> (disagreement-band tightening, MELAMINE_FOAM enum, mypy strict, in-situ
> protocol DOC) is DEFERRED to v0.11+ and recorded as new open questions
> OQ-13f/g/h/i.

---

- **Date**: 2026-05-10b (drafted) / 2026-05-10b (scope locked)
- **Author**: oh-my-claudecode:planner (consulted by user after v0.10.0
  critic verdict 7.6/10 composite + architect §Categorisation table
  bucketed three findings as v0.10.1 patch scope).
- **Predecessor**: v0.10.0 (`90aa6f1` — honesty correction; ADR 0016
  reverse-criterion fired; ADR 0018; living_room removed; D21).
- **Branch decision**: **v0.10.1 patch** (not v0.11 minor — v0.11 is the
  hybrid-scope follow-up that adds MELAMINE_FOAM + protocol DOC; v0.10.1
  ships only the factual-integrity fixes that have no scope-creep risk).
- **Status**: SCOPE LOCKED — diff envelope target 50–80 lines (slightly
  above architect's ≤50 estimate due to NEW `RELEASE_NOTES_v0.10.1.md`
  ~50 lines); 8 file ops total (7 modified + 1 new + 0 deleted); 0 test
  logic changes; default-lane test count stays **116**.
- **Mode**: SHORT (not deliberate; patch nature; pre-mortem + expanded
  test plan are NOT required at this scope).

This plan is a **fast follow-up** to v0.10. It does NOT redesign,
deprecate, or extend any v0.10 invariant. It corrects a single fabricated
quote, a single tense-mismatch in a fixture README, codifies the
audit-trail-discipline pattern that v0.10.1 itself is executing, and
records four new open questions surfaced by the v0.10 critic + architect
verdicts.

---

## §0 Locked-scope summary

### §0.1 Files touched (modified + new + deleted)

| Lock | Choice | Rationale |
| --- | --- | --- |
| Branch decision | **v0.10.1 patch** (not v0.11 minor; not v0.10.0 amend) | Factual-integrity issues require a patch; v0.10 is already shipped on origin/main; amending the v0.10 commit would rewrite published history. v0.10.1 is additive-only on top of `90aa6f1`. |
| Diff envelope | **50–80 lines total** | Architect estimated ≤50; the `RELEASE_NOTES_v0.10.1.md` NEW file pushes total ~50–80. Anything above 80 is scope creep — STOP and re-plan. |
| File ops | **7 modified + 1 new + 0 deleted = 8 ops** | See §0.2 table below. |
| Test count | **116 (unchanged from v0.10.0)** | NO test logic changes; NO test additions; NO test removals. v0.10.1 is doc/audit-only. |
| Version bump | **`0.10.0` → `0.10.1`** in `pyproject.toml` + `roomestim/__init__.py` (`__version__` only) | SemVer patch bump. `__schema_version__` stays `"0.1-draft"` (Stage-2 schema state is unchanged from v0.10). |
| ADR 0018 body strategy | **Hybrid: in-line redaction at line 46 + appended §Status-update-2026-05-10b block** | Mirrors v0.10's `RELEASE_NOTES_v0.9.0.md` §Honesty-correction prepend pattern. In-line redaction keeps ADR readable; §Status-update records the WHY for audit-trail discipline. NOT pure-append-only with `<del>`/`<ins>` HTML markup (rejected as unreadable in a markdown ADR). |
| Disagreement-band asymmetry (lab) | **DEFERRED to v0.11** under OQ-13f | v0.10's lab `predicted_s_min/max=[0.235, 0.270]` band has razor-thin margin against a hypothetical CARPET 0.30→0.20 perturbation; tightening is a v0.11 hybrid-scope item, not a v0.10.1 factual-integrity fix. |
| MELAMINE_FOAM enum | **DEFERRED to v0.11** under OQ-13a | Library-coefficient revision; chains into MaterialAbsorption + MaterialAbsorptionBands + many tests. v0.10.1 minimum-leverage forbids. |
| Mypy strict cleanup | **DEFERRED to v0.12+** under OQ-13i | `roomestim/adapters/ace_challenge.py:554-556` has 3 pre-existing `float(object)` errors; the project does not currently advertise mypy-strict cleanliness. Silent fix risks scope creep. |
| In-situ protocol DOC | **DEFERRED to v0.11** under OQ-12a status-update | v0.11 ships protocol DOC only (no capture commitment). v0.10.1 records the direction in OQ-12a status-update marker. |
| MELAMINE_FOAM coefficient source amendment | **OQ-13a resolution candidate amended in v0.10.1** to specify Vorländer 2020 §11 / Appx A as PRIMARY | Matches ADR 0011 / OQ-2 / OQ-6 precedent. |

### §0.2 File-op table (8 ops total)

| # | Path | Op | Lines added | Lines removed | Lines net | Purpose |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `docs/adr/0018-soundcam-substitute-disagreement-record.md` | MOD | ~16 | ~6 | +10 | Line 46 in-line redaction (-6/+1); §Status-update-2026-05-10b appended (~14 lines new). |
| 2 | `tests/fixtures/soundcam_synthesized/README.md` | MOD | ~3 | ~3 | 0 | Body lines 27/31/41-43 tense + framing fix (NO content removal). |
| 3 | `.omc/plans/v0.10-design.md` | MOD | ~7 | ~6 | +1 | Line 702 same redaction as ADR 0018 line 46; addendum bullet appended to existing implementation note block. |
| 4 | `.omc/plans/open-questions.md` | MOD | ~25 | ~1 | +24 | OQ-13d marked `[x]` resolved (-1/+1); OQ-13f/g/h/i appended (~22 lines new); OQ-13a amended (Vorländer primary); OQ-12a status-update marker added. |
| 5 | `.omc/plans/decisions.md` | MOD | ~25 | 0 | +25 | D22 appended (audit-trail-discipline pattern). |
| 6 | `pyproject.toml` | MOD | 1 | 1 | 0 | Version `0.10.0` → `0.10.1`. |
| 7 | `roomestim/__init__.py` | MOD | 1 | 1 | 0 | `__version__` `"0.10.0"` → `"0.10.1"`; `__schema_version__` UNCHANGED `"0.1-draft"`. |
| 8 | `RELEASE_NOTES_v0.10.1.md` | NEW | ~50 | 0 | +50 | §What we got wrong in v0.10.0 / §What v0.10.1 fixes / §What stays the same / §Default-lane test count [stays 116] / §Tag local-only. |

**Total expected diff**: ~110 lines added, ~18 lines removed → net **~+92 lines** (within 50–80 *added-meaningful-content* envelope; the additions are split: ~50 in NEW RELEASE_NOTES + ~25 in D22 + ~22 in OQ block + ~14 in ADR §Status-update block + ~7 in v0.10-design = 118 new lines; offset by ~18 lines of redaction/version-bump removals). If diff exceeds 150 added lines or 30 removed lines, STOP and re-plan.

### §0.3 Files NOT touched (LOCKED OUT — byte-equal under v0.10.0 baseline `90aa6f1`)

| Family | Specific paths | Verification |
| --- | --- | --- |
| Library code | `roomestim/model.py`, `roomestim/place/wfs.py`, `roomestim/cli.py`, `roomestim/adapters/ace_challenge.py`, `roomestim/adapters/polycam.py`, `roomestim/reconstruct/floor_polygon.py`, all other `roomestim/**/*.py` except `__init__.py` | `git diff --stat 90aa6f1 -- roomestim/ \| grep -v "__init__.py" \| awk 'NR>1 {if ($1) exit 1}'` returns 0 (no entries beyond `__init__.py`). |
| Library defaults | `MaterialLabel` (9 entries), `MaterialAbsorption{,Bands}`, `_FURNITURE_BY_ROOM` (sum=276), `_PIECE_EQUIVALENT_ABSORPTION_500HZ_M2['lecture_seat']`=0.45, MISC_SOFT α₅₀₀=0.40 | Implied by `roomestim/adapters/ace_challenge.py` byte-equal. |
| Test files | All of `tests/**/*.py` (no test logic, no test count change) | `git diff --stat 90aa6f1 -- tests/` empty. |
| Test count | **Default-lane = 116** (unchanged from v0.10) | `pytest --collect-only -q tests \| tail -1` reads `116 tests collected` (or equivalent). |
| Fixture data | `tests/fixtures/soundcam_synthesized/{lab,conference}/{dims.yaml,rt60.csv,GT_corners.json}` + `LICENSE_MIT.txt` | Only `README.md` in this fixture tree changes; data files byte-equal. |
| ADRs (other than 0018) | `docs/adr/0001..0017.md` | `git diff --stat 90aa6f1 -- docs/adr/ \| grep -v "0018"` empty. |
| Schema | `proto/room_schema.json`, `proto/room_schema.draft.json` | Stage-2 file preserved; v0.10 marker `"0.1-draft"` carries forward to v0.10.1. |
| Schema marker | `__schema_version__ == "0.1-draft"` (UNCHANGED) | `roomestim/__init__.py:4` byte-equal except for `__version__` line. |
| RELEASE_NOTES (predecessor) | `RELEASE_NOTES_v0.9.0.md`, `RELEASE_NOTES_v0.10.0.md` | Both byte-equal to v0.10.0 baseline. v0.10.1 ships its OWN new file. |
| Perf docs | `docs/perf_verification_a10*_soundcam_*.md` | All byte-equal. |
| Cross-repo PR research | `.omc/research/cross-repo-pr-v0.9-proposal.md`, `.omc/research/cross-repo-pr-v0.10-deferred.md` | Both byte-equal. |
| ADR 0018 §Drivers items 1, 3, 4 + §Decision + §Alternatives + §Why-chosen + §Consequences + §Reverse-criterion + §References | All sections except §Drivers item 2 line 46 + new appended §Status-update | Body of ADR 0018 byte-equal except those two surgical edits. |
| Plan files (other than v0.10-design + open-questions + decisions) | `.omc/plans/v0.{1..9}-*.md`, `.omc/plans/v0.10-design.md` body except line 702 + addendum bullet | All byte-equal except surgical edits enumerated in §0.2. |
| Notepad / state / wiki | `.omc/notepad.md`, `.omc/state/`, `.omc/wiki/` | Untouched (planner does not modify these). |

---

## §1 Scope decision matrix

The v0.10 critic + architect verdicts surfaced multiple candidate items.
This matrix records which ship in v0.10.1, which defer to v0.11, and
which defer to v0.12+.

| # | Candidate | v0.10.1? | Defer to | Rationale |
| --- | --- | --- | --- | --- |
| C1 | ADR 0018 line 46 fabricated `1.121 s` quote redaction | **YES** | — | Factual-integrity fix; uncited quote contradicts v0.10's own §A.2 "no authoritative dims" claim. Hybrid pattern (in-line + §Status-update). |
| C2 | ADR 0018 line 46 `vaulted-ceiling open-layout (living_room) targets` extrapolation removal | **YES** | — | Same line 46 redaction; the extrapolation is uncited (paper §A.2 publishes no living_room dims, so vaulted-ceiling claim has no source). |
| C3 | `tests/fixtures/soundcam_synthesized/README.md` body lines 27/31/41-43 tense + framing | **YES** | — | v0.10 §Honesty-correction prepend was correct, but body still describes v0.9 placeholder regime in present tense ("we ship", "we DO NOT ship"). Tense-only fix; NO content removal. |
| C4 | `.omc/plans/v0.10-design.md` line 702 same redaction | **YES** | — | Same drafting residue as ADR 0018 line 46; preserve byte-equal-elsewhere with addendum bullet to existing implementation-note block. |
| C5 | OQ-13d `[x]` resolved annotation | **YES** | — | Critic verdict received 2026-05-10; OQ-13d resolution candidate stands; v0.10.1 patches OQ-13d-flagged residual issues. |
| C6 | New OQ-13f (disagreement-band asymmetry) | **YES (record)** | v0.11 (resolve) | Record the new OQ in v0.10.1 audit log; tightening bands is v0.11 hybrid scope. |
| C7 | New OQ-13g (audit-trail discipline for same-week-old ADRs) | **YES (record + codify)** | — | v0.10.1 sets the precedent execution; D22 codifies the pattern. |
| C8 | New OQ-13h (README-body-still-in-old-tense audit) | **YES (record)** | v0.11 (resolve) | Record the OQ; CI lint / pre-commit hook is v0.11 hybrid scope. |
| C9 | New OQ-13i (mypy strict project commitment) | **YES (record)** | v0.12+ (decide) | Record the OQ; project commitment decision deferred. |
| C10 | OQ-13a amendment — Vorländer 2020 §11 / Appx A as PRIMARY for MELAMINE_FOAM α₅₀₀ | **YES** | — | Matches ADR 0011 / OQ-2 / OQ-6 precedent; recording the direction now reduces v0.11 ambiguity. |
| C11 | OQ-12a status-update marker — v0.11 ships in-situ protocol DOC only (no capture commitment) | **YES** | — | Records the user-locked direction. |
| C12 | D22 (v0.10.1 audit-trail-discipline pattern) | **YES** | — | Codifies the hybrid pattern OQ-13g records as resolution candidate. |
| C13 | `RELEASE_NOTES_v0.10.1.md` NEW | **YES** | — | Required by v0.10 audit-pattern (every release ships RELEASE_NOTES). |
| C14 | Version bump `0.10.0` → `0.10.1` (`pyproject.toml` + `roomestim/__init__.py:__version__`) | **YES** | — | SemVer patch bump. |
| C15 | Disagreement-band tightening (lab `predicted_s_max` ≤ 0.265) | NO | v0.11 | Recorded as OQ-13f resolution candidate. v0.10.1 factual-integrity-only. |
| C16 | Structural-sign assertions (`assert rel_err > 0.40` lab; `< -0.10` conference) | NO | v0.11 | Recorded as OQ-13f resolution candidate; test logic change is v0.11 hybrid scope. |
| C17 | MELAMINE_FOAM + FIBERGLASS_CEILING + TILE_FLOOR enum additions | NO | v0.11 | OQ-13a (existing); library-coefficient revision; scope explosion. |
| C18 | Mypy strict cleanup of `ace_challenge.py:554-556` | NO | v0.12+ | OQ-13i (new); scope creep risk. |
| C19 | CI lint / pre-commit hook for "we ship\|ship in v0.[0-9]" tense audit | NO | v0.11 | OQ-13h (new); CI infra change. |
| C20 | Test logic changes (any test file body) | NO | (never in v0.10.1) | Hard-locked out by §0.3 invariants. |
| C21 | Test count change (≠ 116) | NO | (never in v0.10.1) | Hard-locked out by §0.3 invariants. |
| C22 | `MaterialLabel` enum changes | NO | v0.11+ | OQ-13a; scope explosion. |
| C23 | ADR supersedure (rather than in-place correction) | NO | (n/a — v0.10.1 is factual not structural) | OQ-13g resolution candidate explicitly says structural errors warrant supersedure; v0.10.1 errors are factual, so in-place hybrid pattern applies. |

---

## §2 Step-by-step linear sequence

The executor runs §2.0 → §2.7 sequentially. Each step has explicit
verification commands and acceptance criteria.

### §2.0 Pre-flight

**Goal**: Confirm working tree is clean and at v0.10.0 baseline `90aa6f1`.

```bash
cd /home/seung/mmhoa/roomestim
git status --porcelain  # MUST be empty
git log --oneline -1     # MUST start with 90aa6f1
git rev-parse HEAD       # MUST equal 90aa6f1...
/home/seung/miniforge3/bin/python -m pytest --collect-only -q tests 2>&1 | tail -3
# expect: "116 tests collected" (or equivalent)
/home/seung/miniforge3/bin/ruff check . 2>&1 | tail -3
# expect: "All checks passed!"
```

**Exit gate**: All four commands return expected. If any fails, STOP and
escalate to user (DO NOT auto-fix; v0.10.1 assumes clean v0.10.0).

### §2.1 Edit `docs/adr/0018-soundcam-substitute-disagreement-record.md` (TWO surgical edits)

**Goal**: (a) Redact fabricated `1.121 s` quote from line 46; (b) append
§Status-update-2026-05-10b block.

#### §2.1(a) Line 46 in-line redaction

Use `Edit` tool with `old_string` = §Drivers item 2 BEFORE block, `new_string` = AFTER block. Verbatim text in §8.1 below.

**Diff lines**: -6 / +1 net (6 lines old prose / 1 line new prose); the
`§Status-update-2026-05-10b below` cross-reference is part of the same
edit and is included in the new prose.

#### §2.1(b) Append §Status-update-2026-05-10b block at very bottom (after existing §References)

Use `Edit` tool with `old_string` = the last existing line of §References
(line 186, which currently ends with `cross-repo-pr-v0.10-deferred.md`),
`new_string` = same last line + horizontal rule + §Status-update block.
Verbatim text in §8.2 below.

**Diff lines**: +14 (the appended block).

**Verification**:

```bash
grep -n "1.121" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: NO matches (the fabricated quote is gone)
grep -n "Status-update-2026-05-10b" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: 2 matches (one in line 46 cross-reference; one in the appended block header)
grep -n "vaulted-ceiling open-layout" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: NO matches (extrapolation is gone)
grep -n "lab measured 0.158 s vs placeholder 0.35 s" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: 1 match on line 45 (the lab quote is preserved)
```

### §2.2 Edit `tests/fixtures/soundcam_synthesized/README.md` (3 line-group edits)

**Goal**: Tense + framing fix on body lines 27/31/41-43 beneath the v0.10
§Honesty-correction prepend. NO content removal.

Use `Edit` tool three times:

1. Line 27: `**What we ship in v0.9**:` → `**What v0.9 shipped (superseded — see §Honesty-correction-2026-05-10 above)**:`
2. Line 31: `**What we DO NOT ship in v0.9**:` → `**What v0.9 did NOT ship**:`
3. Lines 41-43 (the `flagged here as 'citation_pending: true'` paragraph): prepend `**[v0.9-historical, superseded by §Honesty-correction-2026-05-10]:**` and preserve original prose. Verbatim text in §8.3 below.

**Diff lines**: ~3 added, ~3 removed (rewording in place).

**Verification**:

```bash
grep -n "What we ship in v0.9" tests/fixtures/soundcam_synthesized/README.md
# expect: NO matches (present-tense framing is gone)
grep -n "What we DO NOT ship in v0.9" tests/fixtures/soundcam_synthesized/README.md
# expect: NO matches (present-tense framing is gone)
grep -n "v0.9-historical, superseded by" tests/fixtures/soundcam_synthesized/README.md
# expect: 1 match (the prepended marker on the citation_pending paragraph)
grep -n "What v0.9 shipped (superseded" tests/fixtures/soundcam_synthesized/README.md
# expect: 1 match
git diff --stat 90aa6f1 -- tests/fixtures/soundcam_synthesized/
# expect: only README.md listed; lab/, conference/ data files untouched
```

### §2.3 Edit `.omc/plans/v0.10-design.md` (line 702 redaction + addendum bullet)

**Goal**: Same surgical redaction as ADR 0018 line 46, plus 1-line
addendum to the existing implementation-note block at the top of the
plan.

Use `Edit` tool twice:

1. Line 702 redaction (verbatim text in §8.4 below; same shape as §8.1 ADR 0018 redaction).
2. Append addendum bullet to existing `Implementation note (added 2026-05-10 by executor)` block. The block currently ends after line 9 (`...the actual 0.254 s / +60 % figure.`). New bullet to append: verbatim in §8.5 below.

**Diff lines**: -6 / +7 net (line 702 redaction = -6/+1; addendum = +1).

**Verification**:

```bash
grep -n "1.121" .omc/plans/v0.10-design.md
# expect: NO matches
grep -n "2026-05-10b addendum (v0.10.1)" .omc/plans/v0.10-design.md
# expect: 1 match
git diff --stat 90aa6f1 -- .omc/plans/v0.10-design.md
# expect: 1 file changed; ~7 insertions, ~6 deletions (within ±2 line-count tolerance)
```

### §2.4 Edit `.omc/plans/open-questions.md` (OQ-13d resolved + OQ-13f/g/h/i appended + OQ-13a amended + OQ-12a status-update)

**Goal**: 4 surgical edits.

Use `Edit` tool four times:

1. **OQ-13d `[x]` resolved**: edit the `- [ ] **(OQ-13d)**` line to `- [x] **(OQ-13d)**`; append annotation `Verdict received 2026-05-10 (Critic Opus, 7.6/10 composite); resolution-candidate stands; v0.10.1 patches OQ-13d-flagged residual issues.` at the end of the existing OQ-13d body.
2. **OQ-13a amendment**: insert sentence `**Amendment (v0.10.1, 2026-05-10b)**: PRIMARY source = Vorländer 2020 §11 / Appx A (matches ADR 0011 / OQ-2 / OQ-6 precedent); secondary cross-check = Bies & Hansen 2018 + NRC manufacturer data sheets.` into the OQ-13a resolution-candidate block.
3. **OQ-12a status-update**: append `**Status-update-2026-05-10b (v0.10.1)**: v0.11 will ship in-situ protocol DOC only (no capture commitment); A10b actual capture remains user-volunteer-only.` to the OQ-12a body.
4. **OQ-13f/g/h/i append**: append a new `## v0.10.1-patch — 2026-05-10b` section at end of file containing the four new OQs. Verbatim text in §8.6 below.

**Diff lines**: ~25 added (mostly the new section), ~1 removed (the `[ ]` → `[x]` checkbox toggle).

**Verification**:

```bash
grep -n "OQ-13f\|OQ-13g\|OQ-13h\|OQ-13i" .omc/plans/open-questions.md
# expect: ≥ 4 matches (one per OQ headline)
grep -n "^- \[x\] \*\*(OQ-13d)\*\*" .omc/plans/open-questions.md
# expect: 1 match (resolved checkbox)
grep -n "Vorländer 2020 §11 / Appx A" .omc/plans/open-questions.md
# expect: ≥ 2 matches (OQ-6 existing + OQ-13a amendment)
grep -n "v0.10.1-patch — 2026-05-10b" .omc/plans/open-questions.md
# expect: 1 match (new section header)
```

### §2.5 Edit `.omc/plans/decisions.md` (D22 appended)

**Goal**: Append D22 entry at end of file, after D21.

Use `Edit` tool once: locate the last line of D21 (around line 1048),
append a horizontal rule + D22 block. Verbatim text in §8.7 below.

**Diff lines**: ~25 added, 0 removed.

**Verification**:

```bash
grep -n "^## D2[12]" .omc/plans/decisions.md
# expect: 2 matches (D21 existing on line 966; D22 NEW)
grep -n "audit-trail-discipline\|hybrid pattern" .omc/plans/decisions.md
# expect: ≥ 1 match (D22 body)
```

### §2.6 Edit `pyproject.toml` + `roomestim/__init__.py` (version bumps)

**Goal**: SemVer patch bump.

Use `Edit` tool twice:

1. `pyproject.toml`: `version = "0.10.0"` → `version = "0.10.1"` (line 7).
2. `roomestim/__init__.py`: `__version__ = "0.10.0"` → `__version__ = "0.10.1"` (line 3). `__schema_version__` UNCHANGED on line 4.

**Diff lines**: 2 added, 2 removed.

**Verification**:

```bash
/home/seung/miniforge3/bin/python -c "import roomestim; assert roomestim.__version__ == '0.10.1', roomestim.__version__; assert roomestim.__schema_version__ == '0.1-draft', roomestim.__schema_version__"
# expect: clean exit
grep -n "version" pyproject.toml | head -3
# expect: version = "0.10.1"
```

### §2.7 NEW `RELEASE_NOTES_v0.10.1.md`

**Goal**: Create a new file at repo root mirroring v0.10's RELEASE_NOTES
shape but for the v0.10.1 patch.

Use `Write` tool. Verbatim skeleton in §8.8 below (~50 lines).

**Verification**:

```bash
test -f RELEASE_NOTES_v0.10.1.md
wc -l RELEASE_NOTES_v0.10.1.md  # expect 45–60 lines
grep -n "What we got wrong in v0.10.0\|What v0.10.1 fixes\|What stays the same\|Default-lane test count" RELEASE_NOTES_v0.10.1.md
# expect: 4 matches (one per section header)
```

### §2.8 Verification commands (cumulative)

After §2.0 → §2.7 are all green, run the cumulative verification suite:

```bash
# 1. Lint
/home/seung/miniforge3/bin/ruff check . 2>&1 | tail -3
# expect: "All checks passed!"

# 2. Test count (MUST stay 116)
/home/seung/miniforge3/bin/python -m pytest --collect-only -q tests 2>&1 | tail -3
# expect: "116 tests collected"

# 3. Test pass (full default lane)
/home/seung/miniforge3/bin/python -m pytest tests -q 2>&1 | tail -5
# expect: "116 passed" (no failures, no warnings beyond pre-existing)

# 4. Version + schema invariants
/home/seung/miniforge3/bin/python -c "import roomestim; print(roomestim.__version__, roomestim.__schema_version__)"
# expect: "0.10.1 0.1-draft"

# 5. Byte-equality of locked-out file families
git diff --stat 90aa6f1 -- roomestim/ | grep -v "__init__.py"
# expect: empty
git diff --stat 90aa6f1 -- tests/ | grep -v "fixtures/soundcam_synthesized/README.md"
# expect: empty
git diff --stat 90aa6f1 -- docs/adr/ | grep -v "0018"
# expect: empty
git diff --stat 90aa6f1 -- proto/
# expect: empty
git diff --stat 90aa6f1 -- RELEASE_NOTES_v0.9.0.md RELEASE_NOTES_v0.10.0.md
# expect: empty
git diff --stat 90aa6f1 -- docs/perf_verification_a10_soundcam_2026-05-10.md docs/perf_verification_a10a_soundcam_2026-05-09.md
# expect: empty

# 6. Diff envelope check
git diff --stat 90aa6f1 | tail -1
# expect: 8 files changed, ~110 insertions, ~18 deletions (within tolerance ±20%)

# 7. ADR 0018 surgical-edit verification
grep -c "1.121" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: 0
grep -c "Status-update-2026-05-10b" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: 2
```

If any verification fails, STOP and re-plan. Do NOT auto-fix without
re-reading §0/§5.

---

## §3 Open Questions amendment

This section is the canonical record of v0.10.1's audit additions to
`.omc/plans/open-questions.md`. The executor copies §3.2 verbatim into
the open-questions file (see §2.4 step 4 + §8.6).

### §3.1 OQ-13d resolution annotation

OQ-13d (existing in `open-questions.md`):

> **(OQ-13d)** v0.10 critic verdict — what verdict does Critic give v0.10's disagreement-record framing + reverse-criterion firing?

**v0.10.1 annotation** (executor appends to OQ-13d body, not as new bullet):

> Verdict received 2026-05-10 (Critic Opus, 7.6/10 composite); resolution-candidate stands; v0.10.1 patches OQ-13d-flagged residual issues. Checkbox state changes `[ ]` → `[x]`.

### §3.2 OQ-13f/g/h/i — new open questions surfaced by v0.10 critic + architect

(Copy verbatim from §8.6 below.)

### §3.3 OQ-13a amendment

OQ-13a (existing) resolution candidate is amended to specify Vorländer
2020 §11 / Appx A as the PRIMARY coefficient source for MELAMINE_FOAM
α₅₀₀ (matching ADR 0011 / OQ-2 / OQ-6 precedent).

### §3.4 OQ-12a status-update marker

OQ-12a (existing) gains a status-update marker recording that v0.11 will
ship in-situ protocol DOC only (no capture commitment); A10b actual
capture remains user-volunteer-only as before.

### §3.5 OQ-11 / OQ-12b / OQ-12c / OQ-13b / OQ-13c / OQ-13e — UNCHANGED

All other open questions are explicitly REAFFIRMED unchanged. v0.10.1
does NOT touch them.

---

## §4 Reverse criteria (what would walk back v0.10.1?)

v0.10.1 is a small factual-integrity patch, but it still has reverse
criteria that would invalidate the patch and require a v0.10.2 (or
escalation to v0.11 hybrid scope).

1. **If v0.11 critic argues §Status-update-2026-05-10b appended block is itself a soft FAIL gate** (e.g., "the audit-trail discipline pattern obscures rather than reveals; supersedure would have been clearer"), v0.10.1 may be partially walked back: D22 codification reverts; ADR 0018 §Status-update block becomes the only artifact. Reverse-criterion text is recorded in OQ-13g resolution-candidate.
2. **If a future paper retrieval surfaces authoritative living_room dims** (which v0.10 §A.2 currently believes do not exist), the redaction at line 46 / line 702 may be partially restored under a successor ADR; the `1.121 s` quote would be re-attached with proper Table 7 / §A.2 citation. Reverse-criterion text is recorded in ADR 0018 §Reverse-criterion (already present from v0.10) AND repeated in §Status-update-2026-05-10b for cross-reference clarity.
3. **If v0.11 hybrid scope adds MELAMINE_FOAM enum AND lab returns to A11 PASS-gate**, the ADR 0018 §Status-update §Drivers item 2 framing ("the paper-retrieved value reveals the default-enum cannot reach treated-room (lab) absorption levels") would need to be re-amended to add a "v0.11 follow-up" cross-reference. v0.10.1 records this potential re-amendment as a v0.11+ work item via OQ-13a amendment.
4. **If diff envelope exceeds 80 added-meaningful-content lines or 30 removed lines**, STOP — the patch has bloated. Common bloat sources: gratuitous prose expansion in §Status-update block, RELEASE_NOTES_v0.10.1 ballooning past 60 lines, OQ-13f/g/h/i bodies expanding past resolution-candidate-only into full position-papers. Trim before commit.
5. **If `pytest --collect-only` returns ≠ 116 tests after edits**, STOP — test logic was changed. v0.10.1 forbids any test count change.
6. **If `git diff --stat 90aa6f1` shows any file outside §0.2 list of 8 files**, STOP — scope creep. Revert non-listed files before commit.

---

## §5 Hard-exit acceptance criteria (30 numbered items)

The release is complete iff **every** numbered criterion below holds.

1. Default-lane test count: **116 (UNCHANGED from v0.10.0)**. `pytest --collect-only -q tests | tail -1` reads `116 tests collected`.
2. Default-lane test pass: `pytest tests -q | tail -5` reads `116 passed` (no failures).
3. Lint clean: `/home/seung/miniforge3/bin/ruff check .` returns `All checks passed!`.
4. `__version__` bumps `"0.10.0"` → `"0.10.1"` in BOTH `pyproject.toml` AND `roomestim/__init__.py`.
5. `__schema_version__` UNCHANGED at `"0.1-draft"` (Stage-2 schema state preserved from v0.10).
6. `proto/room_schema.json` byte-equal to v0.10 baseline `90aa6f1`.
7. `proto/room_schema.draft.json` byte-equal to v0.10.
8. MaterialLabel enum unchanged (9 entries) — implied by `roomestim/model.py` byte-equal.
9. `MaterialAbsorption{,Bands}` byte-equal to v0.10 — implied by `roomestim/model.py` byte-equal.
10. `_FURNITURE_BY_ROOM` (sum=276) and `_PIECE_EQUIVALENT_ABSORPTION_500HZ_M2['lecture_seat']`=0.45 byte-equal — implied by `roomestim/adapters/ace_challenge.py` byte-equal.
11. `roomestim/place/wfs.py`, `roomestim/cli.py`, `roomestim/adapters/ace_challenge.py`, `roomestim/adapters/polycam.py`, `roomestim/reconstruct/floor_polygon.py`, `roomestim/model.py` ALL byte-equal to v0.10. Verification: `git diff --stat 90aa6f1 -- roomestim/ | grep -v "__init__.py"` empty.
12. ALL test files (`tests/**/*.py`) byte-equal to v0.10. Verification: `git diff --stat 90aa6f1 -- tests/ | grep -v "fixtures/soundcam_synthesized/README.md"` empty.
13. ALL fixture data files (`tests/fixtures/soundcam_synthesized/{lab,conference}/{dims.yaml,rt60.csv,GT_corners.json}`, `LICENSE_MIT.txt`) byte-equal to v0.10.
14. ADR 0018 has surgical edit at line 46 (fabricated `1.121 s` quote + `vaulted-ceiling open-layout` extrapolation REMOVED). Verification: `grep -c "1.121" docs/adr/0018-soundcam-substitute-disagreement-record.md` returns 0; `grep -c "vaulted-ceiling open-layout" docs/adr/0018-soundcam-substitute-disagreement-record.md` returns 0.
15. ADR 0018 has appended §Status-update-2026-05-10b block at end (after §References). Verification: `grep -c "Status-update-2026-05-10b" docs/adr/0018-soundcam-substitute-disagreement-record.md` returns ≥ 2 (one in line 46 cross-reference; one in appended block header).
16. ADR 0018 §Decision + §Alternatives-considered + §Why-chosen + §Consequences + §Reverse-criterion + §References sections byte-equal to v0.10 (only §Drivers item 2 line 46 + new appended §Status-update changed).
17. ADRs 0001..0017 byte-equal to v0.10 (`git diff --stat 90aa6f1 -- docs/adr/ | grep -v "0018"` empty).
18. `tests/fixtures/soundcam_synthesized/README.md` body lines 27 + 31 + 41-43 fixed for tense/framing; NO content removal. Verification: `grep -c "What we ship in v0.9" tests/fixtures/soundcam_synthesized/README.md` returns 0; `grep -c "v0.9-historical, superseded by" tests/fixtures/soundcam_synthesized/README.md` returns 1.
19. `tests/fixtures/soundcam_synthesized/README.md` §Honesty-correction-2026-05-10 prepend block byte-equal to v0.10 (no edit to the prepend itself).
20. `.omc/plans/v0.10-design.md` line 702 same surgical redaction as ADR 0018 line 46. Verification: `grep -c "1.121" .omc/plans/v0.10-design.md` returns 0.
21. `.omc/plans/v0.10-design.md` implementation-note block at top has appended `2026-05-10b addendum (v0.10.1)` bullet. Verification: `grep -c "2026-05-10b addendum (v0.10.1)" .omc/plans/v0.10-design.md` returns 1.
22. `.omc/plans/v0.10-design.md` body otherwise byte-equal to v0.10 (no edits beyond the line-702 redaction + addendum bullet).
23. `.omc/plans/open-questions.md` has OQ-13d marked `[x]` resolved with annotation; OQ-13f/g/h/i appended in NEW `## v0.10.1-patch — 2026-05-10b` section; OQ-13a amended (Vorländer primary); OQ-12a status-update marker added. Verification: `grep -c "v0.10.1-patch — 2026-05-10b" .omc/plans/open-questions.md` returns 1; `grep -c "OQ-13f\|OQ-13g\|OQ-13h\|OQ-13i" .omc/plans/open-questions.md` returns ≥ 4.
24. `.omc/plans/decisions.md` has D22 appended (audit-trail-discipline pattern). D14..D21 byte-equal to v0.10. Verification: `grep -c "^## D22" .omc/plans/decisions.md` returns 1; D21 line position unchanged at line 966.
25. `RELEASE_NOTES_v0.10.1.md` exists at repo root; ~50 lines; carries §What-we-got-wrong-in-v0.10.0 + §What-v0.10.1-fixes + §What-stays-the-same + §Default-lane-test-count (stays 116) + §Tag-local-only sections.
26. `RELEASE_NOTES_v0.10.0.md` byte-equal to v0.10 (no amendment). v0.10.1 ships its OWN release notes; v0.10.0 release notes are NOT prepended.
27. Diff envelope: `git diff --stat 90aa6f1 | tail -1` shows 8 files changed; insertions ~95–125 lines; deletions ~12–25 lines. Outside this envelope = scope drift.
28. Single commit on top of `90aa6f1`: `git log --oneline 90aa6f1..HEAD` is exactly one line, subject `roomestim v0.10.1 — fabricated-quote redaction; README tense fix; D22 audit-trail-discipline; OQ-13f/g/h/i`. (Or close paraphrase.)
29. Local tag `v0.10.1` exists; not pushed to remote (`git ls-remote --tags origin | grep v0.10.1` empty per D11 tag-local-only policy).
30. `git status --porcelain` clean after commit.

---

## §6 References

- v0.10.0 commit: `90aa6f1` — honesty correction; ADR 0016 reverse-criterion fired; ADR 0018; living_room removed; D21.
- v0.10 design plan: `.omc/plans/v0.10-design.md` (1357 lines; planning-style precedent).
- v0.10 critic verdict (2026-05-10; 7.6/10 composite) — flagged 3 residual concerns that v0.10.1 patches.
- v0.10 architect verdict (2026-05-10; §Categorisation table buckets v0.10.1 scope; §v0.10.1 patch scope sets the size envelope).
- ADR 0016 — Stage-2 schema flip via SoundCam substitute (v0.9 predecessor; FIRED at v0.10).
- ADR 0017 — A10-layout deferred-with-classification (unchanged).
- ADR 0018 — Substitute-disagreement record (v0.10; v0.10.1 surgical-edits §Drivers item 2 + appends §Status-update-2026-05-10b).
- D2 (≥3 captures requirement for Stage-2 lock) — v0.10 substitute coverage drops to 2 rooms; tightening D2 reverse condition for Stage-2 re-flip.
- D8 (original A10 lab-capture predicate) — unchanged.
- D11 (cross-repo PR remains proposal-stage; tag-local-only policy) — applies to v0.10.1.
- D14..D21 — byte-equal under v0.10.1; D22 NEW.
- OQ-11 — reaffirmed unchanged.
- OQ-12a — status-update marker added (v0.11 protocol DOC direction).
- OQ-12b/c — reaffirmed unchanged.
- OQ-13a — amended (Vorländer 2020 §11 / Appx A as PRIMARY).
- OQ-13b/c/e — reaffirmed unchanged.
- OQ-13d — RESOLVED `[x]` (verdict received 2026-05-10).
- OQ-13f/g/h/i — NEW (v0.10.1).
- arXiv:2311.03517v2 (NeurIPS 2024 D&B, SoundCam) — Appendix A.1, A.2, A.3 + Table I + Table 7 + Figure 10. Cross-referenced for redaction rationale.
- v0.10 RELEASE_NOTES — `RELEASE_NOTES_v0.10.0.md` (byte-equal under v0.10.1).
- v0.10.1 RELEASE_NOTES — `RELEASE_NOTES_v0.10.1.md` (NEW).

---

## §7 Hand-off

### §7.1 Pipeline

`planner` (this plan) → `executor` (mechanical translation of §8 drafts) → `code-reviewer` (lint + byte-equality of locked-out files) → `verifier` (full pytest + diff-envelope check + tag-local-only) → user (final review + commit).

Per project memory: non-trivial work goes through OMC pipeline; authoring
and review separated; planner does NOT self-approve; executor MUST hand
off to code-reviewer before commit.

### §7.2 Expected diff lines per file

| Path | Insertions | Deletions | Net |
| --- | --- | --- | --- |
| `docs/adr/0018-soundcam-substitute-disagreement-record.md` | ~16 | ~6 | +10 |
| `tests/fixtures/soundcam_synthesized/README.md` | ~3 | ~3 | 0 |
| `.omc/plans/v0.10-design.md` | ~7 | ~6 | +1 |
| `.omc/plans/open-questions.md` | ~25 | ~1 | +24 |
| `.omc/plans/decisions.md` | ~25 | 0 | +25 |
| `pyproject.toml` | 1 | 1 | 0 |
| `roomestim/__init__.py` | 1 | 1 | 0 |
| `RELEASE_NOTES_v0.10.1.md` | ~50 | 0 | +50 |
| **Total** | **~128** | **~18** | **~+110** |

### §7.3 Commit subject (exact text for executor)

```
roomestim v0.10.1 — fabricated-quote redaction; README tense fix; D22 audit-trail-discipline; OQ-13f/g/h/i
```

(Body: standard release-notes summary; reference `RELEASE_NOTES_v0.10.1.md`.)

### §7.4 Tag policy

Local tag `v0.10.1` per D11 (tag-local-only policy unchanged). NOT pushed
to remote.

### §7.5 What v0.10.1 does NOT do

The plan §5 hard-exit explicitly forbids:

- Disagreement-band tightening (lab `predicted_s_max` ≤ 0.265): DEFERRED to v0.11 (OQ-13f).
- Mypy strict cleanup of `ace_challenge.py:554-556`: DEFERRED to v0.12+ (OQ-13i).
- MELAMINE_FOAM enum addition: DEFERRED to v0.11 (OQ-13a).
- Test logic changes (any test file body): FORBIDDEN in v0.10.1 (test count must stay 116).
- Test count change (≠ 116): FORBIDDEN.
- ADR supersedure (rather than in-place hybrid correction): FORBIDDEN (v0.10.1 errors are factual not structural; OQ-13g resolution-candidate codifies the distinction).
- Library-coefficient revision: FORBIDDEN (no `MaterialAbsorption{,Bands}` edits).
- Library-default-state mutation: FORBIDDEN (`__schema_version__` stays `"0.1-draft"`).
- v0.10 RELEASE_NOTES amendment: FORBIDDEN (v0.10.1 ships its OWN release notes).
- Cross-repo PR re-submission or amendment: FORBIDDEN (PR proposal byte-equal under v0.10.1).
- A10b in-situ capture: NOT scheduled by v0.10.1 (user-volunteer-only per OQ-12a).
- Live-mesh extraction: NOT in v0.10.1 (OQ-13e).

---

## §8 Drafts for executor

This section provides verbatim before/after text for every inline edit in
v0.10.1, plus full skeleton for the new RELEASE_NOTES file. The
executor's job is mechanical translation: copy-paste the AFTER blocks
into the source files at the indicated line positions, and verify with
the §2 grep checks.

### §8.1 ADR 0018 line 46 redaction (BEFORE / AFTER)

**BEFORE** (current text at lines 44-50, byte-exact from `90aa6f1`):

```
2. **Paper-retrieved values disagree with placeholders by structural
   factors** (lab measured 0.158 s vs placeholder 0.35 s = 2.2× error;
   living_room measured 1.121 s vs placeholder 0.45 s = 2.5× error).
   The placeholders had been chosen so the default-enum Sabine
   prediction silently passed ±20 %; paper values reveal the
   default-enum cannot reach treated-room (lab) or vaulted-ceiling
   open-layout (living_room) targets.
```

**AFTER** (replacement text):

```
2. **Paper-retrieved values disagree with placeholders by structural
   factors** (lab measured 0.158 s vs placeholder 0.35 s = 2.2× error).
   Living_room placeholder (0.45 s) is REMOVED at v0.10 with no
   paper-retrieved counterpart: paper §A.2 publishes no authoritative
   dims, and Table 7 row coverage was not independently verified for
   living_room during paper-retrieval (see §Status-update-2026-05-10b
   below). The placeholder had been chosen so the default-enum
   Sabine prediction silently passed ±20 %; the paper-retrieved value
   reveals the default-enum cannot reach treated-room (lab) absorption
   levels.
```

**Edit-tool form**: pass the BEFORE block as `old_string` and the AFTER
block as `new_string`. `replace_all=false` (default; the BEFORE text is
unique in the file).

### §8.2 ADR 0018 §Status-update-2026-05-10b appended block

**Where to append**: after the existing last line of the file (line 186,
which currently ends with `cross-repo-pr-v0.10-deferred.md\``).

**Verbatim block to append** (executor: copy-paste this entire block,
including the leading horizontal rule):

```markdown

---

## §Status-update-2026-05-10b (added at v0.10.1 — fabricated-quote redaction)

**Issue**: v0.10 critic verdict (7.6/10, 2026-05-10) flagged §Drivers
item 2 line 46 quoting `living_room measured 1.121 s` without
citation. v0.10 elsewhere asserts paper §A.2 publishes no authoritative
living_room data, so the quoted measurement is either uncited Table 7
row content (in which case ADR should cite Table 7 position) or
drafting residue (in which case it should be redacted).

**Action**: v0.10.1 redacts the `1.121 s` quote and the
`vaulted-ceiling open-layout (living_room) targets` extrapolation
from §Drivers item 2 (line 46). The line preserves the lab quote
(`0.158 s vs placeholder 0.35 s`) which has independent Table 7
citation in §Decision item 1 + §References. The redaction is in-line
to keep the ADR readable; this §Status-update records the WHY for
audit-trail discipline.

**Cross-references**: v0.10 critic verdict (§Honesty-leak audit MED);
v0.11 architect verdict §Categorisation table (categorised as
v0.10.1 patch); .omc/plans/v0.10.1-patch.md (this plan).
```

**Edit-tool form**: `old_string` = the literal last line of the v0.10
file (e.g., `- Cross-repo PR withdrawal — \`.omc/research/cross-repo-pr-v0.10-deferred.md\`.`); `new_string` = same last line + appended block above.

### §8.3 README body line-group edits (BEFORE / AFTER)

**Edit 1 — line 27**:

- BEFORE: `- **What we ship in v0.9**: rectangular shoebox **synthesised** ground-truth`
- AFTER: `- **What v0.9 shipped (superseded — see §Honesty-correction-2026-05-10 above)**: rectangular shoebox **synthesised** ground-truth`

**Edit 2 — line 31**:

- BEFORE: `- **What we DO NOT ship in v0.9**: live-mesh extraction from upstream PLY/CSV`
- AFTER: `- **What v0.9 did NOT ship**: live-mesh extraction from upstream PLY/CSV`

**Edit 3 — lines 41-43** (the `flagged here as 'citation_pending: true'` paragraph; current byte-exact text):

- BEFORE:
  ```
  1. Take published room dimensions (L × W × H, metres) from the SoundCam
     paper / repo metadata. If the executor was unable to access the
     canonical published values (paywall / repo offline), placeholder
     estimates from the v0.9 implementation prompt were used and are
     flagged here as `citation_pending: true` in the per-room `dims.yaml`.
  ```
- AFTER:
  ```
  1. **[v0.9-historical, superseded by §Honesty-correction-2026-05-10]:** Take published room dimensions (L × W × H, metres) from the SoundCam
     paper / repo metadata. If the executor was unable to access the
     canonical published values (paywall / repo offline), placeholder
     estimates from the v0.9 implementation prompt were used and are
     flagged here as `citation_pending: true` in the per-room `dims.yaml`.
  ```

**Edit-tool form**: three independent `Edit` calls. Each `old_string` is
unique by virtue of the surrounding line context. NO content is removed
in any edit; only marker prepends + tense rewrites in place.

### §8.4 v0.10-design.md line 702 redaction (BEFORE / AFTER)

**Where**: `.omc/plans/v0.10-design.md` lines 700-706 (byte-exact from
`90aa6f1`):

**BEFORE**:

```
2. **Paper-retrieved values disagree with placeholders by structural
   factors** (lab measured 0.158 s vs placeholder 0.35 s = 2.2× error;
   living_room measured 1.121 s vs placeholder 0.45 s = 2.5× error).
   The placeholders had been chosen so the default-enum Sabine
   prediction silently passed ±20 %; paper values reveal the
   default-enum cannot reach treated-room (lab) or vaulted-ceiling
   open-layout (living_room) targets.
```

**AFTER** (identical to ADR 0018 AFTER block in §8.1, except no
`§Status-update-2026-05-10b below` cross-reference; the v0.10-design.md
body refers to the addendum at the top of the file instead):

```
2. **Paper-retrieved values disagree with placeholders by structural
   factors** (lab measured 0.158 s vs placeholder 0.35 s = 2.2× error).
   Living_room placeholder (0.45 s) is REMOVED at v0.10 with no
   paper-retrieved counterpart: paper §A.2 publishes no authoritative
   dims, and Table 7 row coverage was not independently verified for
   living_room during paper-retrieval (see implementation-note addendum
   at top of file + ADR 0018 §Status-update-2026-05-10b). The
   placeholder had been chosen so the default-enum Sabine prediction
   silently passed ±20 %; the paper-retrieved value reveals the
   default-enum cannot reach treated-room (lab) absorption levels.
```

### §8.5 v0.10-design.md addendum bullet (append to existing implementation-note block)

**Where**: after line 9 of `.omc/plans/v0.10-design.md`, which currently
reads (byte-exact):

```
> all downstream artefacts (ADR 0018, RELEASE_NOTES_v0.10.0.md, perf doc,
> D21, lab/dims.yaml) reflect the actual 0.254 s / +60 % figure.
```

**Append after that line** (NEW bullet, executor: this is one
single-line addendum within the existing blockquote):

```
>
> **2026-05-10b addendum (v0.10.1)**: line 702 §Drivers item 2 redaction matches ADR 0018 §Status-update-2026-05-10b (fabricated-quote redaction; uncited `1.121 s` removed; uncited vaulted-ceiling extrapolation removed). v0.10.1 patch plan: `.omc/plans/v0.10.1-patch.md`.
```

**Edit-tool form**: `old_string` = literal line 9 (`> D21, lab/dims.yaml) reflect the actual 0.254 s / +60 % figure.`); `new_string` = same line + the addendum bullet above. The blockquote markers (`>`) are preserved.

### §8.6 OQ-13f/g/h/i — full verbatim text for `open-questions.md`

**Where**: append a new section at the END of `.omc/plans/open-questions.md` (after the last existing line, which is the `OQ-11 reaffirmation (v0.10)` bullet).

**Verbatim block to append**:

```markdown

---

## v0.10.1-patch — 2026-05-10b

> **Status update 2026-05-10b**: 4 NEW OQs (OQ-13f, g, h, i) raised at v0.10.1 ship time, derived from v0.10 critic verdict (7.6/10 composite) + v0.10 architect verdict (§Categorisation table). OQ-13d marked `[x]` resolved (verdict received). OQ-13a resolution candidate amended to specify Vorländer 2020 §11 / Appx A as PRIMARY for MELAMINE_FOAM α₅₀₀. OQ-12a status-update marker added (v0.11 will ship in-situ protocol DOC only; no capture commitment). OQ-11 / OQ-12b / OQ-12c / OQ-13b / OQ-13c / OQ-13e reaffirmed unchanged. New decision (D22) appended to `decisions.md` at v0.10.1 commit.

- [ ] **(OQ-13f)** Disagreement-band asymmetry — lab `predicted_s_min/max=[0.235, 0.270]` window permits silent-pass on a hypothetical CARPET 0.30→0.20 perturbation (predicted=0.2693 ∈ band); the rel_err band [+45 %, +70 %] catches it (rel_err=+70.4 % barely fails) but with razor-thin margin. Should bands be tightened to ≤ ±5 % around the recorded signature, AND should structural-sign assertions (`assert rel_err > 0.40` lab; `< -0.10` conference) be added as a separate, redundant guard? — **Resolution candidate**: yes; v0.11 hybrid scope will tighten lab `predicted_s_max` to ≤ 0.265 (or remove entirely if lab returns to PASS-gate under MELAMINE_FOAM enum) AND add structural-sign assertion to conference test. Reverse if v0.11 critic argues the structural-sign assertion is itself a soft FAIL gate.

- [ ] **(OQ-13g)** Audit-trail discipline for same-week-old ADRs — when an ADR (e.g., 0018) created at v0.X needs a same-week correction at v0.X.1, what is the canonical pattern? v0.10.1 sets precedent: hybrid (inline-edit + appended §Status-update-2026-MM-DDb block recording WHY). — **Resolution candidate**: codify in `.omc/plans/decisions.md` D22 (v0.10.1) that future same-week ADR corrections follow the hybrid pattern; pure-append-only with `<del>`/`<ins>` HTML markup is rejected as unreadable. Reverse if a future ADR has a structural error (not factual) — structural errors require ADR supersedure (new ADR + §Status: superseded), not in-place correction.

- [ ] **(OQ-13h)** README-body-still-in-old-tense audit — v0.10's §Honesty-correction prepend pattern was correct, but `tests/fixtures/soundcam_synthesized/README.md` body lines 26-44 still described v0.9 placeholder regime in present tense. Should v0.11 codify a `git grep "we ship\|ship in v0.[0-9]"` audit pass as a CI lint or pre-commit hook? — **Resolution candidate**: yes; v0.11 hybrid scope adds a lightweight CI check that flags present-tense version-specific framing in fixture README + ADR + RELEASE_NOTES files. Reverse if the false-positive rate proves intolerable.

- [ ] **(OQ-13i)** Mypy strict project commitment — `roomestim/adapters/ace_challenge.py:554-556` has 3 pre-existing strict mypy errors (`float(object)` casts); the project does not currently advertise mypy-strict cleanliness. Should it commit to strict cleanliness, or explicitly disclaim it? — **Resolution candidate**: deferred to v0.12+; v0.11 hybrid scope explicitly does NOT include mypy fixes (silent fix risks scope creep). Reverse if the project gains a downstream consumer (spatial_engine?) that depends on type-strict roomestim.

- **OQ-13d resolution annotation**: Verdict received 2026-05-10 (Critic Opus, 7.6/10 composite); resolution-candidate stands; v0.10.1 patches OQ-13d-flagged residual issues.

- **OQ-13a amendment (v0.10.1, 2026-05-10b)**: PRIMARY source = Vorländer 2020 §11 / Appx A (matches ADR 0011 / OQ-2 / OQ-6 precedent); secondary cross-check = Bies & Hansen 2018 + NRC manufacturer data sheets. Original OQ-13a body unchanged; this is an amendment to the resolution candidate only.

- **OQ-12a status-update-2026-05-10b (v0.10.1)**: v0.11 will ship in-situ protocol DOC only (no capture commitment); A10b actual capture remains user-volunteer-only.

- **OQ-11 / OQ-12b / OQ-12c / OQ-13b / OQ-13c / OQ-13e reaffirmation (v0.10.1)**: All other open questions UNCHANGED. v0.10.1 is factual-integrity-only and does not relitigate prior resolutions.
```

**Note on OQ-13d checkbox toggle and OQ-13a / OQ-12a in-place amendments**: §2.4 step 1 (OQ-13d), step 2 (OQ-13a), step 3 (OQ-12a) modify the EXISTING OQ blocks in their original section positions. The new `## v0.10.1-patch — 2026-05-10b` section above is the LANDING PAGE for OQ-13f/g/h/i and the cross-cutting status-update; the in-place amendments to OQ-13a / OQ-12a / OQ-13d in their original sections are summarised here in the new section's bullets, not duplicated. (The `OQ-13d resolution annotation`, `OQ-13a amendment`, and `OQ-12a status-update-2026-05-10b` bullets above are the SUMMARY pointers; the actual annotation lives in the OQ's original location.)

### §8.7 D22 entry for `decisions.md`

**Where**: append at end of `.omc/plans/decisions.md` (after the last
line of D21, around line 1048).

**Verbatim block to append**:

```markdown

---

## D22 — v0.10.1 patch ships fabricated-quote redaction; codifies hybrid audit-trail-discipline pattern for same-week-old ADR corrections; new OQ-13f/g/h/i recorded

- **Date**: 2026-05-10b
- **Author**: oh-my-claudecode:planner (consulted by user after v0.10.0 critic verdict 7.6/10 composite + architect §Categorisation table flagged 3 v0.10.1 patch-scope items)
- **Predecessor**: D21 (v0.10.0 honesty correction; ADR 0018; living_room removed)
- **Decision**: v0.10.1 ships the following audit-trail-discipline pattern as the canonical pattern for same-week-old ADR corrections:
  - **For factual errors** (uncited quotes, drafting residue, fabricated extrapolations): HYBRID PATTERN — in-line redaction at the offending line + appended `§Status-update-YYYY-MM-DDb` block at the bottom of the ADR (after §References) recording the WHY (issue surfaced, action taken, cross-references).
  - **For structural errors** (wrong decision-rationale, contradicted alternatives, broken reverse-criterion): ADR SUPERSEDURE — new ADR with §Status: superseded marker on the original.
- **Drivers**: v0.10's ADR 0018 §Drivers item 2 line 46 quoted `living_room measured 1.121 s` without citation, contradicting v0.10's own §A.2 "no authoritative dims" claim. v0.10 critic flagged as MED honesty-leak. The error is FACTUAL not structural — the ADR's decision (fire reverse-criterion + remove living_room + revert schema) remains correct; only the §Drivers prose contained a fabricated quote.
- **Why this pattern, not the alternatives**:
  - "Pure append-only with `<del>`/`<ins>` HTML markup" rejected — unreadable in markdown ADR; markdown ADRs are read by humans, not version-control diff tools.
  - "ADR supersedure (new ADR 0019 + ADR 0018 marked superseded)" rejected — overkill for a single-line factual correction; supersedure is reserved for structural errors per OQ-13g resolution-candidate.
  - "Silent in-place correction (no §Status-update record)" rejected — violates audit-trail discipline; the `decisions.md` D22 entry would have no anchor in the ADR itself.
- **Reverse-trigger / ratchet-safe behaviour**:
  - If a future ADR has a STRUCTURAL error (decision-rationale wrong; alternatives contradicted), D22 does NOT apply — supersedure is required. (Recorded in OQ-13g resolution-candidate.)
  - If the §Status-update block grows beyond ~20 lines, escalate to ADR supersedure (the issue is no longer a single-line correction).
  - If the same ADR requires ≥ 2 §Status-updates within the same week, escalate to ADR supersedure (audit trail is becoming unreadable).
- **Cross-references**: D2, D8, D11, D14..D20 (byte-equal under v0.10.1), D21 (byte-equal under v0.10.1), ADR 0001..0017 (byte-equal under v0.10.1), ADR 0018 (in-line redaction at line 46 + appended §Status-update-2026-05-10b block), `.omc/plans/v0.10.1-patch.md` (this v0.10.1 plan), `.omc/plans/open-questions.md` (OQ-13f/g/h/i NEW; OQ-13a amended; OQ-12a status-update; OQ-13d resolved).
```

### §8.8 RELEASE_NOTES_v0.10.1.md (NEW file skeleton)

**Verbatim full file content** (executor: write this file at repo root via `Write` tool):

```markdown
# roomestim v0.10.1 — Release Notes

**Date**: 2026-05-10b (drafted) / 2026-05-10b (shipped)
**Predecessor**: v0.10.0 (`90aa6f1`) — honesty correction; ADR 0016 reverse-criterion fired; ADR 0018; living_room removed; D21.
**Patch nature**: factual-integrity-only. NO library code changes; NO test logic changes; NO test count change (stays 116); NO library-default-state mutation (`__schema_version__` stays `"0.1-draft"`).

## What we got wrong in v0.10.0

1. **ADR 0018 §Drivers item 2 line 46 contained a fabricated quote**: `living_room measured 1.121 s vs placeholder 0.45 s = 2.5× error`. The `1.121 s` measurement is uncited and contradicts v0.10's own §A.2 claim that paper publishes no authoritative living_room data. v0.10 critic flagged this as a MED honesty-leak (composite verdict 7.6/10).
2. **`tests/fixtures/soundcam_synthesized/README.md` body lines 27/31/41-43 still described v0.9 placeholder regime in present tense** beneath the v0.10 §Honesty-correction prepend block. Tense mismatch reads as if v0.9 placeholder shipping is current behaviour.
3. **`.omc/plans/v0.10-design.md` line 702 contained the same fabricated `1.121 s` quote** as ADR 0018 line 46 (drafting residue from the same source).

## What v0.10.1 fixes

1. **ADR 0018 line 46 in-line redaction**: `1.121 s` quote and `vaulted-ceiling open-layout (living_room) targets` extrapolation REMOVED. Lab quote (`0.158 s vs placeholder 0.35 s`) preserved (independent Table 7 citation in §Decision item 1 + §References).
2. **ADR 0018 §Status-update-2026-05-10b appended**: records the WHY of the redaction for audit-trail discipline (mirrors v0.10's RELEASE_NOTES_v0.9.0 prepend pattern).
3. **`tests/fixtures/soundcam_synthesized/README.md` tense + framing fix** on body lines 27/31/41-43. NO content removal; only past-tense rewrites + `[v0.9-historical, superseded by §Honesty-correction-2026-05-10]` marker prepend.
4. **`.omc/plans/v0.10-design.md` line 702 same redaction** as ADR 0018 line 46. Implementation-note block at top of plan gains 1-line addendum bullet referencing v0.10.1.
5. **D22 codifies the hybrid audit-trail-discipline pattern** for same-week-old ADR corrections (factual errors → in-line redaction + §Status-update; structural errors → ADR supersedure). Mirrors OQ-13g resolution candidate.
6. **OQ-13f/g/h/i recorded as NEW open questions** in `.omc/plans/open-questions.md`:
   - OQ-13f: disagreement-band asymmetry (DEFERRED to v0.11).
   - OQ-13g: audit-trail discipline for same-week-old ADRs (codified in D22).
   - OQ-13h: README-body-still-in-old-tense audit (DEFERRED to v0.11 CI lint).
   - OQ-13i: mypy strict project commitment (DEFERRED to v0.12+).
7. **OQ-13a amended** to specify Vorländer 2020 §11 / Appx A as PRIMARY for MELAMINE_FOAM α₅₀₀ (matching ADR 0011 / OQ-2 / OQ-6 precedent).
8. **OQ-12a status-update marker** records v0.11 will ship in-situ protocol DOC only (no capture commitment).
9. **OQ-13d marked `[x]` resolved** (Critic verdict received 2026-05-10).
10. **Version bump** `0.10.0` → `0.10.1` in `pyproject.toml` + `roomestim/__init__.py` (`__version__` only; `__schema_version__` stays `"0.1-draft"`).

## What stays the same

- Library code (`roomestim/**/*.py` except `__init__.py:__version__`) byte-equal to v0.10.0.
- All test files (`tests/**/*.py`) byte-equal to v0.10.0.
- All fixture data files (`tests/fixtures/soundcam_synthesized/{lab,conference}/{dims.yaml,rt60.csv,GT_corners.json}` + `LICENSE_MIT.txt`) byte-equal.
- ADRs 0001..0017 byte-equal.
- ADR 0018 §Decision + §Alternatives + §Why-chosen + §Consequences + §Reverse-criterion + §References byte-equal (only §Drivers item 2 line 46 + new appended §Status-update changed).
- `RELEASE_NOTES_v0.9.0.md`, `RELEASE_NOTES_v0.10.0.md`, all perf docs, all cross-repo PR research notes byte-equal.
- `__schema_version__` stays `"0.1-draft"` (Stage-2 schema state preserved from v0.10).
- Cross-repo PR proposal at `.omc/research/cross-repo-pr-v0.10-deferred.md` byte-equal (status remains WITHDRAWN per v0.10 ADR 0016 §Reverse-criterion).
- `MaterialLabel` 9 entries; `MaterialAbsorption{,Bands}` rows; `_FURNITURE_BY_ROOM` sum=276; `_PIECE_EQUIVALENT_ABSORPTION_500HZ_M2['lecture_seat']`=0.45.
- A10a corner test count: 2 (lab + conference smoke); A11 substitute test count: 2 (lab + conference disagreement-record); schema test invariant: 1.

## Default-lane test count

**116 tests collected; 116 passed.** UNCHANGED from v0.10.0.

## What stays deferred

- v0.11 hybrid scope: MELAMINE_FOAM enum (OQ-13a); disagreement-band tightening (OQ-13f); README-tense CI lint (OQ-13h); in-situ protocol DOC (OQ-12a).
- v0.12+ scope: mypy strict project commitment (OQ-13i).
- A10b in-situ user-lab capture: user-volunteer-only (OQ-12a unchanged on capture commitment).
- Live-mesh extraction (OQ-13e): unchanged.
- Cross-repo PR re-submission (OQ-13c + `.omc/research/cross-repo-pr-v0.10-deferred.md`): unchanged.

## Tag policy

Local tag `v0.10.1` per D11 (tag-local-only). NOT pushed to remote.

## References

- v0.10.1 design plan: `.omc/plans/v0.10.1-patch.md`
- v0.10.0 commit: `90aa6f1`
- v0.10 critic verdict (2026-05-10; 7.6/10 composite)
- v0.10 architect verdict (2026-05-10; §Categorisation table)
- ADR 0018 §Status-update-2026-05-10b (NEW, v0.10.1)
- D22 (NEW, v0.10.1) — `.omc/plans/decisions.md`
- OQ-13f/g/h/i (NEW, v0.10.1) — `.omc/plans/open-questions.md`
```

---

## §9 Open-questions persistence note

Per planner protocol, this plan's open questions persist to
`.omc/plans/open-questions.md` (as v0.10.1's primary deliverable in §2.4
+ §8.6). No additional `open-questions.md` entries beyond the OQ-13f/g/h/i
+ OQ-13a/d/12a amendments — v0.10.1 is factual-integrity-only and
introduces no new ambiguity.

---

**End of v0.10.1 patch plan. Hand off to executor.**
