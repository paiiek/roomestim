# roomestim v0.16.1 — Patch Design (v0.16.0 follow-up closure + OQ-32)

**Routing**: D29 — `.omc/plans/v0.16.1-patch.md`. **Versioning**: D30 —
`pyproject.toml` `0.16.0` → `0.16.1` (PATCH bump); `roomestim.__version__`
`0.16.0` → `0.16.1`; `roomestim_web.__version__` `0.13-web.0` → `0.13-web.1`
(web lane MINOR PATCH — wiring 보강 + OQ-32 viewer 색 갱신; 신규 surface 없음).
**Predecessor**: v0.16.0 (`2d71523` — material override + 2D blueprint + engine
toggle land). **Trigger**: v0.16.0 `RELEASE_NOTES_v0.16.0.md` §Known gaps에
명시된 MEDIUM-2 / MEDIUM-3 / LOW-1 / LOW-2 / LOW-3 + OQ-32 (3D viewer mesh
색 미갱신) 6 item 일괄 closure. **Mode**: SHORT (factual follow-up patch;
신규 policy 없음 / 신규 ADR 없음 / 신규 D 없음 / 신규 OQ 없음 — OQ-32 closure만).

---

## 1. 목표 — v0.16.0 known-gap 6건 closure (신규 ADR / D / OQ 없음)

v0.16.0 code-review가 absorb 하지 않고 known-gaps로 기록한 5 follow-up + OQ-32
1건을 v0.16.1에서 닫는다. ADR 0030 §A–§E byte-equal (predictor cascade 정책
불변), ADR 0031/0032/0033 본문 byte-equal (재질 override / blueprint / engine
toggle 정책 불변). 6건 모두 wiring · 결정성 보강 · UX 흠집 닫기.

| # | Source | Closure scope |
|---|---|---|
| MEDIUM-2 | `roomestim/viz/blueprint.py` — `matplotlib.use("Agg")` 가 함수 본체 안에서 호출됨 (현재 line 295–297); 이미 다른 코드가 non-Agg backend을 setup한 process에서 호출되면 backend switch 거부 + PNG byte-equal 결정성 회귀 lock 깨질 위험 | 모듈 import 시점에 backend 강제 (`matplotlib.get_backend() != "Agg"` 시 `matplotlib.use("Agg", force=True)`) + 회귀 lock 케이스 추가 |
| MEDIUM-3 | `tests/test_engine_toggle.py::test_cli_export_cli_overrides_env` 가 `/tmp/cli_engine_dir_nonexistent` 만으로 음성 검증 (CLI > ENV precedence는 보장되지만 정상-path positive variant 미검증) | tmpdir에 valid `geometry_schema.json` 2개 (ENV용 / CLI용) 생성 → CLI flag로 가리킨 path 사용 + export 성공 + WARNING 헤더 부재 positive integration variant 신규 |
| LOW-1 | `roomestim_web/app.py:557–560` `_on_apply_overrides_wrapper` inline `__import__("json").loads(changes_str)` 으로 change count 계산 | module top-level `import json` 이동 + `_count_changes(changes_str: str) -> int` named helper 추출 |
| LOW-2 (UX) | `roomestim_web/material_override.py:158–164` Dataframe `interactive=False` + `roomestim_web/app.py:457` `changes_state` `"{}"` pre-seed 만 — surface-table Dataframe interaction이 `changes_state`를 자동 갱신하지 않음. 사용자가 JSON 직접 타이핑해야 함 | Gradio 4.x dropdown column 지원 verification 후 — 지원되면 Dataframe `interactive=True` + Material 컬럼 type `"dropdown"` + `change` 이벤트 → `_dataframe_changes_to_json(rows: list[list[str]], initial_room: RoomModel) -> str` helper 로 `changes_state` 자동 갱신. 미지원 시 폴백: 추가 `gr.Textbox("Changes JSON", lines=2)` 사용자 visible 입력박스 + 도움말 메시지 ("예: `{\"0\": \"glass\"}`"). 두 경로 모두 wrapper 의 `changes_str` 계약은 동일 |
| LOW-3 | `build_material_override_tab()` Dataframe `interactive=False` — LOW-2 와 동일 line. LOW-2 와 묶어 land | LOW-2 와 동일 wiring + dropdown column 지원 시 회귀 lock (Dataframe `interactive` 속성 검증 + dropdown choices 검증) |
| OQ-32 | 3D viewer mesh 색이 Material Override Apply 후 갱신 안 됨 (Apply → AcousticReport + RT60 chart만 재계산, `viewer_plot`은 stale). `roomestim_web/viewer.py:31, 55, 60–67` `Mesh3d(color=MATERIAL_PALETTE[surface.material])` 매핑 로직 존재 — Apply 후 새 room으로 `build_room_figure` 재호출하면 갱신됨 | `_on_apply_overrides_wrapper` 가 `viewer_plot` 도 output 으로 받고 `build_room_figure(new_room, current_layout)` 재호출. layout은 `room_state` 옆에 신규 `layout_state` gr.State 추가하여 `_on_submit` 11→12-tuple로 확장. 신규 viewer 컬러 갱신 회귀 lock 추가 |

비-스코프 (영구 또는 v0.17+ deferral):
- OQ-30 per-wall α decomposition (v0.15.x+ 유지).
- OQ-23 polygon ISM non-shoebox (v0.15.x+ 유지).
- OQ-31 multi-engine schema target (v0.18+ 유지).
- `Surface.kind` enum 확장 (v0.17 schema bump 필요).
- USDZ/gLTF export (v0.17).
- Speaker nudge (v0.18).

---

## 2. 결정 (신규 ADR / D / OQ 없음; ADR 0030 §Status-update-v0.16.1 NEW)

- **ADR 0030 §Status-update-v0.16.1 NEW** (~25줄) — D22 audit-trail-discipline
  패턴 (v0.10.1 / v0.15.1 / v0.15.2 precedent). Item L (MEDIUM-2 closure) +
  Item M (MEDIUM-3 closure) + Item N (LOW-1 closure) + Item O (LOW-2 + LOW-3
  묶음 closure) + Item P (OQ-32 closure — 3D viewer 색 갱신 land + OQ-32
  CLOSED 명시) 5 block. 기존 §Status-update-v0.16 본문 위에 append하며 기존
  본문 retroactive 수정 X.
- **신규 ADR 없음** — 6 item 모두 v0.16.0 정책 surface 의 wiring / 결정성 /
  UX 흠집 closure. 새 policy decision 부재.
- **신규 D 없음** — D39 (evolve helper placement) / D40 (manual Apply
  trigger) / D41 (blueprint coordinate) / D42 (engine validation precedence) /
  D43 (ADR 0009 invariant on evolved room) 5개 v0.16 D 모두 byte-equal.
- **신규 OQ 없음** — OQ-32 closure이지 신규 deferral 아님.
- **D29 lane scope 재확인** — Phase 2 (engine toggle test) 는 core 단독 (test
  파일이 web 의존 없음); Phase 1 / Phase 3 / Phase 4 는 web touch (app.py +
  material_override.py + viewer wiring) — 의도된 lane touch. ADR 0029
  §Cross-lane-geom-amendment scope 침범 없음 (web → core stable public util
  import 패턴 유지).
- **D30 byte-equal 원칙** — `git diff 2d71523 -- roomestim/` 는 (i) `__init__.py`
  version 1줄 + (ii) `roomestim/viz/blueprint.py` ~6 LoC (module-level Agg
  guard) 만 변경. `roomestim_web/__init__.py` MINOR PATCH bump + `app.py` +
  `material_override.py` + (선택) `viewer.py` 영향. 정책 변경 cycle 이므로
  D30 위반 없음.
- **MEDIUM-2 설계 선택** — (A) module import 시점 `matplotlib.use("Agg",
  force=True)` 강제 vs (B) `render_blueprint` 본체 첫 줄에서 `force=True`
  추가 vs (C) `roomestim/viz/__init__.py` 에서 강제. **(A) 채택** — viz/blueprint.py
  를 import만 해도 backend lock-in이 되어 이후 다른 모듈에서 plt.show() 같은
  GUI 호출이 unreachable해지므로 결정성 보장. (B)는 import-only 사용처가
  결정성 깨질 수 있음. (C)는 floorplan_png.py 와 충돌 (이미 함수 안에서
  `matplotlib.use("Agg")` 호출 — 영향 없음 검증 필요). 이미 다른 코드가
  GUI backend을 의도적으로 활성화한 process에서는 `force=True`가 충돌 경고
  로그를 남기지만 functional 영향 없음 (matplotlib 3.7+ 동작 검증).
- **MEDIUM-3 설계 선택** — (A) 기존 negative-precedence 테스트는 그대로 두고
  positive-precedence 신규 케이스 1개 추가 vs (B) negative + positive 합쳐서
  rewrite. **(A) 채택** — 회귀 lock 패턴 분리 (negative = CLI invalid path 시
  실패 보장; positive = CLI valid path 시 ENV 무시 + success). 두 케이스 모두
  D42 precedence를 다른 각도에서 검증.
- **LOW-1 설계 선택** — `_on_apply_overrides_wrapper` 내부 `__import__("json")`
  을 module top-level `import json` + private helper `_count_changes(s: str)
  -> int` 로 분리. helper는 `json.loads` exception 시 0 반환 (현재 동일 행위).
  test_app 에 helper unit test 추가.
- **LOW-2 + LOW-3 설계 선택** — (A) Dataframe dropdown column 사용 +
  `change` 이벤트 핸들러 vs (B) `gr.Textbox` 보조 입력 박스 fallback only vs
  (C) 두 경로 union (dropdown column 지원 검증 후 미지원 시 fallback).
  **(C) 채택** — Gradio 4.x dropdown column이 일부 환경에서 `Dropdown` cell
  렌더링 한계 있음 (Gradio repo issues #5xxx 참고). 1차 시도 (A) → 환경
  smoke 실패 시 자동 fallback (B). 모든 환경에서 `changes_state` 가 valid JSON
  유지되어야 한다는 wrapper 계약 보존. 실 implementation은 executor 가 Gradio
  버전 smoke 후 결정 — plan은 두 경로 모두 명세.
- **OQ-32 closure 설계 선택** — (A) `_on_apply_overrides_wrapper` 가
  `viewer_plot` 추가 output + `build_room_figure(new_room, layout_state)` 재
  호출 vs (B) Plotly Mesh3d trace 의 `color` 속성만 in-place patch vs (C) 3D
  viewer 별도 "재생성" 버튼. **(A) 채택** — `build_room_figure` 는 기존
  helper (v0.13.0+) 이며 재호출 cost = lab_room 약 50ms (mesh 6 surface).
  Apply 버튼 latency budget (현재 ISM cascade 1.9s) 안에서 무시할 수준.
  (B)는 Gradio Plotly trace patch API가 stable 하지 않음. (C)는 사용자
  추가 클릭 필요 — UX 후퇴. layout 은 `_on_submit` 에서 신규 `layout_state`
  gr.State 로 보존 (`room_state` 와 동일 패턴).

---

## 3. 구현 (Phase 1 → Phase 5 순차)

### Phase 1 — MEDIUM-2: matplotlib Agg backend module-level guard

- `roomestim/viz/blueprint.py` 수정:
  - 라인 31 (`from __future__ import annotations`) 아래에 backend guard 블록
    NEW (~6 LoC):
    ```python
    # ADR 0032 §D + v0.16.1 MEDIUM-2 closure: force Agg backend at IMPORT time
    # so that even import-only consumers cannot break PNG byte-equal determinism.
    # `force=True` is required when another module has already initialised a
    # non-Agg backend in the same process (e.g., test runners with `--mpl`).
    import matplotlib  # noqa: PLC0415
    if matplotlib.get_backend().lower() != "agg":
        matplotlib.use("Agg", force=True)
    ```
  - 라인 295–297 영역 `render_blueprint` 본체에서 기존 `matplotlib.use("Agg")`
    line 297 제거 (모듈 top-level guard로 이미 보장됨; 중복 호출은 경고 log
    없이 idempotent 하지만 가독성 위해 제거). `import matplotlib` 라인 295 +
    `import matplotlib.pyplot as plt` 라인 298 은 유지 (현재 lazy import
    패턴 보존; module-level guard 는 backend lock-in 만 담당, pyplot 자체는
    `[viz]` extra 없는 환경에서 import 가능해야 함 — guard 블록의 `import
    matplotlib` 가 함수 본체 `import matplotlib` 보다 먼저 fail 가능함 →
    모듈 top-level 에서 `try: import matplotlib; ... except ImportError: pass`
    로 wrap 하여 graceful degradation 보존).
  - 최종 guard 블록 (확정안, ~9 LoC):
    ```python
    try:
        import matplotlib  # noqa: PLC0415
        if matplotlib.get_backend().lower() != "agg":
            matplotlib.use("Agg", force=True)
    except ImportError:
        pass  # matplotlib is a [viz] extra; render_blueprint() will surface later
    ```
- `tests/test_viz_blueprint.py` 1 케이스 추가:
  - `test_blueprint_module_locks_agg_backend`: `import roomestim.viz.blueprint`
    직후 `matplotlib.get_backend().lower() == "agg"` 회귀 lock. Pre-condition
    으로 test 첫 줄에 다른 backend `matplotlib.use("Agg")` 호출하지 말 것
    (모듈 import 가 backend 강제하는지 검증이 목적).

### Phase 2 — MEDIUM-3: positive CLI > ENV precedence integration test

- `tests/test_engine_toggle.py` 1 케이스 추가 (기존 6 → 7 케이스):
  - `test_cli_export_cli_overrides_env_positive_success`: tmpdir에 2개 fake
    engine dir 생성:
    - `env_engine/proto/geometry_schema.json` — anti-permissive schema
      (`{"type": "object", "required": ["__MUST_NOT_EXIST_PROP__"]}`) → ENV
      path 면 export 실패함.
    - `cli_engine/proto/geometry_schema.json` — permissive schema
      (`{"type": "object"}`) → CLI path 면 export 성공함.
    - `ENV SPATIAL_ENGINE_REPO_DIR=env_engine` + `--validate-engine cli_engine`
      → 결과: exit 0 + `layout.yaml` 생성 + WARNING 헤더 부재 ⇒ CLI path 가
      실제 사용됨 증명 (만약 ENV 가 사용됐다면 required prop 부재로 schema
      validation 실패하여 exit 비-0 이었어야 함).
  - 기존 `test_cli_export_cli_overrides_env` (negative variant) 는 유지 —
    두 케이스가 다른 각도에서 D42 precedence 회귀 lock.

### Phase 3 — LOW-1: app.py wrapper json import refactor

- `roomestim_web/app.py` 수정:
  - 라인 7–19 영역 import 블록 top-level 에 `import json` 추가 (이미 다른
    모듈에서 사용; `material_override.py` 가 이미 top-level import 함).
  - 라인 536 영역 `if _apply_btn is not None:` 블록 위 (module-level) 에
    helper 신규 (~6 LoC):
    ```python
    def _count_changes(changes_json: str) -> int:
        """Return number of entries in a JSON-encoded {idx: material} mapping; 0 on parse error."""
        if not changes_json.strip():
            return 0
        try:
            parsed = json.loads(changes_json)
        except (json.JSONDecodeError, ValueError):
            return 0
        return len(parsed) if isinstance(parsed, dict) else 0
    ```
  - 라인 557–560 `_on_apply_overrides_wrapper` 내부:
    `n = len([k for k in ({} if not changes_str.strip() else __import__("json").loads(changes_str))])`
    → `n = _count_changes(changes_str)` 1줄 단순화.
- `tests/web/test_material_override_ui.py` 1 케이스 추가 (기존 4 → 5):
  - `test_count_changes_helper`: empty string / `"{}"` / `'{"0": "glass"}'` /
    invalid JSON / `'[]'` (non-dict) 5 입력 → 0/0/1/0/0 반환 회귀 lock.

### Phase 4 — LOW-2 + LOW-3 + OQ-32: Material Override 자동 갱신 + 3D viewer 색 재계산

#### Sub-step 4A — Dataframe interactive + dropdown column wiring (LOW-2 + LOW-3)

- `roomestim_web/material_override.py` 수정:
  - 라인 158–164 `build_material_override_tab` 내부 `gr.Dataframe(...)`:
    - 1차 시도 (dropdown column 경로):
      ```python
      dataframe = gr.Dataframe(
          headers=["index", "kind", "material", "polygon_summary"],
          datatype=["str", "str", "str", "str"],
          col_count=(4, "fixed"),
          interactive=True,
          label="표면 목록 (material 열 클릭하여 재질 정정)",
      )
      ```
    - Gradio 4.x dropdown column 직접 미지원 (Gradio API: `gr.Dataframe`
      `column_widths` + `headers` 만 노출, per-column dropdown choices 는
      v4.36+ 부터 partial support). executor 가 Gradio 버전 smoke
      (`python -c "import gradio; print(gradio.__version__)"`) 후:
      - v4.36+ 이고 `gr.Dataframe(..., column_types=[..., "dropdown(choices=...)", ...])`
        지원 시: 위 datatype 에 `[..., {"type": "dropdown", "choices":
        _MATERIAL_CHOICES}, ...]` 추가.
      - 미지원 시: fallback 경로 (sub-step 4B).
  - 라인 165 `apply_btn = gr.Button(...)` 위에 helper Textbox 추가
    (fallback 표시; dropdown 경로에서도 power-user 가 raw JSON 수정 가능
    하도록):
    ```python
    changes_textbox = gr.Textbox(
        label="변경 사항 (JSON)",
        value="{}",
        lines=2,
        info='surface index → material 매핑. 예: `{"0": "glass", "3": "carpet"}`. '
             '재질 열에서 dropdown 변경 시 자동 갱신됩니다.',
    )
    ```
  - 반환 dict 에 `changes_textbox` 추가: `{"tab", "dataframe", "apply_btn",
    "status_md", "changes_textbox"}`.
- `roomestim_web/material_override.py` `_dataframe_to_changes_json(rows:
  list[list[str]], initial_room: RoomModel) -> str` helper NEW (~15 LoC):
  - 입력: Dataframe 현재 rows + `initial_room.surfaces` (baseline material 비교용).
  - 출력: `json.dumps({str(i): rows[i][2] for i in range(len(rows)) if
    rows[i][2] != initial_room.surfaces[i].material.value})` —
    baseline 과 다른 행만 포함.
  - 빈 dict 인 경우 `"{}"` 반환.
- `roomestim_web/app.py` 수정:
  - 라인 487–490 영역 `_mat_comps = build_material_override_tab()` 다음 줄
    부터:
    `_changes_textbox = _mat_comps.get("changes_textbox") if _mat_comps else None`
    추가.
  - 라인 569–573 영역 `_apply_btn.click(...)` 위에 Dataframe change 이벤트
    wiring 추가:
    ```python
    if material_table is not None and _changes_textbox is not None:
        material_table.change(
            fn=lambda rows, room: _dataframe_to_changes_json(rows, room) if room is not None else "{}",
            inputs=[material_table, room_state],
            outputs=[_changes_textbox],
        )
        _changes_textbox.change(
            fn=lambda s: s,  # mirror textbox value into changes_state
            inputs=[_changes_textbox],
            outputs=[changes_state],
        )
    ```
    (Gradio Dataframe `change` 이벤트가 미지원이면 try/except 로 wrap +
    log warning + skip wiring; wrapper 계약 (changes_state 가 valid JSON)
    은 fallback 경로의 사용자 직접 입력으로 유지.)
  - `_dataframe_to_changes_json` import 추가 (line 23 영역).

#### Sub-step 4B — OQ-32: 3D viewer 색 갱신 on Apply

- `roomestim_web/app.py` 수정:
  - 라인 453 영역 `room_state: gr.State = gr.State(value=None)` 아래에 `layout_state:
    gr.State = gr.State(value=None)` 추가.
  - `_on_submit` 반환 11-tuple → 12-tuple 확장: 마지막에 `result.layout` 추가.
    `result_layout` field 명시.
  - 모든 4 반환 경로 (file=None / ValueError / Exception / 정상) 12-tuple
    일관 (None × 4 추가).
  - 라인 526–532 `submit_btn.click(outputs=[...])`: 12요소로 확장
    (`..., room_state, layout_state`).
  - 라인 537–567 `_on_apply_overrides_wrapper`:
    - 시그니처 변경: `def _on_apply_overrides_wrapper(room, layout, changes_json):`
      (3-인자, layout 추가).
    - 정상 분기 끝부분 (status_update 직전) `viewer_plot` 재생성:
      ```python
      try:
          from roomestim_web.viewer import build_room_figure  # noqa: PLC0415
          new_figure = build_room_figure(new_room, layout) if layout is not None else None
      except Exception:
          _LOG.exception("build_room_figure failed in _on_apply_overrides_wrapper")
          new_figure = None
      ```
    - 반환 tuple 5요소 → 6요소: `(room_state, viewer_plot, report_plot,
      report_json, status_update, changes_state_reset)`.
    - 4 반환 경로 모두 6-tuple 일관 (`viewer_plot` 자리는 None / new_figure).
  - 라인 569–573 `_apply_btn.click(...)`:
    `inputs=[room_state, layout_state, changes_state]` (3개);
    `outputs=[room_state, viewer_plot, report_plot, report_json,
    _override_status_md, changes_state]` (6개).
- `roomestim_web/viewer.py` 본체 byte-equal 유지 (color 매핑 로직 이미
  v0.13 부터 존재; 단순히 새 room으로 재호출하면 갱신됨; 추가 wiring 불필요).

#### Sub-step 4C — 테스트

- `tests/web/test_material_override_ui.py` 3 케이스 추가 (Phase 3 1 + Phase 4
  3 = 기존 4 + 4 = 8):
  - `test_dataframe_changes_to_json_helper`: baseline lab_room.surfaces[0] =
    WALL_PAINTED → rows[0][2] = `"glass"` 로 변경 시 `_dataframe_to_changes_json`
    반환 `'{"0": "glass"}'`. 변경 없는 행은 결과에 미포함.
  - `test_dataframe_changes_to_json_no_change`: 모든 행 baseline 그대로 →
    `"{}"` 반환.
  - `test_apply_returns_viewer_figure`: `_on_apply_overrides_wrapper(lab_room,
    fake_layout, '{"0": "glass"}')` 호출 후 6-tuple 반환 + `result[1]` (viewer)
    is not None (build_room_figure 호출 흔적).
- `tests/web/test_blueprint_ui.py` 검증 변경 없음 (Phase 1 module guard 는
  blueprint 회귀 lock 1 추가만으로 충분).
- 기존 `tests/web/test_app_exception_handling.py`,
  `tests/web/test_binaural_fallback.py`, `tests/web/test_wfs_ux.py` 4 반환
  경로 모두 11-tuple → 12-tuple 마이그레이션 (executor 가 패치).

### Phase 5 — 버전 + ADR 0030 §Status-update-v0.16.1 + 릴리스 노트

- `pyproject.toml`: `version = "0.16.0"` → `"0.16.1"` (PATCH).
- `roomestim/__init__.py`: `__version__ = "0.16.0"` → `"0.16.1"`;
  `__schema_version__` 불변 `"0.1-draft"`.
- `roomestim_web/__init__.py`: `__version__ = "0.13-web.0"` → `"0.13-web.1"`
  (PATCH per D30 — UI 신규 surface 없음, wiring 보강 + viewer 색 갱신).
- `docs/adr/0030-predictor-default-switch.md` 끝에 `## §Status-update-v0.16.1
  (2026-05-XX)` 블록 NEW (~25줄, 250줄 → ~275줄):
  - Item L (MEDIUM-2) — module-level Agg backend guard. 근거: PNG byte-equal
    determinism 회귀 lock 강건성.
  - Item M (MEDIUM-3) — CLI > ENV positive precedence integration test 추가.
  - Item N (LOW-1) — `_on_apply_overrides_wrapper` JSON import refactor +
    `_count_changes` helper.
  - Item O (LOW-2 + LOW-3) — Material Override Tab Dataframe interactive wiring
    + `_dataframe_to_changes_json` helper + Textbox 보조 입력 (fallback).
  - Item P (OQ-32 CLOSED) — 3D viewer Plotly Mesh3d color 갱신 on Apply.
    `layout_state` gr.State 추가 + `_on_apply_overrides_wrapper` viewer_plot
    재생성. `MATERIAL_PALETTE[surface.material]` 매핑 (viewer.py line 31, 55)
    재호출로 갱신.
  - D22 audit-trail-discipline 패턴 (v0.10.1 / v0.15.1 / v0.15.2 precedent).
- `RELEASE_NOTES_v0.16.1.md` NEW (~85줄):
  - §What v0.16.0 missed (5 follow-up + OQ-32 — 위 표 6 item).
  - §What v0.16.1 fixes (Item L–P; 신규 정책 없음).
  - §What stays the same (ADR 0030/0031/0032/0033 본문 byte-equal, RoomModel
    frozen invariant, ADR 0009 invariant on evolved rooms, schema 불변).
  - §Test count 표:
    - default lane: v0.16.0 185 → v0.16.1 187 (+2: blueprint module guard,
      engine toggle positive precedence).
    - web lane: v0.16.0 55 → v0.16.1 60 (+5: count_changes, dataframe_to_json,
      dataframe_to_json no-change, apply_returns_viewer_figure, +1 wiring
      smoke).
  - §Migration note: `_on_submit` 반환 11→12 tuple (`layout_state` 추가);
    `_on_apply_overrides_wrapper` 반환 5→6 tuple (viewer_plot 추가). 외부
    consumer는 `roomestim_web` API public 부재이므로 영향 없음 (internal
    helpers 만 변경).
  - §Known gaps (v0.17+ 로 유지): Surface.kind 확장, USDZ/gLTF export, Speaker
    nudge, OQ-30/OQ-23/OQ-31. 신규 known gap 없음.
  - §Tag local-only (D30 web-track versioning patron).

---

## 4. 수용 기준 (총 22 게이트 — 신규 v0.16.1 검증 12 + v0.16.0 회귀 lock 10)

**원칙** (per `feedback-verify-each-step.md`): 매 Phase 직후 + 커밋 직전
풀게이트 재실행. 신규 기능 land 만 확인하고 기존 회귀 패스 금지.

| # | Check | Expected |
|---|---|---|
| 1 | `pytest tests/test_viz_blueprint.py` | 6 passed (+1: `test_blueprint_module_locks_agg_backend`) |
| 2 | `pytest tests/test_engine_toggle.py` | 7 passed (+1: `test_cli_export_cli_overrides_env_positive_success`) |
| 3 | `pytest tests/test_edit_room.py` | 8 passed (byte-equal vs v0.16.0) |
| 4 | `pytest tests/web/test_material_override_ui.py` | 8 passed (+4: count_changes, dataframe_to_json baseline, no-change, apply_returns_viewer_figure) |
| 5 | `pytest tests/web/test_blueprint_ui.py` | 2 passed (byte-equal vs v0.16.0) |
| 6 | `pytest tests/web/test_app_exception_handling.py + test_binaural_fallback.py + test_wfs_ux.py` | 모두 passed (12-tuple 마이그레이션) |
| 7 | `pytest -m "not lab and not web"` | ≥ 187 passed + 4 skipped (+2 from v0.16.0 185 baseline; blueprint guard + engine toggle positive) |
| 8 | `pytest tests/web/` | ≥ 60 passed + 1 skipped (+5 from v0.16.0 55 baseline) |
| 9 | `ruff check roomestim/ roomestim_web/ tests/ scripts/` | All checks passed |
| 10 | `mypy --strict roomestim/` | 0 errors (source files 동일; blueprint.py guard 만 변경) |
| 11 | `roomestim.__version__` | `"0.16.1"` |
| 12 | `pyproject.toml version` | `"0.16.1"` |
| 13 | `roomestim_web.__version__` | `"0.13-web.1"` |
| 14 | `roomestim.__schema_version__` | `"0.1-draft"` (불변) |
| 15 | `python -c "import roomestim.viz.blueprint; import matplotlib; assert matplotlib.get_backend().lower() == 'agg'"` smoke | exit 0 (MEDIUM-2 module-level guard 확인) |
| 16 | `render_blueprint(lab_room, placement, /tmp/bp.png) ×2` byte-equal | True (matplotlib Agg 결정성, v0.16.0 회귀 lock 보존 — guard 강화로 더 견고) |
| 17 | `roomestim export ... --no-engine-validation` | exit 0 + WARNING 헤더 (v0.16.0 회귀 lock 보존) |
| 18 | `roomestim export ... --validate-engine VALID_PATH (CLI) + ENV INVALID_PATH` | exit 0 + WARNING 부재 (CLI > ENV positive 신규 케이스) |
| 19 | `evolve_room_material(lab_room, 0, MaterialLabel.GLASS).surfaces[0].absorption_500hz` smoke | `0.04` (v0.16.0 회귀 lock 보존) |
| 20 | ADR 0009 invariant `ism_rt60 ≥ eyring_rt60 - 1e-6` on evolved room (50 random seeds) | 유지 (D43 회귀 lock 보존) |
| 21 | `build_demo()` smoke | 신규 탭 2개 (재질 정정 + 2D 블루프린트) 존재 (v0.16.0 회귀 lock) + Material Override Tab 의 Dataframe `interactive=True` (LOW-2 wiring 확인) + `_changes_textbox` 컴포넌트 존재 |
| 22 | `docs/adr/0030-predictor-default-switch.md` + `RELEASE_NOTES_v0.16.1.md` | §Status-update-v0.16.1 블록 존재 (Item L–P) + RELEASE_NOTES NEW 파일 존재 (~85줄 ±10) |

**풀세트 회귀 lane** (커밋 직전 verifier 패스):
- Gate 7 + Gate 8 = 247+ default + web lane 통합 (v0.16.0 240 baseline + 7 신규).
- Gate 9 + Gate 10 = 정적 분석 GREEN.
- Gate 15 + Gate 16 + Gate 17 + Gate 18 + Gate 19 + Gate 20 = 6 smoke + invariant
  회귀 lock 직접 실행.

---

## 5. 리스크 + 롤백

| 리스크 | 영향 | 완화 / 롤백 |
|---|---|---|
| MEDIUM-2: module-level `matplotlib.use("Agg", force=True)` 가 다른 process consumer (예: Jupyter, GUI dev) 의 backend 강제 override → 사용자 의도 backend 깨짐 | 외부 사용자가 blueprint 모듈 import 시 GUI 그래픽 끊김 | guard 가 `if matplotlib.get_backend().lower() != "agg":` 조건부 — 이미 Agg 면 no-op. 다른 backend 면 강제 switch 후 경고 log (matplotlib 동작). 롤백: `force=True` 제거 → 조건부 strict mode 만 유지 (다른 backend 이미 활성화 시 함수 본체 `matplotlib.use("Agg")` 호출이 무시되어 PNG 결정성 깨질 수 있음 → 대신 함수 본체에서 `force=True` 만 추가하는 약화 옵션). |
| MEDIUM-3: positive precedence 테스트의 anti-permissive schema 가 모든 layout YAML 에 대해 `required: [__MUST_NOT_EXIST_PROP__]` 로 reject — 만약 실제 layout YAML 이 우연히 이 키를 포함하면 false-positive | 미래 layout 스키마 변경 시 테스트가 잘못 통과 | (i) 키 이름이 `__MUST_NOT_EXIST_PROP__` 더블 underscore + 영구 미사용 명명 — 충돌 가능성 0. (ii) test docstring 에 "this property name is intentionally absurd; do not add to layout schema" 명시. 롤백: `pytest.skip` 메시지로 회귀 발견 시 키 이름 rotate. |
| LOW-1: `import json` module top-level 이동 — `_count_changes` 가 `json.JSONDecodeError` 외에 다른 exception (`TypeError`, `RecursionError`) 던질 가능성 | `_on_apply_overrides_wrapper` crash | helper docstring 에 "0 on parse error" 명시 + `except (json.JSONDecodeError, ValueError, TypeError)` 까지 confine. test 케이스에 None / int 등 비-str 입력 회귀 lock 추가 가능 (Phase 3 sub-test). 롤백: inline import 패턴 복원 (LOW-1 deferral). |
| LOW-2: Gradio Dataframe `change` 이벤트가 v4.x 일부 환경 (HF Spaces) 에서 부분 지원만 → dropdown column 의 user interaction 캡쳐 실패 | Apply 후 변경 미반영 + UX 후퇴 | 폴백 textbox 가 항상 활성 (`changes_textbox` 컴포넌트는 dropdown 경로와 무관하게 존재) — 사용자가 직접 raw JSON 수정 가능. wrapper 계약 (`changes_state` 가 valid JSON) 은 textbox 직접 입력으로 유지. executor 가 Gradio 버전 smoke 후 결정. 롤백: dropdown column 시도 제거 + textbox-only mode (LOW-2 v0.16.2 로 push) → ADR 0030 §Status-update-v0.16.2 NEW 또는 v0.16.1 Item O scope 축소 명문화. |
| LOW-3: Dataframe `interactive=True` 가 cell edit 허용하지만 dropdown 미지원 환경에서 사용자가 임의 string 입력 가능 (validation 부재) | `_dataframe_to_changes_json` 이 invalid material 포함한 JSON 생성 → Apply 시 `MaterialLabel(invalid)` ValueError | 기존 `on_apply_overrides` 핸들러가 이미 valid_values 검증 + errors list 반환 (`roomestim_web/material_override.py:103–117`). 잘못된 material 입력 시 status_md 에 한국어 에러 메시지 표시. 회귀 lock 케이스 추가 (Phase 4 sub-test option). |
| OQ-32: `build_room_figure` 재호출이 Apply 버튼 click latency 증가 (lab_room 약 50ms — mesh 6 surface; 큰 room 은 ~200ms 가능) | UX 지연 | Plotly figure rebuild 는 ISM cascade (1.9s) 대비 무시할 수준. latency 측정 smoke (`time.perf_counter()`) Phase 4 test 에 추가 가능. 롤백: viewer_plot 갱신 OFF + status_md 에 "3D 뷰어는 새 실행 시 갱신됩니다" 메시지 (OQ-32 v0.17 로 push). |
| `_on_submit` 반환 tuple 12-확장이 외부 test 깨짐 | 회귀 lane (test_app_exception_handling 등) fail | 4 기존 web test 모듈 모두 `result[:N]` 또는 `len(result)` 검증 패턴 — 신규 12-tuple 길이로 마이그레이션 명시 (Phase 4 sub-step 4C). 회귀 lock 케이스 (`len(_on_submit(None, ...)) == 12`) 신규 추가. 롤백: layout_state 분리 안 함 + `_on_apply_overrides_wrapper` 가 매번 file 재읽기 (느림) → OQ-32 partial closure (viewer 만 갱신 안 함). |
| `roomestim_web.__version__` PATCH bump 가 HF Spaces 배포 트리거 | HF Spaces cold-boot 시 ADR 0029 §B daemon thread 재실행 + 신규 wiring 로딩 | 정책 변경 없음 (ADR 0029 본문 byte-equal). 신규 system dep 없음 (matplotlib 이미 `[viz]`). packages.txt byte-equal. 검증: Gate 9 ruff + Gate 10 mypy + Gate 21 build_demo smoke 통과 시 boot 무결성. |
| ADR 0030 §Status-update-v0.16.1 블록이 ADR 0031/0032/0033 본문이 아닌 ADR 0030 에 누적되어 ADR 0030 길이 비대 (현 250 → ~275줄) | Audit-trail 가독성 저하 | D22 audit-trail-discipline 패턴 의 의도된 cost — `grep "Item [A-Z] —" docs/adr/0030*` 1줄로 모든 v0.15+ closure 항목 추적 가능. ADR 0030 ≥ 400줄 도달 시 v0.17 에서 §Status-update sections 분리 검토 (별도 ADR-meta 파일 또는 `docs/adr/0030-history.md` split). 본 patch 에서는 split 안 함. |
| Phase 4 sub-step 4C 의 fake_layout (placeholder PlacementResult) 가 `build_room_figure` 의 internal field 요구 검증 안 됨 | test 의 fake layout 이 production layout 과 구조 불일치 → 회귀 미감지 | tests/test_edit_room.py 의 lab_room + run_placement 패턴 재사용 (`from roomestim.place.dispatch import run_placement; layout = run_placement(lab_room, "vbap", 5, 2.0, 0.0)`). 실제 PlacementResult instance 로 test. |

---

## 6. 버전

**v0.16.1** — v0.16.0 follow-up patch (PATCH bump per ADR 0030
§Status-update-v0.16.1). v0.16.0 code-review known-gap 5건 (MEDIUM-2/3 +
LOW-1/2/3) + OQ-32 (3D viewer mesh 색 갱신) = 총 6 item closure. 신규 정책
없음, 신규 invariant 없음, 신규 ADR 없음, 신규 D 없음, 신규 OQ 없음 (OQ-32
는 CLOSED 명시). RoomModel frozen invariant 보존, ADR 0009 ISM ≥ Eyring
invariant 보존, schema 불변 (`__schema_version__ = "0.1-draft"`).

**Web lane**: `roomestim_web.__version__` `0.13-web.0` → `0.13-web.1` (PATCH
per D30 — wiring 보강 + viewer 색 갱신; 신규 UI surface 없음, HF Spaces
재배포 트리거됨). D29 lane separation 유지 (web → core 단방향).

**예상 LoC 변화** (executor 추정):
- `roomestim/viz/blueprint.py` ~+6 LoC (module guard) / ~-1 LoC (함수 본체
  `matplotlib.use` 제거) ⇒ 순 +5 LoC.
- `roomestim_web/app.py` ~+30 LoC (import json + _count_changes helper +
  layout_state + 12-tuple 확장 + 3-인자 wrapper + Dataframe change event +
  viewer rebuild).
- `roomestim_web/material_override.py` ~+25 LoC (`_dataframe_to_changes_json`
  helper + interactive Dataframe + changes_textbox).
- `tests/test_viz_blueprint.py` ~+15 LoC (+1 케이스).
- `tests/test_engine_toggle.py` ~+30 LoC (+1 케이스 with 2 fake schema).
- `tests/web/test_material_override_ui.py` ~+50 LoC (+4 케이스).
- `tests/web/test_app_exception_handling.py` + `test_binaural_fallback.py` +
  `test_wfs_ux.py` ~+0 LoC 순변화 (12-tuple 마이그레이션, 길이 동일).
- `docs/adr/0030-predictor-default-switch.md` ~+25 줄 (§Status-update-v0.16.1).
- `RELEASE_NOTES_v0.16.1.md` ~+85 줄 (NEW).
- 총 ~+260 LoC + ~+110 docs.

**v0.16.1 land 후 다음 큰 작업** = v0.17.0 (Surface schema 확장 — 기둥/도어/
창문; `RoomModel.schema_version` `"0.1-draft"` → `"0.2-obstacles"` MAJOR
bump). 사용자가 "v0.16.1 + v0.17 두 개 순서대로 오토 진행" 지시 — v0.16.1
verifier GREEN 직후 v0.17.0 planner expand (`.omc/plans/v0.17-design.md`
skeleton 확장) 진입.

**D26 forbidden-indefinite-deferral 패턴 유지** — 작은 follow-up도 다음
패치에서 닫는다 (v0.10.1 / v0.15.1 / v0.15.2 precedent). OQ-32 도 v0.16.1
한 cycle 안에서 closure 함으로써 패턴 강화.
