# roomestim v0.18 — Design (speaker nudge + layout round-trip)

**Status**: SKELETON (planner expansion pending; depends on v0.17 land).
**Routing**: D29 — `.omc/plans/v0.18-design.md`. **Versioning**: D30 — MINOR bump `0.17.0` → `0.18.0`; `roomestim_web.__version__` `0.14-web.0` → `0.15-web.0`.
**Predecessor**: v0.17.0.
**Mode**: MEDIUM (output 편집 — input pipeline 안정화 후 자연스러운 다음 단계).

## 1. 목표 — 사용자 4-제안 ② (출력 편집)

스피커 좌표 미세 조정 + layout.yaml round-trip (parse → edit → write back + 재검증).

## 2. 결정 (planner expansion needed)

- ADR 0034 NEW — Layout round-trip 정책 (정밀도 보존, comment 보존 X)
- D44 NEW — 스피커 nudge UI granularity (1°? 5°? 0.1m?)
- D45 NEW — Layout 편집 후 자동 재검증 (engine schema)

## 3. 구현 Phase (high-level)

### Phase 1 — Layout parse + evolve
- `roomestim/load_layout.py` NEW — `layout.yaml` → `PlacementResult` parse
- `evolve_placement(placement, *, speakers=...) -> PlacementResult`
- Round-trip 테스트: write → read → equal

### Phase 2 — Web UI 스피커 편집
- 3D Viewer에 스피커 클릭 핸들러 (Plotly customdata)
- 사이드바 "Selected Speaker" 패널 — az/el/radius 입력 필드
- "Apply" → evolve_placement → 재렌더

### Phase 3 — Layout 재검증
- 편집 후 engine schema 재검증 자동
- 위반 시 빨간 경고 (regularity_hint 위반 등)

### Phase 4 — CLI 편집 명령
- `roomestim edit layout.yaml --speaker 0 --az 45` (시안)
- 변경 후 자동 write + diff 출력

### Phase 5 — 테스트 + 문서
- `tests/test_layout_round_trip.py` NEW
- `tests/web/test_speaker_nudge_ui.py` NEW
- `tests/test_cli_edit.py` NEW
- ADR 0034 작성
- README §출력 편집 절 NEW
- RELEASE_NOTES_v0.18.0.md

## 4. 수용 기준 (planner expansion needed)
- layout.yaml round-trip: byte-equal when no edit
- 편집 후 schema validation 자동
- 195+ → 210+ passed

## 5. 리스크
- Plotly 3D 클릭 정확도 (특히 small marker)
- 편집 충돌 (다중 사용자 시 — 우선순위 낮음)

## 6. 버전
**v0.18.0** — MINOR bump.
