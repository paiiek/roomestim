# v0.24.x — 비-shoebox 재구성(Cycle B) + 다중뷰 정확도 스파이크(Cycle A)

## RESUME POINTER
- **결정**: 사용자 "B + A 둘 다 연속". 자율 execute→verify(full gate)→commit→repeat. OMC 오케스트레이션(executor→code-reviewer→executor-fix→독립 verifier), 자기승인 금지.
- **근거(3-lane 분석 수렴)**: 다운스트림 스택(placement/export/RIR-binaural `from_corners+extrude`/RT60-Eyring/listener_area)은 **이미 임의 폴리곤 수용**. 유일 갭 = 소스: `floor_polygon.py:39` 무조건 NotImplementedError(dead) + `mesh.py:135` convex-hull이 오목성 소거. → B가 A의 구조적 선행조건(고정밀 비직사각 재구성이 내려앉을 곳 확보).
- **게이트 baseline (시작 시점)**: default+web 합산 387p/8s exit0. canonical `/home/seung/miniforge3/bin/python -m pytest`. ruff/mypy(roomestim only)/tense EXIT0.

## 진행 상태 (체크포인트)
- [x] A-pre: 라이선스 체크 — **VGGT 상업 OK**(2025-07-29 재라이선스, 군사 제외; kill-switch 해소). **MASt3R CC-BY-NC-SA 차단**. → A는 VGGT 단독 진행.
- [x] B1: `floor_polygon_from_mesh` 구현 (`shapely.concave_hull` ratio=0.4/simplify=0.05, 신규 의존 0, CCW simple polygon)
- [x] B2: MeshAdapter 와이어 (`floor_reconstruction` 생성자 인자 + `ROOMESTIM_MESH_FLOOR_RECON` env; default=convex **byte-equal** 회귀핀)
- [x] B3: degeneracy 가드 (ratio∈(0,1]incl.NaN, <3pts→ValueError, MultiPolygon→최대, holes→exterior, concave degeneracy→convex+UserWarning)
- [x] B 검증·커밋: full gate GREEN(default 312/5, web 86/4, ruff/mypy/tense EXIT0) → code-review APPROVE-WITH-NITS(적용) → 독립 verifier PASS(0 blocker) → v0.24.0 MINOR 범프 + D82 + ADR0042 §Status-update-v0.24.0 + OQ-13e 부분진척 + RELEASE_NOTES. **커밋 예정(이 커밋)**.
- [ ] A: OQ-53 out-of-repo 다중뷰 스파이크 (**VGGT 단독**) → metric-scale 안정성 + 점군→floor-ring 계약 추출 verdict → ADR 0045 §C 갱신

## 다음 = Cycle A (OQ-53)
재개 시 진실원천. VGGT(facebookresearch/vggt, 상업OK) 단독. out-of-repo 스파이크(HorizonNet 선례 `/home/seung/mmhoa/spike-image-geometry/`처럼 repo 무변경). 게이트=ADR 0045 blocking gate #2: (i) 다점 앵커 metric-scale가 단일-파노 단일 cam_h(±10cm→median 18→32-38cm) 대비 안정적, (ii) 점군→RANSAC/Manhattan 평면적합→`floor_polygon`+`ceiling_height_m`+vertical-rect walls 계약 산출, 코너오차 ≤15cm(vs RoomPlan ~8.5cm). B의 concave floor-ring 추출기 재사용 가능. GT=ScanNet/ScanNet++/ARKitScenes posed multi-view 또는 Stanford2D3D 재유도(5-10방이면 verdict 충분).

## 설계 출처
- B 슬라이스/테스트 게이트: architect 스코핑(이 세션) — ADR 0042(live-mesh corner) + OQ-13e 거버넌스, D74(alpha-shape 채택). 신규 무거운 의존성 0(shapely 이미 hard dep).
- A 게이트: ADR 0045 blocking gate #2 / OQ-53(metric-scale 안정성 + floor-ring 계약 추출) / Reverse-criterion #3(단일-파노 대비 유의미 개선).

## 부수 발견(코드 무변경 cheap fix 후보, 이번 사이클 범위 밖 — 추후)
- [CRITICAL] docs/architecture.md ADR 인덱스 2/42만 등재 → 재생성 필요
- web-tier mypy 미게이트(20에러), OQ-13i 오해소지 CLOSED
- v0.23.0 BRIR `synthesize_brir` UI 미연결(dead from user)
- OQ-31 D26 케이던스 위반(v0.18 하드월 미준수)

## 커밋
- B는 in-repo 코드 변경 → web-tier feature, 버전 범프 검토(MINOR). 커밋·푸시는 게이트 GREEN 후.
