# roomestim v0.18.0 — Verifier Report (independent re-run)

**Date**: 2026-05-22
**Tree**: working tree (uncommitted), HEAD = `0246659` (v0.17.0)
**Interpreter**: `/home/seung/miniforge3/bin/python` (Python 3.12.11) — has roomestim deps + ruff 0.15.12 + mypy 1.20.2 colocated. (`python` not on PATH in non-login shell; `/usr/bin/python3` also imports roomestim 0.18.0.)

## Verdict: VERIFIED GREEN — 0 blockers

All requested gates re-run with fresh evidence. Round-trip honesty claim independently confirmed.

| # | Gate | Result | Evidence |
|---|------|--------|----------|
| 1 | default lane `-m "not lab and not web"` ≥259 | PASS | **260 passed, 6 skipped, 61 deselected** in 38.31s |
| 2 | web lane `tests/web/` ≥63 | PASS | **70 passed, 1 skipped** in 14.08s (baseline assumption 59 was stale; actual far above 63) |
| 3 | ruff `roomestim/ roomestim_web/ tests/` | PASS | `All checks passed!` |
| 4 | mypy --strict roomestim/ 0 errors | PASS | `Success: no issues found in 39 source files` |
| 5a | roomestim.__version__ == "0.18.0" | PASS | `'0.18.0'` |
| 5b | pyproject version "0.18.0" | PASS | `version = "0.18.0"` |
| 5c | roomestim_web.__version__ == "0.15-web.0" | PASS | `'0.15-web.0'` |
| 5d | __schema_version__ == "0.2-draft" (UNCHANGED) | PASS | `'0.2-draft'` — not bumped |
| 6 | D29 `grep -rn 'import roomestim_web' roomestim/` 0 hits | PASS | 0 hits (grep exit 1) |
| 7 | RT60 negative control == 1.9190766987173207 | PASS | `rt60_s = 1.9190766987173207`, byte-equal True (built lab_room via RoomPlanAdapter+area_dict exactly like test) |
| 8 | ADR 0009: ism ≥ eyring − 1e-6 | PASS | ism=1.9190766987173207 ≥ eyring=0.5377699462619655 − 1e-6 → True |
| 9 | Round-trip honesty (load-bearing) | PASS | see below |
| 10a | CLI `edit --del-deg 3` exit 0 + write + diff | PASS | exit 0; wrote layout.yaml (1259 B); diff shows spk0 `el_deg 0.0→3.0000000000000004` |
| 10b | CLI `--daz 5 --dx 0.1` non-zero + "mutually exclusive" | PASS | exit 1; stderr `error: nudge_speaker: spherical Δ and Cartesian Δ are mutually exclusive` |

### Per-module new-test counts (match design §4 table exactly)
- test_edit_placement.py: 8 passed
- test_layout_round_trip.py: 10 passed
- test_cli_edit.py: 6 passed
- test_export_round_trip.py: 4 passed (HIGH-2 export FIX lock)
- test_export_layout_yaml.py: 7 passed (existing writer suite — MED-1 writer untouched)
- web/test_speaker_nudge_ui.py: 4 passed
- Total new: 28 default + 4 web. 260 = 231 baseline +28 +1 (web baseline drift). Default lane comfortably ≥259.

### Gate 9 — Round-trip honesty (independent test, scratch script run then deleted)
**(a) NON-axis-aligned layout (az=120°, el=15°, CIRCULAR):**
- max position drift res1→res2 (read→write→read): **3.152e-16 m** — ≤1e-9 Level-1 structural contract HOLDS.
- max position drift res0→res1 (original→1st read): **4.578e-16 m** — ≤1e-9 HOLDS.
- byte-equal na1.yaml == na2.yaml: **False** — exactly as executor honestly claimed. Max serialized `dist_m` drift **4.441e-16** (~1 ULP), az_deg drift 0. Byte-equality is NOT the contract; ≤1e-9 structural equivalence IS, and it holds.

**(b) EXPLICIT-AIM layout (aim along az=0, speaker at az=45; toward-origin default would be −135.0):**
- 1st write x_aim_az_deg = **0.0** (explicit).
- NEW reader restored aim_direction = Point3(0.0, 0.0, 1.0).
- after read→write x_aim_az_deg = **0.0** — explicit aim PRESERVED, NOT corrupted to −135.0; ex1==ex2 byte-equal True.

**(b-bug) Reproduced PRE-v0.18 bug with committed HEAD reader:**
- HEAD reader returns aim_direction = **None**; re-write corrupts explicit `0.0` → **−135.0** (toward-origin). Bug confirmed real; v0.18 reader genuinely fixes a shipped corruption.

The executor's honesty claims hold up under independent test: axis-aligned + explicit-aim are byte-equal idempotent; non-axis-aligned drifts ~1 ULP in serialized dist_m per cycle but stays ≤1e-9 structurally. No overclaim found.

## Recommendation: APPROVE
Every acceptance gate verified with fresh post-implementation evidence; the load-bearing round-trip honesty claim and the shipped-export aim-corruption fix are both independently confirmed.
