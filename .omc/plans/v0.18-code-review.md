# v0.18.0 Code Review — Speaker nudge + layout.yaml round-trip

**Reviewer**: code-reviewer (separate pass, read-only)  **Date**: 2026-05-22
**Base**: HEAD v0.17.0 (0246659), working tree dirty  **Verdict**: APPROVE

## Evidence collected
- 28 v0.18 core tests PASS (test_edit_placement / test_cli_edit / test_export_round_trip / test_layout_round_trip)
- Default lane: 255 passed, 12 skipped, 53 deselected, 0 failures, no regressions
- D29 lane separation: `grep -rn 'import roomestim_web' roomestim/` = 0 hits
- py_compile + import smoke on all 9 modified modules: clean
- Empirical round-trip: non-axis-aligned (az=30/120/210/300) is NOT byte-equal (honest);
  Level-1 structural drift = 3.3e-16 single cycle, bounded 2.2e-16 over 10 iterations (no accumulation)
- WARNING-header / in-place clobber / aim-preservation / WFS-collector edge: all verified clean
- mypy/ruff/web-UI NOT runnable in reviewer env (mypy/ruff/fsspec absent) — same env-drift footnote as prior releases

## Findings
| Sev | Location | Issue | Disposition |
|-----|----------|-------|-------------|
| (none CRITICAL/HIGH) | | byte-equal honesty + Level-1 contract both verified | — |
| LOW-1 | cli.py _cmd_edit | double validation when validate ON (collector + writer) | accept (nicer UX errors; harmless) |
| LOW-2 | cli.py _cmd_edit | --no-engine-validation diff shows added WARNING header on a previously-validated input | accept (semantically correct) |
| LOW-3 | layout_yaml.validate_placement | bare `except Exception` | accept (deliberate UX collector per ADR 0036 §D) |

No blockers.
