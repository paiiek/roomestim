# roomestim v0.12.0 — Design Plan (DELIBERATE-mode hybrid scope)

> **PLAN NATURE NOTE (2026-05-11)**: v0.12.0 is a **minor-version bump**
> on top of v0.11.0 (`eee3014`). It ships a 4-item hybrid scope that
> closes the most visible v0.11 honesty-pending flag (Vorländer verbatim
> citation for MELAMINE_FOAM α₅₀₀), tightens two low-cost bookkeeping
> guards (lint-scope expansion + OQ-13f reverse-criterion re-examination),
> and lands the v0.11-still-OPEN conference glass-heavy disagreement
> characterising study (OQ-13b):
>
> 1. **Item C — OQ-13a verbatim closure** (Vorländer 2020 §11 / Appx A
>    α₅₀₀ = 0.85 pending → verbatim or envelope-confirmed; → ADR 0019
>    §Status-update; potentially ADR 0021 NEW if verbatim shifts the
>    value outside the [0.80, 0.95] invariant envelope).
> 2. **Item K — OQ-13h reverse: CI lint scope expansion** (add
>    `docs/perf_verification_*.md`, `docs/architecture.md`, `README.md`
>    to `scripts/lint_tense.py` scope; preemptive guard).
> 3. **Item L — OQ-13f reverse-criterion re-examination** (with lab
>    returned to PASS-gate at v0.11, is `assert rel_err < 0.20` now a
>    soft FAIL gate per ADR 0019 §Reverse-criterion? Bookkeeping pass.)
> 4. **Item D — OQ-13b conference glass-heavy Sabine-shoebox residual
>    characterising study** (-22.7% rel_err under paper-faithful map;
>    compute mirror-image-source-method ratio vs Sabine on conference
>    room; if ratio > 1.15: name as Sabine-approximation effect, open
>    Eyring / Millington-Sette adoption path; if ratio < 1.10: name as
>    coefficient-sourcing issue, defer to v0.13+ enum/coefficient work).
>    → ADR 0021 NEW (study findings); D26 NEW (predictor-adoption
>    deferral policy if applicable).
>
> The planner's job is NOT to relitigate whether each item belongs in
> v0.12 — it is to **lock the scope envelope**, **file-op table**,
> **gating tests**, **ADR list**, and **pre-mortem** for v0.12. Each
> open question listed in the parent prompt receives a single
> planner-locked default (with rationale + reverse-criterion); the user
> is not asked to re-choose.
>
> v0.12 is **DELIBERATE-mode**: it adds a NEW characterising study with
> a new test surface (mirror-image-source residual ratio), a new ADR,
> potential D-decision additions, and lint-scope expansion. It is NOT
> SHORT-mode (which is reserved for factual-integrity-only patches per
> v0.10.1 precedent).

---

- **Date**: 2026-05-11 (drafted) / 2026-05-12+ (target scope-lock + ship)
- **Author**: oh-my-claudecode:planner (consulted by user after v0.11.0
  shipped 4-item hybrid scope; OQ-13a closed with VERBATIM-CITATION-PENDING
  honesty flag; OQ-13b still OPEN; OQ-13h reverse-criterion considered
  but not fired at v0.11; OQ-13f resolution-candidate explicitly named
  "structural-sign assertion may become soft FAIL gate" as the reverse
  trigger that v0.12 must re-examine after PASS-gate recovery).
- **Predecessor**: v0.11.0 (`eee3014` — MELAMINE_FOAM enum / lab A11
  PASS-gate recovered at rel_err +2.40 % / CI tense-lint / in-situ A10b
  protocol DOC stub / D23/D24/D25).
- **Branch decision**: **v0.12.0 minor** (not v0.11.1 patch — adds a
  new ADR 0021 + new test surface for mirror-image ratio + lint-scope
  expansion + potential D26; library/test surface area exceeds patch
  threshold; SemVer minor bump is the correct lane).
- **Status**: SCOPE LOCKED — diff envelope target 300-500 lines of
  library/test code + ~200 lines of plan/ADR prose (planner-locked: see
  §0.2 below); 12-14 file ops total (planner-locked at 13 in §0.2);
  default-lane test count target **124 → 128** (+4 net: see §0.2.B).
- **Mode**: **DELIBERATE** (characterising study + new ADR + lint-scope
  expansion; v0.11 precedent).

This plan locks the **scope envelope and acceptance criteria** for v0.12.
It does NOT prescribe the exact final mirror-image-source ratio value
for the conference room beyond "compute the ratio per §2.4 procedure
and record the empirical result"; the executor empirically determines
the ratio. Reverse-criteria are explicit and ratchet-safe.

---

## §0 Locked-scope summary

### §0.1 Locks (planner-resolved opens — single default; no multi-choice)

| Lock | Choice | Rationale |
| --- | --- | --- |
| Branch decision | **v0.12.0 minor** | New ADR 0021 + new test surface (mirror-image ratio computation) + lint-scope expansion + potential D26; exceeds patch threshold. Reverse-criterion: if at code-review time the diff stays under 100 lines + zero new test surfaces (i.e., item D residual study is dropped mid-flight), downgrade to v0.11.1 patch with OQ-13b re-deferred to v0.13+. |
| Bundle decision | **C + K + L + D** (4-item bundle) | Items C, K, L are individually too small for their own release (~50-200 lines each, mostly bookkeeping); bundling with D (~300-400 lines, a characterising study with real numerical work) amortises a release cycle and matches the v0.11 4-item bundling precedent. Reverse-criterion: if D residual study reveals the ratio is ambiguous (1.10 ≤ ratio ≤ 1.15 zone — neither Sabine-approximation nor coefficient-sourcing signature dominates), defer D's ADR 0021 finding to v0.13+ and ship v0.12 as 3-item (C + K + L) under a downgraded scope. |
| Item C: verbatim source mechanism | **Executor performs Vorländer 2020 §11 / Appx A lookup** (page + row + panel-thickness column citation extraction); records VERBATIM α₅₀₀ value in ADR 0019 §Status-update-2026-05-12 (in-place D22 hybrid pattern; ADR 0019 is a same-cycle ADR so the §Status-update pattern applies). If verbatim value lands inside [0.80, 0.95] invariant envelope → §Status-update only (no new ADR; coefficient invariant test value brackets stay; commit notes record the verbatim citation). If verbatim value lands OUTSIDE [0.80, 0.95] → ADR 0021 NEW (records the coefficient revision + the invariant-envelope shift + the test-update). Per the v0.11 ADR 0019 §References commitment "Citation status (v0.11): verbatim page + row + panel-thickness column PENDING". | The OQ-13a resolution at v0.11 explicitly designated v0.11 as "envelope mid-value" + verbatim-citation-pending; v0.12 closes the pending flag. The two-branch path is necessary because we don't know which branch lands until the textbook lookup completes — the planner cannot pre-commit to a single ADR-vs-no-ADR outcome. Reverse-criterion: if Vorländer's "melamine foam panel" entry is itself a planner-envelope range (e.g., "0.80-0.95 depending on panel thickness 2-4 inch") without a single α₅₀₀ scalar, executor MUST record the panel-thickness assumption explicitly and use the geometric mean (0.87) — NOT the v0.11 mid-value (0.85) — and document as fixed in §Status-update-2026-05-12. |
| Item C: coefficient value choice if verbatim differs | **Adopt verbatim value verbatim** (DO NOT keep 0.85 if Vorländer publishes 0.80 or 0.90). Re-run lab A11 test; record new `predicted` + `rel_err`; if rel_err leaves [-0.20, +0.20] PASS-gate envelope: STOP and escalate (per §0.4 STOP rule #7); the most likely planner-response is fixture re-map (partial-foam walls) — NOT silent coefficient retuning. | The honesty-first principle from D22 + v0.10 cycle is: cite the source verbatim, then live with the empirical consequence. Reverse-criterion: if the verbatim value forces lab A11 to leave PASS-gate, the planner's response is to add a successor ADR 0022 reframing PASS-gate as fixture-flip-dependent (per ADR 0019 §Reverse-criterion item 3), NOT to silently retune. |
| Item K: lint scope expansion target | **Add `docs/perf_verification_*.md`, `docs/architecture.md`, `README.md`** to `scripts/lint_tense.py` scope. Keep current 3 file-family scope intact (fixture README + ADR + past-RELEASE_NOTES). Expansion is ADDITIVE; no existing exclusion semantics change. | OQ-13h reverse-criterion explicitly named these three file families as the "scope expansion if a leak lands outside current scope" trigger. At v0.11 ship time, the README.md was updated to reflect v0.11.0 state (commit `8ab5d54`) — the lint did NOT block it because README.md is out of current scope; but a future README rewrite could re-introduce a v0.11-tense leak that the lint silently misses. Expansion preempts. Reverse-criterion: if the expanded scope yields > 5 false-positives on the first run (current `8ab5d54` README is the most likely failure case), STOP and re-tune scope per §0.4 STOP rule #6 — most likely response is adding word-bounded exclusions for "current version" patterns inside the lint, not removing files from scope. |
| Item K: current-version README exclusion | **NO — `README.md` is NOT excluded** even though it currently describes v0.11.0 as the current version. | README.md is the project's living-document landing page; it SHOULD be in past-tense relative to the current code state (e.g., "v0.11 added MELAMINE_FOAM"), not present-tense ("v0.11 adds MELAMINE_FOAM" — which becomes false the day v0.12 ships). The asymmetry that excludes the current-version `RELEASE_NOTES_v0.X.0.md` does NOT apply to README.md because README.md is not a snapshot. Reverse-criterion: if the v0.11.0 README content forces > 3 noqa markers on first lint expansion, refactor the README to past-tense in the same v0.12 commit (still inside 800-line envelope). |
| Item L: OQ-13f reverse-criterion firing | **Re-examine; planner-locked default = NO firing at v0.12.** The lab PASS-gate `assert rel_err < 0.20` and lab `_LAB_EXPECTED.rel_err_max = +0.20` are NOT soft FAIL gates because: (i) they were planned at v0.11 §2.4 sub-branch A as PASS-gate boundaries, NOT artefacts of a disagreement-record regime; (ii) at v0.11 ship time the recorded value was rel_err = +2.40 % (3.7σ inside the +20 % boundary), giving substantial margin; (iii) the structural-sign assertion is the redundant guard, not the primary gate (`_LAB_EXPECTED.rel_err_min/max` is the primary). v0.12 bookkeeping: record the re-examination outcome in commit body + OQ-13f resolution-annotation; NO test change. | OQ-13f resolution at v0.11 explicitly wrote "structural-sign assertion may become soft FAIL gate" as the reverse-criterion; v0.12 must NOT silently ignore that trigger. The re-examination outcome (NO firing) is itself the v0.12 audit-trail action. Reverse-criterion: if a v0.12+ critic argues the re-examination decision is wrong (e.g., margin between +2.40 % and +20.0 % is too large — the assertion is too loose to detect a genuine regression of, say, +18 %), tighten the band in v0.13+ under a successor ADR. |
| Item D: mirror-image-source comparator choice | **Eyring** (already in-repo predictor; `roomestim/predict/eyring.py`; D9 / ADR 0009; runtime invariant `eyring ≤ sabine + 1e-9` already enforced). NOT a full mirror-image-source method (would require new library code beyond v0.12 envelope). | The OQ-13b resolution-candidate explicitly named "mirror-image source method OR ray tracing" as the comparator, but mirror-image-source for non-rectangular rooms is a several-day library effort. Eyring is a strict-inequality on Sabine for the same α-table (Eyring's `−S·ln(1-ᾱ)` term diverges from Sabine's `Σαᵢ·Sᵢ` for high ᾱ); the ratio `sabine_RT60 / eyring_RT60` is a known diagnostic for "Sabine-approximation effect" (> 1.15 indicates the room is too absorbent for the Sabine assumption). Reverse-criterion: if Eyring ratio is in the ambiguous [1.10, 1.15] zone for conference, defer D's ADR 0021 finding to v0.13+ and either (a) wait for a more discriminating comparator (mirror-image-source) OR (b) accept the ambiguity and document — DO NOT force a binary classification. |
| Item D: study scope (rooms covered) | **Conference room only** (single-room characterising study). NOT lab (lab is at PASS-gate; the study is about un-closed disagreement). NOT ACE corpus rooms (those have multiple known confounders — coupled-space exclusion, paper-not-publishing-materials caveat; the SoundCam conference room is the cleanest glass-heavy un-closed case). | Minimum-leverage. Conference rel_err = -22.7 % is the single un-closed v0.11 signature (`sabine_shoebox_underestimates_glass_wall_specular`); expanding to multi-room is v0.13+ scope. Reverse-criterion: if conference ratio is inconclusive (in the [1.10, 1.15] ambiguous zone), expand to 1 ACE corpus glass-heavy room (Office_1 has ~5 m² window; could provide a confirming/disconfirming second data point) — still within v0.12 envelope. |
| Item D: comparator value source | **Compute live at test time** from `roomestim/predict/eyring.py::predict_eyring_rt60` + the existing conference fixture (`tests/fixtures/soundcam_synthesized/conference/dims.yaml`). Store the computed ratio in a test-time variable; record in commit body + ADR 0021 §Decision. NOT pre-computed and pinned in a snapshot — the ratio is a derived characterisation, not a regression target. | This avoids fabricated-quote risk (per ADR 0018 §Drivers item 2 / D22 audit-trail precedent). The ratio MUST be derived from in-repo predictors + in-repo fixtures, not from external sources. Reverse-criterion: if Eyring predictor diverges silently from its v0.11 baseline (e.g., a v0.12 floating-point regression), STOP and escalate — Eyring monotonicity invariant (`eyring ≤ sabine + 1e-9`, D9 / ADR 0009) is part of the gate. |
| Item D: classification thresholds | **Sabine-approximation effect**: Eyring ratio > 1.15. **Coefficient-sourcing issue**: Eyring ratio < 1.10. **Ambiguous**: 1.10 ≤ ratio ≤ 1.15 → document and defer. | The OQ-13b resolution-candidate verbatim. Reverse-criterion: if a v0.12+ critic argues 1.15 is too loose or 1.10 is too tight, revise under a successor ADR 0022. |
| Item D: predictor adoption action | If Sabine-approximation effect → **DO NOT switch default predictor to Eyring** at v0.12; record the finding in ADR 0021; create NEW OQ-15 "Should v0.13+ ship Eyring or Millington-Sette as default for glass-heavy rooms?" with explicit acceptance criteria. v0.12 changes ONLY the documentation + the conference disagreement-record `signature` (from `sabine_shoebox_underestimates_glass_wall_specular` to `sabine_shoebox_approximation_for_glass_heavy_room`). If coefficient-sourcing issue → record in ADR 0021 + OQ-15 NEW frames as "v0.13+ coefficient-sourcing audit for glass". If ambiguous → record + OQ-15 NEW frames as "v0.13+ comparator upgrade (mirror-image-source) required". | The honesty principle from D22 + v0.10 cycle: characterise first, decide second. v0.12 is the characterising release; v0.13+ is the decision release. Reverse-criterion: if v0.12 critic argues this is unnecessary fence-sitting (i.e., if the ratio is unambiguous, the decision should land in v0.12 too), the planner reconsiders — but the v0.12 envelope explicitly does NOT include predictor-default change (that would require library logic mutation outside the §0.3 LOCKED-OUT envelope). |
| Item D: ADR 0021 or §Status-update? | **ADR 0021 NEW.** The conference disagreement-record signature changes; this is a STRUCTURAL change (not factual), and per D22 structural changes use a NEW ADR rather than §Status-update-in-place. Original ADR 0018 §Drivers framing of conference signature is preserved byte-equal; ADR 0021 reframes the signature with cross-link. | The D22 rule: structural changes → ADR supersedure or NEW ADR; factual errors → §Status-update. The conference signature change is structural (it reframes the disagreement-record causal model from "Sabine-shoebox + glass-wall specular" to "Sabine-approximation regime"). Reverse-criterion: if executor §2.4 lands ratio in the [1.10, 1.15] ambiguous zone, ADR 0021 lands with "characterising-only; signature unchanged at v0.12" framing — NO signature change, NO library change. |
| Schema marker | **`__schema_version__` stays `"0.1-draft"`** | Stage-2 schema flip is bound to A10b in-situ capture per ADR 0016 §Reverse-criterion + ADR 0018 §Reverse-criterion. v0.12 does not include A10b capture (OQ-12a unchanged on capture commitment); marker MUST NOT re-flip. Item D characterising study does not satisfy ADR 0016 §Reverse-criterion (which requires substitute disagreement to RESOLVE on the same predictor / adapter path AND ≥ 3 captures per D2 + ADR 0018 §References tightening). Reverse-criterion: unchanged from v0.11 §0.1. |
| Schema-marker side-channel | **NO `__schema_version__` change at v0.12.** Adapter writes still emit `version: "0.1-draft"`. | See above; binding to ADR 0016 §Reverse-criterion. |
| Test count target | **128 default-lane** (= 124 at v0.11 + 4 net: see §0.2.B). The CI command is `pytest -m "not lab" -v`; full collection at v0.11 is 125, becomes 129 at v0.12; the `not lab` filter still subtracts 1 lab-marked test in `test_acceptance_lab_room.py`. | Concrete acceptance line: `pytest -m "not lab" --collect-only -q tests \| tail -1` reads `128 tests collected` (or `128/129 tests collected` if any other deselection appears). Reverse: if pytest count is 127 or 129 (off-by-one), STOP and re-plan; the test counter is the canonical gate. |
| Version bump | **`0.11.0` → `0.12.0`** in `pyproject.toml` + `roomestim/__init__.py` (`__version__` only). | SemVer minor bump (new characterising study + new ADR + lint scope expansion). `__schema_version__` UNCHANGED at `"0.1-draft"`. |
| ADR list | **ADR 0021 NEW (conference glass-heavy Sabine-shoebox residual study).** ADR 0019 gains a §Status-update-2026-05-12 block ONLY IF verbatim Vorländer lookup shifts the α₅₀₀ value (planner-locked default = §Status-update lands; v0.11 envelope mid-value 0.85 is verbatim-confirmed at planner-pre-implementation envelope confidence ~50 %, see §2.2.2 envelope discussion). Per D22 hybrid pattern (factual: §Status-update; structural: new ADR), the ADR 0019 update is a §Status-update because it is a citation-completion (factual), not a structural rationale change. ADR 0020 byte-equal. ADR 0016/0017/0018 byte-equal. | ADR 0021 captures the conference characterising-study finding (Sabine-approximation effect vs coefficient-sourcing classification per §0.1 thresholds). ADR 0019 §Status-update closes the verbatim-citation pending flag. Reverse: if Vorländer lookup returns a value outside [0.80, 0.95] → ADR 0021 absorbs the citation + coefficient revision (single ADR; bundle); if inside [0.80, 0.95] → §Status-update on ADR 0019 + ADR 0021 lands as conference-study only. |
| D-decision additions | **D26 NEW** (predictor-adoption deferral policy: characterise first, decide second; codifies that v0.12 ships the OQ-13b characterising study WITHOUT switching default predictor). Optional **D27** (OQ-13a verbatim closure pattern: codifies that v0.11-style "planner-locked envelope + verbatim-pending" patterns SHIP at the next minor release as §Status-update; planner-locked default for INCLUDE based on v0.11 § precedent + ADR 0019 §References pending-flag.) | Two decisions because two distinct policy threads: (D26) characterise-first-decide-second for predictor adoption; (D27) verbatim-pending → next-release-status-update for honesty-flag closure cadence. Reverse: if at code-review the two threads collapse to one, fold to D26 only (which subsumes D27 as a special case). |
| Living_room re-introduction | **NO**, byte-equal removed at v0.10. | ADR 0018 §Reverse-criterion requires authoritative dims; v0.12 paper retrieval (if any) is bounded to MELAMINE_FOAM verbatim citation (item C) + conference Eyring ratio (item D), NOT living_room dim re-retrieval. Unchanged from v0.11 §0.1. Reverse: deferred to v0.13+ under OQ-13e (live-mesh extraction) precursor — unchanged. |
| Cross-repo PR status | **UNCHANGED (WITHDRAWN)**. | ADR 0018 §Reverse-criterion: re-submission requires lab + conference at A11 PASS AND ≥ 3 captures (D2). v0.12 does not change capture count (still 2 substitute rooms + 0 in-situ); conference is still in disagreement-record (item D characterises it but does NOT close it). PR stays WITHDRAWN. Reverse: if at v0.12 ship time conference returns to ±20 % AND ≥ 3 captures land — neither expected at v0.12 — separate v0.13+ PR re-submission cycle. Unchanged from v0.11 §0.1. |
| AVOIDED items | **Item A** (A10b actual capture): user-physical-access blocking; NOT planner-schedulable; OQ-12a unchanged. **Item B** (FIBERGLASS_CEILING + TILE_FLOOR additions): not load-bearing — no room currently requires them; OQ-14 unchanged. **Item E** (cross-repo PR re-submission): conference still in disagreement-record + only 2 substitute rooms; unchanged. **Item F** (live-mesh extraction): too large for v0.12 envelope (3-5 days, requires SoundCam PLY download access); OQ-13e unchanged. **Item G** (mypy strict): OQ-13i unchanged. **Item H** (AnyRIR watchlist): passive; unchanged. **Item I** (ARKitScenes): unchanged. **Item J** (OQ-11 Lecture_2 ratification): unchanged — requires independent evidence. | See §1 scope decision matrix for per-item rationale. Reverse-criteria are recorded per-item in §1. |

### §0.2 File-op table

#### §0.2.A File ops summary (13 ops)

| # | Path | Op | Lines added | Lines removed | Lines net | Purpose |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `docs/adr/0019-melamine-foam-enum-addition.md` | MOD | ~15 | ~3 | +12 | Add `## §Status-update-2026-05-12 (v0.12.0) — verbatim Vorländer citation` block after §References. Records the verbatim page + row + panel-thickness column citation. If verbatim value differs from 0.85 within [0.80, 0.95] envelope: update §Decision item line "MaterialAbsorption[MaterialLabel.MELAMINE_FOAM] = 0.85" → new value, with the v0.11 envelope-mid-value rationale preserved in §Status-update body. Per D22 hybrid (factual; in-line + §Status-update). |
| 2 | `roomestim/model.py` | MOD | ~1 | ~1 | 0 | ONLY if verbatim value differs from 0.85 inside [0.80, 0.95]: update `MaterialAbsorption[MaterialLabel.MELAMINE_FOAM] = <new>` (one line). If verbatim value = 0.85 exactly: BYTE-EQUAL (no change). NOTE: band row stays byte-equal in either case (band-2 = 0.85 invariant is preserved by construction); IF band-2 must change (because verbatim α₅₀₀ ≠ 0.85), executor MUST also update band row [2] (and re-verify monotonicity sanity test). Planner-locked default: verbatim ≈ 0.85 (~50 % likely; planner envelope-mid-value matches typical "melamine foam panel 2-inch" published values); if differs, the most likely value is 0.90 (4-inch panel; ~30 % likely). |
| 3 | `tests/test_a11_soundcam_rt60.py` | MOD | ~10 | ~3 | +7 | (a) `_predict_conference()` body: byte-equal. (b) Conference test body gains 1 NEW assertion + 2 NEW computed values: `eyring_predicted` (from `roomestim.predict.eyring.predict_eyring_rt60`); `sabine_eyring_ratio = sabine_predicted / eyring_predicted`; assertion `assert sabine_eyring_ratio > 1.0` (Eyring monotonicity guard; redundant with D9 / ADR 0009 invariant but explicit at the conference-room level). (c) `_CONFERENCE_EXPECTED` dict: byte-equal on prediction/measured/rel_err fields; gains 1 NEW field `disagreement_classification` storing the §2.4 executor decision-point output ("sabine_approximation_effect" / "coefficient_sourcing_issue" / "ambiguous"). (d) Update module docstring to reflect ADR 0021 + the §2.4 decision. |
| 4 | `tests/test_a11_soundcam_rt60.py` (continued — NEW companion test) | MOD | ~25 | 0 | +25 | NEW test function `test_a11_soundcam_conference_eyring_ratio_characterises` records the Eyring ratio for conference and asserts it lies in [1.0, 2.0] (Eyring is always ≤ Sabine for same α-table per D9; ratio of 1.0 = no Sabine-approximation effect, ratio of 2.0 = extreme high-absorption regime). NEW companion test `test_a11_soundcam_conference_disagreement_classification` asserts `_CONFERENCE_EXPECTED["disagreement_classification"] in {"sabine_approximation_effect", "coefficient_sourcing_issue", "ambiguous"}` and records the executor's §2.4 decision. |
| 5 | `tests/test_room_acoustics_octave.py` | MOD | ~2 | 0 | +2 | ONLY if verbatim α₅₀₀ ≠ 0.85: update `test_melamine_foam_band_a500_matches_legacy_scalar` expected value (band-2 = new value). If verbatim = 0.85: BYTE-EQUAL. NEW companion test `test_melamine_foam_a500_verbatim_citation_complete` asserts the §Status-update-2026-05-12 block has landed in `docs/adr/0019-melamine-foam-enum-addition.md` — uses subprocess `grep` like `test_lint_tense.py`. |
| 6 | `tests/test_misc_soft_furniture_budget.py` | MOD | ~1 | ~1 | 0 | ONLY if verbatim α₅₀₀ ≠ 0.85: update `test_melamine_foam_a500_in_expected_range` envelope still [0.80, 0.95] (NO change to envelope; verbatim must land inside it). If verbatim is OUTSIDE [0.80, 0.95]: STOP and escalate (§0.4 STOP rule #5; planner-response is ADR 0021 + envelope-revision). BYTE-EQUAL if verbatim = 0.85. |
| 7 | `scripts/lint_tense.py` | MOD | ~12 | ~4 | +8 | Expand scope to add `docs/perf_verification_*.md`, `docs/architecture.md`, `README.md`. Pattern + block-exclusion + per-line escape semantics unchanged. Live-repo first-run on expanded scope MUST flag ≤ 5 files (§0.4 STOP rule #6); if > 5, executor either (a) rewrites the flagged lines, or (b) escalates. Planner pre-implementation envelope: README.md `8ab5d54` body uses present-tense "v0.11.0 currently ships" framing in ~3 places (intentional; current-version landing page) — those land as noqa-suppressed at v0.12 with rationale "current-version landing page; not version-specific framing". |
| 8 | `tests/test_lint_tense.py` | MOD | ~5 | 0 | +5 | NEW test function `test_lint_tense_scope_includes_expanded_files` asserts that `scripts/lint_tense.py`'s file-discovery hits at least one file from `docs/perf_verification_*.md` + `docs/architecture.md` + `README.md`. Existing `test_no_present_tense_version_specific_framing` byte-equal. |
| 9 | `docs/adr/0021-conference-sabine-shoebox-residual-study.md` | NEW | ~120 | 0 | +120 | NEW ADR 0021 — conference glass-heavy Sabine-shoebox residual characterising study. §Decision = study outcome (one of: sabine_approximation_effect / coefficient_sourcing_issue / ambiguous). §Drivers = OQ-13b unchanged; conference -22.7 % residual at v0.10/v0.11. §Alternatives considered = mirror-image-source (deferred per §0.1; library cost > envelope), ray-tracing (deferred per §0.1; not in-repo), multi-room expansion (deferred per §0.1; conference-only at v0.12). §Why chosen = Eyring is in-repo + cheap; ratio-based classification (1.15 / 1.10 thresholds) is honest characterisation, not decision. §Consequences = (depending on §2.4 outcome) conference signature reframes / OQ-15 NEW raised / library defaults UNCHANGED at v0.12. §Reverse-criterion = if Eyring ratio shifts > 0.05 at v0.13+, re-run; if mirror-image-source becomes feasible, supersede with ADR 0022. §References = ADR 0009 (Eyring), ADR 0018 (substitute-disagreement record), ADR 0019 (v0.11 enum precedent), D9 / D26 NEW. |
| 10 | `.omc/plans/open-questions.md` | MOD | ~30 | ~5 | +25 | OQ-13a §Status-update-2026-05-12 appended (verbatim Vorländer citation closed; envelope-mid-value 0.85 confirmed OR revised per executor §2.4 outcome). OQ-13b status-update appended (conference characterising study landed at v0.12; result: <one of three classifications>). OQ-13f re-examination outcome recorded (NO firing at v0.12; structural-sign assertion is not soft FAIL gate; rationale per §0.1). OQ-13h reverse-criterion ANNOTATION (lint scope expanded at v0.12; v0.12 first-run flag count + noqa count recorded). OQ-15 NEW (per §0.1 D26 policy: predictor-adoption decision deferred to v0.13+ pending mirror-image-source comparator or further characterising data). All other OQs reaffirmed UNCHANGED. |
| 11 | `.omc/plans/decisions.md` | MOD | ~60 | 0 | +60 | D26 NEW (predictor-adoption deferral: characterise first, decide second). D27 NEW (verbatim-pending → next-release-status-update closure cadence). D14..D25 byte-equal under v0.12. |
| 12 | `pyproject.toml` | MOD | 1 | 1 | 0 | `version = "0.11.0"` → `version = "0.12.0"`. |
| 13 | `roomestim/__init__.py` | MOD | 1 | 1 | 0 | `__version__ = "0.11.0"` → `__version__ = "0.12.0"`. `__schema_version__` UNCHANGED `"0.1-draft"`. |
| 14 | `RELEASE_NOTES_v0.12.0.md` | NEW | ~110 | 0 | +110 | NEW release notes — §What v0.12.0 ships / §What stays the same / §Default-lane test count [124 → 128] / §ADR list / §What stays deferred / §Tag-local-only / §References. |

**Total expected diff envelope**: ~393 lines added, ~18 lines removed → net **~+375 lines**.

Breakdown:
- **Library + tests + script**: ~57 lines (rows 1-8).
- **Docs (1 ADR + ADR 0019 status-update)**: ~135 lines (rows 1, 9).
- **Plan + OQ + decisions + RELEASE_NOTES + version**: ~201 lines (rows 10-14).

If diff exceeds **600 added lines** or **40 removed lines**, STOP and
re-plan (scope creep — common bloat sources: ADR 0021 §Alternatives
ballooning, mirror-image-source library code drift, lint-scope
expansion creating > 10 noqa markers needing rationale comments).

(File-op table is numbered 1-14 but described as "13 ops" because
rows 12-13-14 are paired: pyproject + `__init__.py` are a single
version-bump operation, and `RELEASE_NOTES_v0.12.0.md` is the standard
release-notes companion to each release-bump commit.)

#### §0.2.B Test count target

| Stage | Default-lane count (`pytest -m "not lab"`) | Full collection | Delta |
| --- | --- | --- | --- |
| v0.11.0 baseline | **124** | 125 | (anchor) |
| v0.12.0 target | **128** | 129 | +4 default-lane / +4 full |

Net +4 additions:

1. `tests/test_a11_soundcam_rt60.py::test_a11_soundcam_conference_eyring_ratio_characterises` — Eyring ratio characterisation (item D).
2. `tests/test_a11_soundcam_rt60.py::test_a11_soundcam_conference_disagreement_classification` — classification record (item D).
3. `tests/test_room_acoustics_octave.py::test_melamine_foam_a500_verbatim_citation_complete` — ADR 0019 §Status-update presence guard (item C).
4. `tests/test_lint_tense.py::test_lint_tense_scope_includes_expanded_files` — lint scope expansion guard (item K).

**Item L** (OQ-13f re-examination) is bookkeeping; NO new test.

Concrete acceptance line (executor verification, §2.10):

```bash
/home/seung/miniforge3/bin/python -m pytest -m "not lab" --collect-only -q tests 2>&1 | tail -3
# expect: "128 tests collected" (or "128/129 tests collected (1 deselected)")
```

### §0.3 Files NOT touched (LOCKED OUT — byte-equal under v0.11.0 baseline `eee3014`)

| Family | Specific paths | Verification |
| --- | --- | --- |
| Library defaults (other than the conditional 1-line `MaterialAbsorption[MELAMINE_FOAM]` row update IF verbatim Vorländer ≠ 0.85) | `MISC_SOFT` row byte-equal (α₅₀₀=0.40), all other 8 existing `MaterialLabel` entries + their `MaterialAbsorption{,Bands}` rows byte-equal, `_FURNITURE_BY_ROOM` sum=276, `_PIECE_EQUIVALENT_ABSORPTION_*` byte-equal, `MaterialLabel` enum still has exactly 10 entries (no new additions; OQ-14 unchanged). | `git diff --unified=0 eee3014 -- roomestim/model.py` shows at most 1 changed line (MELAMINE_FOAM α₅₀₀ row), 0 if verbatim = 0.85. NO new enum entries. |
| Schema marker | `__schema_version__ == "0.1-draft"` UNCHANGED | `roomestim/__init__.py:4` byte-equal except for `__version__` line. |
| Stage-2 strict schema file | `proto/room_schema.json` byte-equal. | `git diff --stat eee3014 -- proto/` empty. |
| Stage-1 draft schema file | `proto/room_schema.draft.json` byte-equal. | Same as above. |
| ADR 0019 §Decision body (other than §Status-update-2026-05-12 append) | §Decision / §Drivers / §Alternatives / §Why chosen / §Consequences / §Reverse-criterion / §§2.4 executor decision-point record / §References sections BODY byte-equal. ONLY the §Status-update block is appended. | `grep -c "## §Status-update-" docs/adr/0019-melamine-foam-enum-addition.md` returns 1 (the v0.12 block). Body diff stat shows only addition lines. |
| ADR 0020 | byte-equal under v0.12 — even though item K expands the LINT SCOPE, the policy ADR's mechanism / scope / pattern fields are recorded at the abstraction-level "fixture README + ADR + RELEASE_NOTES" and "perf docs / architecture / README" — both fall under "high-signal file families known to carry version-specific honesty leaks". The scope-expansion is an implementation refinement, NOT an architectural change. | Per D22: factual changes → §Status-update; structural → new ADR. Lint-scope expansion is operational refinement → DOES land a §Status-update-2026-05-12 in ADR 0020 to record the expansion. Re-evaluating: this is a §Status-update at v0.12 (factual: scope list grows), NOT byte-equal. **REVISION**: `docs/adr/0020-ci-lint-tense-policy.md` MOD with §Status-update-2026-05-12 block appending the expanded scope list. (Row count: +8 to row 7 in §0.2.A → adjust §0.2.A row 7 to also include `docs/adr/0020-ci-lint-tense-policy.md` MOD ~8 lines.) **EXECUTOR NOTE**: Update §0.2.A by adding row 7b at executor time. Total adjusted: ~+8 lines, no removed lines, file-op count 13 → 14 (the ADR 0020 §Status-update). |
| ADR 0016/0017/0018 | byte-equal — none of items C/K/L/D touch ADR 0016 (in-situ vs substitute architecture), ADR 0017 (A10-layout non-substitutable), or ADR 0018 (substitute-disagreement record). | `git diff --stat eee3014 -- docs/adr/ \| grep -vE "0019\|0020\|0021"` empty. |
| ADR 0001..0015 | byte-equal | `git diff --stat eee3014 -- docs/adr/ \| grep -E "000[1-9]\|001[0-5]"` empty. |
| Test files (other than the 4 modified + 1 NEW listed in §0.2.A rows 3-8) | `test_acceptance_lab_room.py`, `test_ace_geometry_audit.py`, `test_adapter_*`, `test_cli_*`, `test_coords_*`, `test_e2e_ace_challenge_rt60.py`, `test_engine_roundtrip.py`, `test_export_*`, `test_lecture_2_ceiling_seat_bracket.py`, `test_no_external_writes.py`, `test_per_band_mae_ex_bl_snapshot.py`, `test_placement_*`, `test_schema_octave_band_compat.py`, `test_schema_stage2_validates.py`, `test_a10a_soundcam_corner.py` | `git diff --stat eee3014 -- tests/ \| grep -vE "test_a11_soundcam_rt60.py\|test_misc_soft_furniture_budget.py\|test_room_acoustics_octave.py\|test_lint_tense.py"` empty. |
| Fixture data files | `tests/fixtures/soundcam_synthesized/{lab/dims.yaml,lab/GT_corners.json,lab/rt60.csv,conference/dims.yaml,conference/GT_corners.json,conference/rt60.csv,LICENSE_MIT.txt,README.md}` byte-equal | NO fixture changes at v0.12 (items C/K/L/D do not modify any fixture). |
| RELEASE_NOTES (predecessor files) | `RELEASE_NOTES_v0.1.1.md` ~ `RELEASE_NOTES_v0.11.0.md` (11 files) | All byte-equal. v0.12 ships its own NEW `RELEASE_NOTES_v0.12.0.md`. |
| Perf docs | `docs/perf_verification_*.md` (all 7 files) | All byte-equal. Perf-doc updates are deferred to v0.13+ (item D characterising study is recorded in ADR 0021, NOT in a perf doc — v0.12 envelope discipline). |
| Cross-repo PR research | `.omc/research/cross-repo-pr-v0.9-proposal.md`, `.omc/research/cross-repo-pr-v0.10-deferred.md` | Both byte-equal — PR stays WITHDRAWN. |
| In-situ protocol DOC | `docs/protocol_a10b_insitu_capture.md` byte-equal | v0.12 does not edit this; OQ-12a unchanged on capture commitment. |
| Existing plan files | `.omc/plans/v0.{1..11}-*.md`, `.omc/plans/v0.10.1-patch.md`, `.omc/plans/ralplan-iter1-resolutions.md`, `.omc/plans/roomestim-v0-design.md` | All byte-equal. v0.12 ships only `.omc/plans/v0.12-design.md` (this file) + edits to `.omc/plans/open-questions.md` + `.omc/plans/decisions.md`. |
| Notepad / state / wiki | `.omc/notepad.md`, `.omc/state/`, `.omc/wiki/` | Untouched (planner does not modify these). |
| Predictor library (Eyring) | `roomestim/predict/eyring.py` byte-equal | Item D USES Eyring as a comparator at test time but does NOT modify the predictor. |
| CLI / adapters | `roomestim/cli.py`, `roomestim/adapters/*` byte-equal | No CLI / adapter changes at v0.12. |

### §0.4 Scope-creep STOP rules (re-plan triggers)

The executor MUST STOP and consult the planner if ANY of the following
fires during v0.12 implementation. Each STOP rule maps to a specific
v0.12 invariant:

| # | STOP trigger | Reason |
| --- | --- | --- |
| 1 | Diff envelope exceeds **600 added lines** OR **40 removed lines** | Scope creep; the v0.12 envelope is 300-500 added lines. ADR 0021 §Alternatives ballooning, lint scope expansion noqa-cleanup growing, characterising study expanding to multi-room are common sources. |
| 2 | Default-lane test count is **NOT 128** after edits | v0.12 specifies +4 net; over/under is a regression OR an undeclared addition. |
| 3 | `__schema_version__` becomes anything other than `"0.1-draft"` | Stage-2 schema flip is bound to A10b in-situ capture (ADR 0016 §Reverse-criterion); v0.12 has no capture. |
| 4 | `MaterialLabel` enum gains any new entry | OQ-14 unchanged at v0.12; FIBERGLASS_CEILING + TILE_FLOOR deferred to v0.13+. |
| 5 | Verbatim Vorländer α₅₀₀ lands OUTSIDE [0.80, 0.95] invariant envelope | Per §0.1 row "Item C: coefficient value choice"; planner-response is fixture re-map, NOT envelope widening. |
| 6 | CI lint expanded-scope first run flags > **5 files** | Per §0.1 row "Item K: lint scope expansion target"; > 5 means noqa-cleanup is too large for v0.12 envelope. |
| 7 | Conference Eyring ratio lies OUTSIDE [1.0, 2.0] | Eyring monotonicity invariant (`eyring ≤ sabine + 1e-9`, D9 / ADR 0009) is broken; STOP and escalate to predictor regression investigation. |
| 8 | Any file in `roomestim/place/*`, `roomestim/cli.py`, `roomestim/adapters/*`, `roomestim/predict/eyring.py`, `roomestim/predict/sabine.py` is edited | Scope creep — v0.12 is documentation + study + test + lint-scope + version-bump only. No predictor / CLI / adapter logic changes. |
| 9 | Any file in `proto/` is edited | Schema-marker stays "0.1-draft"; proto files are byte-equal. |
| 10 | Cross-repo PR research files are edited | PR re-submission is v0.13+ scope under D11 + ADR 0018 §References tightening. |
| 11 | ADR 0018 is edited (§Drivers / §Decision / §References / etc.) | v0.12 conference signature reframe lands in ADR 0021 (new ADR; structural change per D22), NOT in ADR 0018. Editing ADR 0018 violates D22. |
| 12 | More than 1 NEW OQ is raised (other than OQ-15 predictor-adoption from §0.1 Item D policy) | Scope discipline — defer extra OQs to v0.13+. |
| 13 | ADR 0021 exceeds **180 lines** | The planner-locked depth is ~120 lines; if it grows beyond 180, the §Alternatives / §Consequences sections are over-elaborating. Trim. |
| 14 | A second NEW ADR (beyond ADR 0021) is added (e.g., a separate "lint scope expansion" ADR) | Scope discipline — lint scope expansion is operationally captured via ADR 0020 §Status-update, NOT a new ADR. |

---

## §1 Scope decision matrix

This matrix records every v0.12 candidate (from the parent prompt's
12-item pool plus implicit derived items) with `v0.12? / defer-to /
rationale` columns. Mirrors `.omc/plans/v0.11-design.md` §1 structure.

| # | Candidate | v0.12? | Defer to | Rationale |
| --- | --- | --- | --- | --- |
| A | A10b actual in-situ capture (user-physical-access required) | NO | user-volunteer-only (OQ-12a unchanged) | User-physical-access blocking; not planner-schedulable. v0.11 already shipped protocol DOC. |
| B | FIBERGLASS_CEILING + TILE_FLOOR enum additions | NO | v0.13+ (OQ-14 unchanged) | Not load-bearing at v0.12 — no captured room currently requires these enums. Pre-empting an unobserved need is scope creep. Reverse: if v0.13+ A10b capture lands and uses fiberglass/tile, immediately reconsider. |
| **C** | OQ-13a Vorländer α₅₀₀ verbatim citation closure (PENDING → verbatim) | **YES** | — | Highest-visibility v0.11 honesty-flag (ADR 0019 §References pending status). Bookkeeping pattern matches v0.11 §Status-update precedent on ADR 0018. Low risk (envelope-invariant test brackets [0.80, 0.95]). |
| **D** | OQ-13b conference glass-heavy Sabine-shoebox residual characterising study | **YES** | — | Closes a v0.11-still-OPEN disagreement signature. Hi-leverage: opens path to ADR 0022 predictor-adoption decision (or invalidates that path via coefficient-sourcing classification). Uses in-repo Eyring (no library cost). |
| E | Cross-repo PR re-submission to spatial_engine | NO | v0.13+ (OQ-13c unchanged) | Stays WITHDRAWN per ADR 0018 §Reverse-criterion until ≥ 3 captures (D2) + both substitute rooms at A11 PASS. v0.12 satisfies neither: conference is still in disagreement-record (item D characterises but does not close); capture count unchanged. |
| F | OQ-13e live-mesh extraction from SoundCam PLY meshes | NO | v0.13+ | 3-5 days; requires SoundCam mesh download access; out of v0.12 envelope (which has ~400-line envelope, F alone is several hundred lines). |
| G | OQ-13i mypy strict project commitment | NO | v0.13+ unchanged | Project policy decision deferred; v0.12 has higher-leverage items. |
| H | OQ-12b AnyRIR watchlist | NO | (passive; no v0.12 action) | Status unchanged; promotion criteria not met. |
| I | OQ-12c ARKitScenes scoping | NO | v0.13+ unchanged | Disk budget + license still open. |
| J | OQ-11 Lecture_2 ratification 3-condition gate | NO | (requires independent evidence) | Status unchanged from v0.8. |
| **K** | OQ-13h reverse: CI lint scope expansion (perf docs + architecture + README) | **YES** | — | Bundle item; near-zero cost. Pre-empts v0.11 README leak (current README content is acceptable but future rewrites could re-introduce). Lint-scope expansion lands operationally via §Status-update on ADR 0020, no new ADR. |
| **L** | OQ-13f reverse-criterion re-examination (is `assert rel_err < 0.20` a soft FAIL gate?) | **YES (bookkeeping; no test change)** | — | Bundle item; OQ-13f resolution at v0.11 explicitly named this as the reverse trigger. Re-examination outcome at v0.12 is "NO firing" per §0.1; recorded in OQ-13f resolution-annotation. |
| Y1 | ADR 0021 NEW (conference characterising-study finding) | **YES** | — | Required for item D. Structural-change ADR per D22 (conference signature reframes). |
| Y2 | ADR 0019 §Status-update-2026-05-12 (verbatim citation closure) | **YES** | — | Required for item C. Factual-change §Status-update per D22 hybrid pattern. |
| Y3 | ADR 0020 §Status-update-2026-05-12 (lint scope expansion record) | **YES** | — | Required for item K. Operational-refinement §Status-update per D22; lint-scope is an implementation detail of the policy ADR 0020 records. |
| Y4 | ADR 0022 (predictor-adoption decision — switch to Eyring or Millington-Sette for glass-heavy rooms) | NO | v0.13+ if applicable | Per §0.1 D26 policy: characterise first, decide second. v0.12 ships the characterising study (ADR 0021); v0.13+ ships the decision (ADR 0022) iff appropriate. |
| Y5 | D26 NEW (predictor-adoption deferral: characterise first, decide second) | **YES** | — | Codifies the v0.12 policy explicit in item D scope. |
| Y6 | D27 NEW (verbatim-pending → next-release-status-update closure cadence) | **YES** | — | Codifies the v0.11→v0.12 honesty-flag closure pattern. |
| Y7 | OQ-15 NEW (predictor-adoption decision deferred to v0.13+; classification from §2.4 informs framing) | **YES** | — | Captures the deferred decision per D26 policy. Status: OPEN; resolution candidate depends on item D classification outcome. |
| Y8 | Living_room re-introduction | NO | v0.13+ (OQ-13e precursor) | Unchanged from v0.11. |
| Y9 | A10a corner test reframing | NO | (already smoke-test at v0.10) | Unchanged. |
| Y10 | F4a / Lecture_2 ratification | NO | OQ-11 unchanged | Independent evidence still required. |
| Y11 | Schema marker re-flip | NO | (bound to ADR 0016 §Reverse-criterion) | Unchanged. |
| Y12 | Conference fixture flip (new enum or coefficient retune) | NO | v0.13+ | Item D is characterising-only; no fixture / coefficient changes at v0.12. |
| Y13 | Lab fixture / test changes | NO | (lab at PASS-gate; v0.11 settled) | Unchanged. |
| Y14 | New roomestim/predict/* library code (mirror-image-source or Millington-Sette) | NO | v0.13+ | Out of v0.12 envelope. Eyring (existing) is the v0.12 comparator. |
| Y15 | Multi-room residual study (add ACE rooms) | NO | v0.13+ | Minimum-leverage at v0.12; conference-only. Reverse: if conference ratio is ambiguous (in [1.10, 1.15]), expand to 1 ACE glass-heavy room within v0.12 envelope. |
| Y16 | RELEASE_NOTES_v0.12.0.md NEW | **YES** | — | Standard release-notes companion. |
| Y17 | OQ-13a marked `[x]` resolved (already `[x]` at v0.11) | NO change | — | Already resolved at v0.11; v0.12 adds §Status-update annotation only. |
| Y18 | OQ-13b marked `[x]` resolved at v0.12 | **YES (conditional)** | — | Marked `[x]` if §2.4 classification is unambiguous (Sabine-approximation effect OR coefficient-sourcing issue). If ambiguous, remains `[ ]` with v0.12 §Status-update. |
| Y19 | OQ-13f marked `[x]` resolved (already `[x]` at v0.11) | NO change | — | Already resolved at v0.11; v0.12 adds re-examination annotation only (no firing). |
| Y20 | OQ-13h marked `[x]` resolved (already `[x]` at v0.11) | NO change | — | Already resolved at v0.11; v0.12 adds scope-expansion annotation only. |
| Y21 | Version bump `0.11.0` → `0.12.0` | **YES** | — | SemVer minor bump per §0.1. |
| Y22 | Mypy strict cleanup of `ace_challenge.py:554-556` | NO | v0.13+ (OQ-13i unchanged) | Unchanged. |
| Y23 | Schema file edits | NO | (never in v0.12) | Hard-locked out by §0.3 + §0.4 STOP rule #9. |
| Y24 | Library predictor / CLI / adapter changes (any) | NO | (never in v0.12) | Hard-locked out by §0.3 + §0.4 STOP rule #8. |
| Y25 | Test logic changes outside the 4 modified + 1 NEW file in §0.2.A | NO | (never in v0.12) | Hard-locked out by §0.3 + §0.4 STOP rule #2 (test count discipline). |

---

## §2 Per-OQ resolution plan

### §2.1 Pre-flight

**Goal**: Confirm working tree is clean and at v0.11.0 baseline `eee3014`.

```bash
cd /home/seung/mmhoa/roomestim
git status --porcelain  # MUST be empty
git log --oneline -1     # MUST start with 8ab5d54 (the README sync commit) OR re-base to eee3014
git rev-parse HEAD       # if 8ab5d54, that is the post-v0.11 state; v0.12 may branch from here
/home/seung/miniforge3/bin/python -m pytest -m "not lab" --collect-only -q tests 2>&1 | tail -3
# expect: "124 tests collected"
/home/seung/miniforge3/bin/ruff check . 2>&1 | tail -3
# expect: "All checks passed!"
/home/seung/miniforge3/bin/python -m mypy roomestim 2>&1 | tail -3
# expect: pre-existing baseline (3 errors in ace_challenge.py:554-556 per OQ-13i; v0.12 does NOT fix)
python scripts/lint_tense.py 2>&1 | tail -3
# expect: exit 0 on the current 3-family scope (fixture README + ADR + RELEASE_NOTES)
```

**Note on baseline commit**: `eee3014` is the v0.11.0 release commit; the
working tree at planner-consultation time is `8ab5d54` which adds the
weekly progress report + README sync. v0.12 branches from `8ab5d54` (the
latest origin/main commit); all "byte-equal to v0.11 baseline" comparisons
in this plan use `eee3014` as the baseline because v0.11 was the last
substantive release. Doc-only commits (`4562b3e`, `8ab5d54`) are
implicitly carried forward.

**Exit gate**: All 4 commands return expected. If any fails, STOP and
escalate.

### §2.2 OQ-13a — Vorländer verbatim citation closure (→ ADR 0019 §Status-update-2026-05-12)

#### §2.2.1 Acceptance criteria

1. ADR 0019 gains a `## §Status-update-2026-05-12 (v0.12.0) — verbatim Vorländer citation` block after §References.
2. The block records: (a) the exact Vorländer 2020 §11 / Appx A page number; (b) the table row identifier (e.g., "Material 42, 'melamine foam panel'"); (c) the panel-thickness column (e.g., "2-inch panel" or "4-inch panel"); (d) the verbatim α₅₀₀ value.
3. If verbatim value is **inside [0.80, 0.95] envelope**: `roomestim/model.py` `MaterialAbsorption[MaterialLabel.MELAMINE_FOAM]` row is updated IF AND ONLY IF the value differs from 0.85 (otherwise BYTE-EQUAL). Band row [2] updated symmetrically IF α₅₀₀ row changes. Lab A11 prediction re-runs; if rel_err leaves [-0.20, +0.20] PASS-gate, STOP and escalate (§0.4 STOP rule #5+#7).
4. If verbatim value is **outside [0.80, 0.95] envelope**: STOP and escalate; planner-response is fixture re-map OR envelope-revision in successor ADR 0022 (NOT silent ratchet).
5. NEW test `test_melamine_foam_a500_verbatim_citation_complete` asserts the §Status-update block exists (subprocess `grep` on ADR file).
6. ADR 0019 §Decision line cites the verbatim source (replacing v0.11's "verbatim page + row + panel-thickness column PENDING" annotation with the verbatim citation).

#### §2.2.2 Verbatim lookup procedure

The executor performs Vorländer 2020 *Auralization* (2nd ed., Springer) §11 / Appendix A lookup. The book is the canonical OQ-13a primary source per ADR 0019 §References and OQ-2/OQ-6 precedent. The executor reads the "melamine foam panel" / "acoustic foam absorber" row and lifts the verbatim α₅₀₀ value at the published panel-thickness column. If multiple panel thicknesses are tabulated (typical: 2-inch, 4-inch), the executor lifts the value that matches the SoundCam lab installation as described in arXiv:2311.03517v2 §A.1 (if SoundCam paper specifies a thickness) OR the geometric mean if SoundCam paper does not specify (planner-locked default: geometric mean of the available thicknesses, with the assumption documented in §Status-update body).

**Planner pre-implementation envelope** (NOT a verified figure, for executor sanity-check): published melamine foam panel α₅₀₀ values typically lie in [0.80, 0.95] across 2-inch to 4-inch thicknesses. The v0.11 value 0.85 is at the geometric mean of [0.80, 0.95] = 0.872; the planner ships 0.85 at envelope-mid-value with ~50 % probability of verbatim agreement.

#### §2.2.3 File-op map (this section's slice)

| Path | Op | Lines (added / net) |
| --- | --- | --- |
| `docs/adr/0019-melamine-foam-enum-addition.md` | MOD | +12 (§Status-update-2026-05-12 block + §Decision line annotation removal) |
| `roomestim/model.py` | MOD | 0 (BYTE-EQUAL if verbatim = 0.85) OR +1/-1 (if differs inside envelope) |
| `tests/test_room_acoustics_octave.py` | MOD | +2 (NEW citation-complete test); +1/-1 IF band-2 must update symmetrically |
| `tests/test_misc_soft_furniture_budget.py` | MOD | 0 (envelope test unchanged; verbatim must land inside [0.80, 0.95]) |
| `tests/test_a11_soundcam_rt60.py` | MOD | 0 conditional (lab `_LAB_EXPECTED` re-run; BYTE-EQUAL if rel_err stays inside PASS-gate; STOP-and-escalate if leaves) |

#### §2.2.4 Gating tests

| Test | Type | Acceptance |
| --- | --- | --- |
| `test_a11_soundcam_lab_band_record` (existing — body byte-equal at v0.12) | default-lane | Passes; lab rel_err stays inside [-0.20, +0.20] PASS-gate. |
| `test_a11_soundcam_lab_pass_gate_recovered` (existing — body byte-equal) | default-lane | Passes; lab rel_err stays inside PASS-gate. |
| `test_melamine_foam_a500_in_expected_range` (existing) | default-lane | Passes; envelope [0.80, 0.95] unchanged at v0.12. |
| `test_melamine_foam_band_a500_matches_legacy_scalar` (existing) | default-lane | Passes for new verbatim value (band-2 = α₅₀₀). |
| `test_melamine_foam_a500_verbatim_citation_complete` (NEW) | default-lane | Passes; asserts §Status-update-2026-05-12 block present in ADR 0019. |

#### §2.2.5 Reverse-criterion

If verbatim Vorländer α₅₀₀ value is OUTSIDE [0.80, 0.95] envelope, STOP and escalate. Planner response: open ADR 0022 NEW that documents (i) the verbatim value; (ii) the rationale for the envelope-revision (the v0.11 invariant test guards against silent drift, so a verbatim-driven revision must land transparently); (iii) the lab A11 re-run with the new value (potentially leaving PASS-gate); (iv) any required fixture re-map.

If lab A11 leaves PASS-gate under the new coefficient: planner response is fixture re-map (partial-foam walls + partial-drywall) per ADR 0019 §Reverse-criterion item 1. This is a v0.12 escalation, NOT a v0.13+ defer.

### §2.3 OQ-13b — Conference Sabine-shoebox residual study (→ ADR 0021 NEW)

#### §2.3.1 Acceptance criteria

1. `tests/test_a11_soundcam_rt60.py` `_predict_conference()` test path computes both Sabine (existing) and Eyring (NEW; via `roomestim.predict.eyring.predict_eyring_rt60`) on the conference fixture.
2. Ratio `sabine_eyring_ratio = sabine_predicted / eyring_predicted` is computed and recorded.
3. Ratio is classified per §0.1 thresholds: > 1.15 = Sabine-approximation effect; < 1.10 = coefficient-sourcing issue; [1.10, 1.15] = ambiguous.
4. NEW test `test_a11_soundcam_conference_eyring_ratio_characterises` records the ratio (`assert 1.0 < ratio < 2.0` for Eyring monotonicity guard).
5. NEW test `test_a11_soundcam_conference_disagreement_classification` records the classification string in `_CONFERENCE_EXPECTED["disagreement_classification"]`.
6. ADR 0021 NEW lands with the classification + study outcome (per §2.4 decision-point).

#### §2.3.2 §2.4 executor decision-point

This is the single empirical decision the executor makes at v0.12. Procedure:

1. Apply edits §2.3 to test file (adding Eyring computation + ratio calculation) without yet writing the classification field to `_CONFERENCE_EXPECTED`.
2. Run conference test with `-s` flag: `pytest tests/test_a11_soundcam_rt60.py::test_a11_soundcam_conference_disagreement_record -v -s` and capture printed sabine_predicted, eyring_predicted, ratio.
3. Decision tree:
   - If `ratio > 1.15`: classification = `"sabine_approximation_effect"`. Conference signature in `_CONFERENCE_EXPECTED["disagreement_signature"]` updates from `sabine_shoebox_underestimates_glass_wall_specular` → `sabine_approximation_regime_for_glass_heavy_room`. ADR 0021 §Decision = Sabine-approximation effect. OQ-15 NEW raised: "Should v0.13+ ship Eyring or Millington-Sette as default for glass-heavy rooms?"
   - If `ratio < 1.10`: classification = `"coefficient_sourcing_issue"`. Conference signature unchanged (Sabine-shoebox + glass-wall still names the geometric/material side). ADR 0021 §Decision = coefficient-sourcing issue. OQ-15 NEW raised: "v0.13+ coefficient-sourcing audit for glass: per-band glass α from Vorländer 2020 §11 / Appx A verbatim".
   - If `1.10 ≤ ratio ≤ 1.15`: classification = `"ambiguous"`. Conference signature unchanged. ADR 0021 §Decision = ambiguous; mirror-image-source comparator required for further discrimination. OQ-15 NEW raised: "v0.13+ comparator upgrade (mirror-image-source) for non-rectangular room characterising".
4. If `ratio < 1.0` (i.e., Eyring > Sabine): STOP and escalate (Eyring monotonicity invariant `eyring ≤ sabine + 1e-9` is broken; means predictor regression elsewhere).
5. Apply edits to `_CONFERENCE_EXPECTED["disagreement_classification"]` + ADR 0021 §Decision with the locked classification.
6. Re-run full default-lane: `pytest -m "not lab" -q tests` MUST pass at 128 tests.

**Planner pre-implementation envelope** (NOT a verified figure): conference room dims (from `tests/fixtures/soundcam_synthesized/conference/dims.yaml`) per v0.10 honesty correction: ~10.5 m × ~5.4 m × ~3.0 m → V ≈ 170 m³. Paper-faithful map: walls drywall + glass partial; ceiling acoustic-tile; floor carpet. Mean α ≈ 0.25-0.35. Sabine 500 Hz ≈ 0.45 s. Eyring at mean ᾱ ≈ 0.30 over total S ≈ 230 m² → Eyring RT60 ≈ −0.161·170/(230·ln(0.70)) ≈ 0.161·170/82 ≈ 0.33 s. Predicted ratio Sabine/Eyring ≈ 0.45/0.33 ≈ **1.36** → would put conference in **Sabine-approximation effect** classification at envelope confidence. This is a planner pre-implementation envelope, NOT a verified figure. The executor MUST run the actual test and let the empirical result drive classification.

#### §2.3.3 File-op map (this section's slice)

| Path | Op | Lines (added / net) |
| --- | --- | --- |
| `tests/test_a11_soundcam_rt60.py` | MOD | +32 (Eyring computation + ratio + classification field + 2 NEW companion tests) |
| `docs/adr/0021-conference-sabine-shoebox-residual-study.md` | NEW | +120 |

#### §2.3.4 ADR list (this section)

- **ADR 0021 NEW** (`docs/adr/0021-conference-sabine-shoebox-residual-study.md`): conference glass-heavy Sabine-shoebox residual characterising study. Captures (i) the classification from §2.4 (one of three); (ii) the Eyring comparator rationale; (iii) the §Why-not-mirror-image-source (envelope cost) + §Why-not-multi-room (minimum-leverage); (iv) §Consequences (depending on classification: conference signature reframes OR unchanged; OQ-15 NEW raised; library defaults UNCHANGED at v0.12); (v) §Reverse-criterion. Cross-refs: ADR 0009 (Eyring; D9), ADR 0018 (substitute-disagreement record), ADR 0019 (v0.11 enum precedent), D26 NEW (predictor-adoption deferral), D27 NEW (verbatim-pending closure cadence), OQ-13b (resolved-with-classification or status-update if ambiguous), OQ-15 NEW.

#### §2.3.5 Reverse-criterion

- If Eyring ratio shifts > 0.05 at v0.13+ (e.g., a v0.13 predictor refactor moves the conference ratio from 1.36 to 1.41 or 1.31), re-run ADR 0021 study and record the shift in a §Status-update.
- If mirror-image-source comparator becomes feasible at v0.13+, supersede ADR 0021 with ADR 0022 (different comparator → different classification thresholds; mirror-image is more discriminating than Eyring).
- If conference fixture map is revised (e.g., per OQ-15 coefficient-sourcing-issue branch ships a glass per-band coefficient revision), re-run ADR 0021 study under the new map; document in v0.13+ release.

### §2.4 OQ-13h reverse — CI lint scope expansion (→ ADR 0020 §Status-update-2026-05-12)

#### §2.4.1 Acceptance criteria

1. `scripts/lint_tense.py` scope list expands to add: `docs/perf_verification_*.md`, `docs/architecture.md`, `README.md`.
2. ADR 0020 gains a `## §Status-update-2026-05-12 (v0.12.0) — lint scope expansion` block after §References. Records the 3 new file families + the rationale (preemptive guard).
3. First-run lint on expanded scope: ≤ 5 files flagged (§0.4 STOP rule #6); each flagged file either (a) rewritten in past tense (preferred) or (b) suppressed via `# noqa: lint-tense` end-of-line marker with rationale.
4. NEW test `test_lint_tense_scope_includes_expanded_files` asserts the expanded scope finds at least 1 file from each of the 3 new families when invoked.
5. README.md is NOT excluded (per §0.1 row "Item K: current-version README exclusion"); current-version landing-page framing lands as noqa-suppressed if present.

#### §2.4.2 File-op map (this section's slice)

| Path | Op | Lines (added / net) |
| --- | --- | --- |
| `scripts/lint_tense.py` | MOD | +8 (3 glob patterns added to scope list) |
| `docs/adr/0020-ci-lint-tense-policy.md` | MOD | +8 (§Status-update-2026-05-12 block) |
| `tests/test_lint_tense.py` | MOD | +5 (NEW scope-expansion test) |
| `docs/perf_verification_*.md`, `docs/architecture.md`, `README.md` | MOD (conditional) | 0 if clean; +X if noqa suppression required (planner envelope: 3 noqa markers expected in README; 0 expected in perf docs + architecture) |

#### §2.4.3 Gating tests

| Test | Type | Acceptance |
| --- | --- | --- |
| `test_no_present_tense_version_specific_framing` (existing) | default-lane | Passes; lint exits 0 on expanded scope at v0.12 ship time. |
| `test_lint_tense_scope_includes_expanded_files` (NEW) | default-lane | Passes; expanded scope finds files in all 3 new families. |
| GH Actions step `Lint (tense)` | CI | Passes on expanded scope. |

#### §2.4.4 Reverse-criterion

- If expanded-scope first run flags > 5 files, STOP per §0.4 STOP rule #6. Planner response: tune scope per-file (e.g., exclude `docs/perf_verification_2026-05-04.md` if it carries v0.4-tense framing that is genuinely v0.4-archival, not version-specific framing).
- If GH Actions step becomes flaky on expanded scope at v0.13+, switch to allow-list-based suppression per ADR 0020 §Reverse-criterion.

### §2.5 OQ-13f reverse — re-examination (bookkeeping only)

#### §2.5.1 Acceptance criteria

1. Re-examination outcome recorded in `.omc/plans/open-questions.md` OQ-13f resolution-annotation: "v0.12 re-examination — NO firing; rationale per `.omc/plans/v0.12-design.md` §0.1 Item L row".
2. No test changes; no library changes; no fixture changes.
3. Conference `assert rel_err < -0.10` structural-sign assertion UNCHANGED (still in place; v0.12 confirms it is not a soft FAIL gate because conference rel_err is empirically -22.7 % at v0.11 with margin of ≈ 0.13 — far from the -0.10 boundary).
4. Lab `assert rel_err < 0.20` PASS-gate structural-sign assertion UNCHANGED (still in place; v0.12 confirms it is not a soft FAIL gate because lab rel_err is empirically +2.40 % at v0.11 with margin of ≈ 0.18 — substantial).

#### §2.5.2 File-op map (this section's slice)

| Path | Op | Lines (added / net) |
| --- | --- | --- |
| `.omc/plans/open-questions.md` | MOD | +5 (OQ-13f resolution-annotation at v0.12) |

#### §2.5.3 Gating tests

- **None** (bookkeeping-only).

#### §2.5.4 Reverse-criterion

- If a v0.12+ critic argues margin is too large (e.g., suggests tightening lab to `assert rel_err < 0.10`), defer the tightening to v0.13+ under a successor ADR; v0.12 only records the re-examination outcome.

### §2.6 D-decision additions (D26 + D27)

#### §2.6.1 D26 — Predictor-adoption deferral policy (characterise first, decide second)

- **Date**: 2026-05-12
- **Author**: oh-my-claudecode:planner (v0.12 design pass).
- **Decision**: When a residual study (e.g., OQ-13b conference Sabine-shoebox residual) classifies a disagreement as predictor-approximation-effect, the v0.12+ release that ships the study does NOT also switch the default predictor. The decision to switch defaults is deferred to a successor release after the characterising data has been confirmed by ≥ 1 independent comparator (e.g., mirror-image-source) AND the disagreement signature is robust across ≥ 2 rooms. Concretely for v0.12 / OQ-13b: ADR 0021 records the classification (Sabine-approximation effect, coefficient-sourcing issue, or ambiguous), but `roomestim/predict/sabine.py` remains the default predictor; OQ-15 NEW records the deferred decision.
- **Drivers**: v0.9-v0.10 honesty cycle taught: predictor change without independent verification is silent claim-shifting. ADR 0009 (D9) recorded Eyring as parallel predictor (not default) specifically for this reason. v0.12 extends the pattern.
- **Reverse-criterion**: if v0.13+ ships a Millington-Sette or ray-tracing predictor library that conclusively disambiguates the conference signature AND a second glass-heavy room confirms the signature, the predictor-default switch may land in v0.13+ under ADR 0022.

#### §2.6.2 D27 — Verbatim-pending closure cadence

- **Date**: 2026-05-12
- **Author**: oh-my-claudecode:planner (v0.12 design pass).
- **Decision**: When v0.X ships a library coefficient with explicit "verbatim citation pending" flag (per v0.11 ADR 0019 §References pattern + D22 honesty-first), the next minor release (v0.X+1) MUST close the pending flag via §Status-update on the original ADR (D22 hybrid: factual change). The closure either (a) confirms the planner-envelope mid-value at verbatim or (b) revises the value within the invariant envelope or (c) escalates with a successor ADR if outside envelope. Concretely for v0.12 / ADR 0019: §Status-update-2026-05-12 lands at v0.12 ship time.
- **Drivers**: pending flags accumulate honesty debt; OQ-13a status at v0.11 ship explicitly designated v0.12 as the closure point. D27 codifies the pattern so future enum / coefficient additions follow the same cadence.
- **Reverse-criterion**: if the verbatim source is access-limited (e.g., Vorländer 2020 OOP / inter-library loan delay) AND the planner-envelope was tight (e.g., ≤ ±5 % range), the closure may slip to v0.X+2 with explicit re-deferral §Status-update at v0.X+1.

#### §2.6.3 File-op map (this section's slice)

| Path | Op | Lines (added / net) |
| --- | --- | --- |
| `.omc/plans/decisions.md` | MOD | +60 (D26 NEW + D27 NEW; D14..D25 byte-equal) |

### §2.7 Cumulative file-op pre-flight

All file ops are independent at the directory level except for `tests/test_a11_soundcam_rt60.py` which is touched by §2.2 (conditional, if α₅₀₀ verbatim ≠ 0.85 forces lab re-run) AND §2.3 (item D Eyring computation + companions). The executor MUST apply §2.2 + §2.3 edits in a single `Edit` session per file to avoid re-write conflicts.

---

## §3 ADR list for v0.12

| ADR | Status | NEW/AMENDED | Purpose | Cross-refs |
| --- | --- | --- | --- | --- |
| 0021 | Accepted (v0.12.0) | **NEW** | Conference glass-heavy Sabine-shoebox residual characterising study. Captures classification per §2.4 (Sabine-approximation effect / coefficient-sourcing issue / ambiguous) + Eyring comparator rationale + reverse-criterion. | ADR 0009 (Eyring), ADR 0018 (substitute-disagreement record), ADR 0019 (v0.11 enum precedent), D9, D26 NEW, D27 NEW, OQ-13b (resolved-with-classification or status-update), OQ-15 NEW. |
| 0019 | Amended via §Status-update-2026-05-12 (v0.12.0) | AMENDED in-place (D22 hybrid pattern; factual: verbatim citation) | Records verbatim Vorländer 2020 §11 / Appx A citation; optionally updates `MaterialAbsorption[MELAMINE_FOAM]` row IF verbatim ≠ 0.85 inside [0.80, 0.95] envelope. | OQ-13a (annotated as v0.12-closed); D27 NEW (closure cadence). |
| 0020 | Amended via §Status-update-2026-05-12 (v0.12.0) | AMENDED in-place (D22 hybrid pattern; operational refinement: scope expansion) | Records lint-scope expansion to include perf docs + architecture + README. | OQ-13h reverse-criterion (annotated as v0.12-fired); D24 (byte-equal core policy). |
| 0016/0017/0018 | byte-equal (v0.10.0 + v0.10.1 §Status-update + v0.11.0 byte-equal) | unchanged at v0.12 | (no §Status-update at v0.12; v0.12 does not re-edit any of these). | — |
| 0001..0015 | byte-equal | unchanged | — | — |

**v0.12 ADR-amend invariant**: v0.12 ships ADR 0021 as NEW; ADR 0019 + ADR 0020 each gain a single §Status-update-2026-05-12 block (D22 hybrid pattern); ADR 0001..0018 (except 0019 / 0020 status-updates) byte-equal.

(Per D22 codification: factual errors / completions → in-line / §Status-update; structural errors → ADR supersedure. v0.12 has 1 factual completion (ADR 0019 verbatim citation), 1 operational refinement (ADR 0020 scope expansion), 1 structural new finding (ADR 0021 NEW). No supersedures.)

---

## §4 Test plan (expanded for DELIBERATE mode)

### §4.1 New tests at v0.12

| # | Test | File | Type | Category |
| --- | --- | --- | --- | --- |
| 1 | `test_a11_soundcam_conference_eyring_ratio_characterises` | `tests/test_a11_soundcam_rt60.py` | integration (Eyring monotonicity guard + ratio recording) | gating for ADR 0021 |
| 2 | `test_a11_soundcam_conference_disagreement_classification` | `tests/test_a11_soundcam_rt60.py` | integration (classification record per §2.4) | gating for ADR 0021 |
| 3 | `test_melamine_foam_a500_verbatim_citation_complete` | `tests/test_room_acoustics_octave.py` | integration (ADR §Status-update presence; subprocess grep) | gating for ADR 0019 §Status-update |
| 4 | `test_lint_tense_scope_includes_expanded_files` | `tests/test_lint_tense.py` | integration (scope expansion verification) | gating for ADR 0020 §Status-update |

### §4.2 Modified tests at v0.12

| # | Test | File | Modification | Rationale |
| --- | --- | --- | --- | --- |
| 1 | `test_a11_soundcam_conference_disagreement_record` (existing) | `tests/test_a11_soundcam_rt60.py` | Body gains Eyring computation + ratio + classification field write. Conference rel_err prediction logic byte-equal. | OQ-13b resolution (item D). |
| 2 | `test_melamine_foam_band_a500_matches_legacy_scalar` (existing) | `tests/test_room_acoustics_octave.py` | Body BYTE-EQUAL if verbatim α₅₀₀ = 0.85; otherwise band-2 expected value updates symmetrically. | OQ-13a verbatim closure (item C). |
| 3 | `test_a11_soundcam_lab_band_record` + `test_a11_soundcam_lab_pass_gate_recovered` (existing) | `tests/test_a11_soundcam_rt60.py` | BYTE-EQUAL if verbatim α₅₀₀ = 0.85; if differs inside envelope, predictions re-run; if rel_err leaves PASS-gate → STOP and escalate (§0.4 STOP rule #5+#7). | OQ-13a verbatim closure (item C). |

### §4.3 Test count check

| Stage | Default-lane count | Full collection | Delta |
| --- | --- | --- | --- |
| v0.11.0 baseline | 124 | 125 | (anchor) |
| v0.12.0 target | **128** | 129 | +4 |

Concrete acceptance line (executor verification, §2.10):

```bash
/home/seung/miniforge3/bin/python -m pytest -m "not lab" --collect-only -q tests 2>&1 | tail -3
# expect: "128 tests collected" (or equivalent "X/Y tests collected (Z deselected)" with X = 128)
```

Per-section count expectations:

- `test_a11_soundcam_rt60.py`: 3 → 5 (+2: eyring_ratio + classification).
- `test_room_acoustics_octave.py`: 18 → 19 (+1: verbatim_citation_complete).
- `test_lint_tense.py`: 1 → 2 (+1: scope_includes_expanded_files).
- All other files: byte-equal counts.

Total: 124 + 2 + 1 + 1 = **128**. Matches §0.2.B.

### §4.4 Existing test invariants at v0.12

| Test | Invariant |
| --- | --- |
| `test_band_a500_matches_legacy_scalar` (existing umbrella) | Passes for ALL 10 enum entries (including MELAMINE_FOAM at verbatim α₅₀₀). |
| `test_a10a_soundcam_corner_*` | byte-equal pass for lab + conference (2 rooms). |
| `test_lecture_2_ceiling_seat_bracket` | byte-equal. |
| `test_e2e_ace_challenge_rt60` (ACE corpus) | byte-equal pass — v0.12 does NOT change ACE. |
| `test_per_band_mae_ex_bl_snapshot` | byte-equal. |
| `test_acceptance_lab_room` (lab-marked) | unchanged; still deselected by `pytest -m "not lab"`. |
| `test_schema_stage2_validates` | byte-equal — `__schema_version__` stays `"0.1-draft"`. |
| `test_misc_soft_furniture_budget` (existing 17 tests) | byte-equal pass on the MISC_SOFT-specific assertions; MELAMINE_FOAM envelope test unchanged. |
| `test_no_external_writes` | byte-equal. |
| `test_no_present_tense_version_specific_framing` | passes at v0.12 ship time on EXPANDED scope (5 file families). |

### §4.5 Lint + type-check invariants

| Check | Command | v0.12 acceptance |
| --- | --- | --- |
| ruff | `/home/seung/miniforge3/bin/ruff check .` | `All checks passed!` |
| mypy | `/home/seung/miniforge3/bin/python -m mypy roomestim` | pre-existing baseline (3 errors in `ace_challenge.py:554-556`; v0.12 does NOT fix; OQ-13i unchanged). |
| tense lint | `python scripts/lint_tense.py` | exit code 0 on EXPANDED 5-family scope. |

### §4.6 Observability

| Signal | Source | Observation |
| --- | --- | --- |
| Verbatim α₅₀₀ value | `git diff` on `roomestim/model.py` + ADR 0019 §Status-update | executor records verbatim page + row + panel-thickness column + value; commit body. |
| Conference Eyring ratio | pytest -s capture | executor records sabine_predicted, eyring_predicted, ratio; commit body + ADR 0021 §Decision. |
| Classification | `_CONFERENCE_EXPECTED["disagreement_classification"]` | executor records ("sabine_approximation_effect" / "coefficient_sourcing_issue" / "ambiguous"); commit body. |
| Lint expanded-scope flag count | `python scripts/lint_tense.py` first run output | executor records (expected ≤ 3 noqa markers; §0.4 STOP rule #6 = > 5). |
| Diff envelope | `git diff --stat eee3014 \| tail -1` | executor records (expected ~393 added / ~18 removed; §0.4 STOP rule #1 = > 600 added). |

---

## §5 Pre-mortem (≥ 5 ways v0.12 could ship a regression or honesty leak)

DELIBERATE-mode requirement. Each pre-mortem scenario specifies a hard-exit
grep guard (with §Status-update block exclusion per D22).

### §5.1 Verbatim Vorländer α₅₀₀ silently retuned to keep lab at PASS-gate

**Scenario**: Executor performs Vorländer 2020 lookup; verbatim value is 0.90 (inside envelope). Re-running lab A11 under α₅₀₀ = 0.90 + MELAMINE_FOAM walls: predicted ≈ 0.115 s, measured 0.158 s → rel_err = -27 % (negative overshoot; LEAVES PASS-gate). Instead of escalating (§0.4 STOP rule #5/#7), executor silently keeps α₅₀₀ = 0.85 in `roomestim/model.py` while citing the 0.90 verbatim in ADR 0019 §Status-update — claiming verbatim closure while not actually using verbatim. v0.13+ critic flags as new honesty leak (matches the v0.9 "placeholder pretending to be measured" pattern).

**Hard-exit guard**:

```bash
# 1. roomestim/model.py MELAMINE_FOAM α₅₀₀ value MUST match ADR 0019 §Status-update verbatim citation
grep -nE "MELAMINE_FOAM.*0\.[89][0-9]" roomestim/model.py
# capture: numeric value M
grep -nE "MELAMINE_FOAM.*0\.[89][0-9]|verbatim.*α₅₀₀.*0\.[89][0-9]" docs/adr/0019-melamine-foam-enum-addition.md
# capture: numeric value V
# M MUST equal V; if not, STOP

# 2. Lab A11 re-run logged in commit body OR ADR 0019 §Status-update if α₅₀₀ changes
grep -nE "predicted = 0\.[0-9]+|rel_err = [+-][0-9]" docs/adr/0019-melamine-foam-enum-addition.md
# expect: if α₅₀₀ changed at v0.12, ≥ 1 match recording the lab re-run

# 3. NEW citation-complete test MUST hard-assert the verbatim value match
grep -nE "MaterialAbsorption\[.*MELAMINE_FOAM.*\] ==" tests/test_room_acoustics_octave.py
# OR
grep -nE "α₅₀₀.*verbatim" tests/test_misc_soft_furniture_budget.py
# expect: at least one test directly asserts the value matches a constant defined in the test
```

**Mitigation**: ADR 0019 §Status-update-2026-05-12 cites the verbatim value EXPLICITLY at one location AND `roomestim/model.py` MELAMINE_FOAM row matches that value byte-equal. The NEW test `test_melamine_foam_a500_verbatim_citation_complete` cross-checks the ADR has the §Status-update block (presence only at v0.12; a stronger value-equality assertion can land in v0.13+). §0.4 STOP rule #5 forces escalation if value lands outside envelope; §0.4 STOP rule #7 forces escalation if lab re-run leaves PASS-gate (executor MUST update `_LAB_EXPECTED` or escalate to ADR 0022).

### §5.2 Conference Eyring ratio computed using wrong predictor signature

**Scenario**: Executor uses `roomestim.predict.eyring.predict_eyring_rt60` but mis-passes the conference fixture (e.g., uses Sabine α-table instead of Eyring's ᾱ = (Σαᵢ·Sᵢ)/(ΣSᵢ) reduction; this is a common pitfall because both predictors share the per-surface α table). Resulting ratio is artefactually 1.0 (Eyring = Sabine because executor accidentally fed both the same input). Classification incorrectly lands "ambiguous" → v0.12 ships a meaningless characterising study.

**Hard-exit guard**:

```bash
# 1. Eyring monotonicity invariant test MUST pass at v0.12
/home/seung/miniforge3/bin/python -m pytest tests/test_eyring_monotonicity.py -v 2>&1 | tail -5
# expect: passes (the runtime invariant `eyring ≤ sabine + 1e-9` holds per D9 / ADR 0009)

# 2. Conference ratio test MUST assert `ratio > 1.0` strict (NOT ratio >= 1.0; a ratio of exactly 1.0 is suspicious)
grep -nE "sabine_eyring_ratio.*> 1\.0|assert.*ratio > 1\.0" tests/test_a11_soundcam_rt60.py
# expect: ≥ 1 match — the test asserts strict inequality

# 3. ADR 0021 §Decision MUST cite both sabine_predicted AND eyring_predicted values, not just the ratio
grep -nE "sabine_predicted.*0\.[0-9]+|eyring_predicted.*0\.[0-9]+" docs/adr/0021-conference-sabine-shoebox-residual-study.md
# expect: ≥ 2 matches recording both predictor values
```

**Mitigation**: Conference test asserts `sabine_eyring_ratio > 1.0` strict (not `≥ 1.0`). ADR 0021 §Decision records both predictor values, not just the derived ratio. Eyring monotonicity invariant test (already in v0.4+ baseline; runtime assertion) ensures the predictor outputs are consistent.

### §5.3 ADR 0021 lands a structural decision that contradicts D22

**Scenario**: Conference signature reframe from `sabine_shoebox_underestimates_glass_wall_specular` → `sabine_approximation_regime_for_glass_heavy_room` is a STRUCTURAL change to ADR 0018 §Drivers item-2 framing (the causal model shifts). D22 says structural changes → ADR supersedure (new ADR + `§Status: superseded` on the original). Executor reframes in ADR 0021 NEW but does NOT mark ADR 0018 as superseded → audit-trail inconsistency.

**Hard-exit guard**:

```bash
# 1. If conference signature reframes (sabine_approximation_effect classification taken), ADR 0018 MUST gain a § cross-link OR §Status: amended-by ADR 0021
grep -nE "amended by ADR 0021|cross-link to ADR 0021|superseded by ADR 0021" docs/adr/0018-soundcam-substitute-disagreement-record.md
# expect: ≥ 1 match IF and ONLY IF item D classification = sabine_approximation_effect
# expect: 0 matches IF classification = coefficient_sourcing_issue or ambiguous

# 2. ADR 0021 §Cross-refs MUST cite ADR 0018 explicitly
grep -nE "ADR 0018" docs/adr/0021-conference-sabine-shoebox-residual-study.md
# expect: ≥ 1 match

# 3. Conference disagreement_signature in _CONFERENCE_EXPECTED MUST match ADR 0021 §Decision
grep -nE "disagreement_signature.*=.*\"" tests/test_a11_soundcam_rt60.py
# capture: signature string S_test
grep -nE "disagreement_signature.*:.*\"" docs/adr/0021-conference-sabine-shoebox-residual-study.md
# capture: signature string S_adr
# S_test MUST equal S_adr; if not, STOP
```

**Mitigation**: ADR 0021 §Cross-refs explicitly names ADR 0018 with the relationship qualifier ("amended-by" if signature reframes; "informs" if classification is ambiguous or coefficient-sourcing). The conference disagreement_signature string in test fixture matches the ADR 0021 §Decision verbatim. STOP rule #11 forbids ADR 0018 edits at v0.12 — the cross-link lands as a NEW LINE in ADR 0018 §References (which is a factual addition, not a structural rewrite; D22 permits this as a §Status-update if needed, but for v0.12 the cross-link can be one-line append-without-§Status-update since the §References section is canonically open-ended).

**REVISION**: per STOP rule #11 strict reading, ADR 0018 is byte-equal at v0.12. The cross-link from ADR 0018 → ADR 0021 lands at v0.13+ if needed; ADR 0021 has its own ADR 0018 cross-link. This is acceptable because ADR-cross-refs are bidirectional but not byte-symmetric (ADR 0021 references ADR 0018; ADR 0018's reference to ADR 0021 is implicit via chronological order). Update §0.4 STOP rule #11 reading: ADR 0018 byte-equal is correct; no cross-link addition needed.

### §5.4 Lint scope expansion creates noqa proliferation

**Scenario**: Expanded scope (perf docs + architecture + README) flags 8 files at first run (README.md alone has present-tense framing in multiple sections describing what v0.11.0 added). Executor suppresses all 8 with `# noqa: lint-tense` markers and rationale comments — but the rationale comments themselves become near-identical boilerplate ("current-version landing page" × 8). v0.13+ reviewer reads the suppressions as a smell ("if 8 lines need the same suppression, the lint is mis-tuned"). v0.12 ships an artefactually-passing lint.

**Hard-exit guard**:

```bash
# 1. v0.12 first-run noqa count MUST be ≤ 5 per §0.4 STOP rule #6
grep -cE "# noqa: lint-tense" README.md docs/perf_verification_*.md docs/architecture.md
# expect: sum ≤ 5

# 2. README.md noqa markers (if any) MUST cite distinct rationales (not boilerplate)
grep -nE "# noqa: lint-tense.*current-version landing page" README.md
# if > 3 matches: the rationale is boilerplate — STOP and rewrite README to past-tense

# 3. ADR 0020 §Status-update-2026-05-12 MUST record the v0.12 first-run flag count
grep -nE "v0\.12 first run.*[0-9] files flagged|expanded scope.*[0-9] noqa" docs/adr/0020-ci-lint-tense-policy.md
# expect: ≥ 1 match recording the empirical count
```

**Mitigation**: §0.4 STOP rule #6 hard-caps first-run flag count at 5. ADR 0020 §Status-update records the empirical flag count. If README hits > 3 noqa markers with boilerplate rationale, the planner-response is rewrite README to past-tense ("v0.11 added MELAMINE_FOAM" → no noqa needed; current-version framing is replaced with past-tense+date).

### §5.5 D26 deferral policy abused as silent decision-shift

**Scenario**: D26 "characterise first, decide second" is meant as procedural discipline. At v0.13+ a downstream consumer (e.g., spatial_engine reverb integration) starts depending on Sabine being default; v0.13+ executor accidentally invokes D26 to defer the predictor-switch INDEFINITELY ("we'll characterise more later") and never lands ADR 0022. Conference disagreement-record stays open for ≥ 3 releases — same "wait for in-situ A10" deadlock pattern that v0.9 was specifically engineered to avoid.

**Hard-exit guard**:

```bash
# 1. D26 body MUST specify a concrete reverse-criterion with a numerical timeline
grep -nE "reverse[- ]criterion.*v0\.1[3-5]|within [1-9] releases|by v0\.1[3-9]" .omc/plans/decisions.md
# expect: ≥ 1 match in D26 body — concrete numerical timeline OR concrete additional-evidence trigger

# 2. OQ-15 (raised at v0.12) MUST have explicit resolution-candidate that names v0.13+ as the closure release
grep -nE "OQ-15.*v0\.1[3-5]|predictor.*decision.*v0\.1[3-5]" .omc/plans/open-questions.md
# expect: ≥ 1 match

# 3. D26 reverse-criterion MUST name the consumer-driven escalation path (spatial_engine integration request)
grep -nE "spatial_engine|downstream consumer" .omc/plans/decisions.md
# expect: ≥ 1 match in D26 body
```

**Mitigation**: D26 body explicitly names a 2-release deferral window (v0.12 characterises; v0.13+ decides). OQ-15 explicitly names v0.13+ as the closure release. D26 §Reverse-criterion names spatial_engine integration request as the consumer-driven escalation path.

### §5.6 v0.12 silently regresses MELAMINE_FOAM coefficient via verbatim "update"

**Scenario**: Executor performs Vorländer lookup; verbatim value lands at 0.83 (inside [0.80, 0.95] envelope but ≠ 0.85). `roomestim/model.py` `MaterialAbsorption[MELAMINE_FOAM]` updates 0.85 → 0.83. Lab A11 re-runs: under α₅₀₀ = 0.83, lab predicted ≈ 0.17 s, measured 0.158 s → rel_err = +7.6 %. Still in PASS-gate (±20 %). Tests pass; v0.12 ships. BUT the test `test_a11_soundcam_lab_pass_gate_recovered` was added at v0.11 with the assumption rel_err = +2.40 %; under +7.6 % the margin is reduced from 17.6 % to 12.4 % — still inside but the regression is silent. v0.13+ critic flags as "PASS-gate margin shrinking under successive verbatim updates" honesty leak.

**Hard-exit guard**:

```bash
# 1. ADR 0019 §Status-update-2026-05-12 MUST record the v0.12 lab re-run predicted + rel_err
grep -nE "lab.*v0\.12.*re-run|v0\.12 predicted.*0\.[0-9]+|v0\.12.*rel_err" docs/adr/0019-melamine-foam-enum-addition.md
# expect: ≥ 1 match IF α₅₀₀ changes at v0.12

# 2. Commit body MUST record the v0.12 lab rel_err if α₅₀₀ changes
git log -1 --format=%B | grep -nE "lab.*rel_err|MELAMINE_FOAM.*0\.[89][0-9]"
# expect: ≥ 1 match IF α₅₀₀ changes
```

**Mitigation**: ADR 0019 §Status-update records v0.12 lab re-run values. v0.12 commit body records the verbatim α₅₀₀ + lab rel_err. v0.13+ critic can read the audit trail and confirm the margin is acceptable. The test margin guard is +20 % (PASS-gate boundary); margin shrinking from 17.6 % to 12.4 % is acceptable as long as it stays > 0 — the test does NOT fail. A v0.13+ tightening of PASS-gate boundary (e.g., +15 %) is a planner-locked decision under D26 reverse-criterion.

---

## §6 OQ updates

### §6.1 OQs resolved at v0.12 ship time

| OQ | Status change | Resolution |
| --- | --- | --- |
| OQ-13b | `[ ]` → `[x]` (conditional on §2.4 classification being non-ambiguous) | Conference Sabine-shoebox residual classified per §2.4 (Sabine-approximation effect / coefficient-sourcing issue / ambiguous). ADR 0021 NEW captures the classification + Eyring comparator rationale. OQ-15 NEW raised per D26 policy. |

### §6.2 OQ updates without resolution (status-update annotations)

| OQ | Status change | Update |
| --- | --- | --- |
| OQ-13a | Already `[x]` at v0.11; v0.12 §Status-update annotation appended | Verbatim Vorländer 2020 §11 / Appx A citation closed at v0.12. α₅₀₀ value: <executor records; 0.85 verbatim-confirmed OR new value inside [0.80, 0.95] envelope>. ADR 0019 §Status-update-2026-05-12 records page + row + panel-thickness column. |
| OQ-13f | Already `[x]` at v0.11; v0.12 re-examination annotation appended | Re-examination outcome at v0.12: NO firing. Lab + conference structural-sign assertions UNCHANGED. Rationale per `.omc/plans/v0.12-design.md` §0.1 Item L row. |
| OQ-13h | Already `[x]` at v0.11; v0.12 reverse-criterion firing annotation appended | Lint scope expanded at v0.12 to add perf docs + architecture + README. ADR 0020 §Status-update-2026-05-12 records the expansion + v0.12 first-run flag count. |
| OQ-13b | If classification = ambiguous: `[ ]` → `[ ]` (status-update only); resolution-candidate revised to "v0.13+ comparator upgrade (mirror-image-source) required". | Per §2.3.5 reverse-criterion. |

### §6.3 OQs reaffirmed UNCHANGED at v0.12

| OQ | Reason |
| --- | --- |
| OQ-11 | UNCHANGED at v0.12. |
| OQ-12a | UNCHANGED on capture commitment (still user-volunteer-only); protocol DOC already shipped at v0.11. |
| OQ-12b/c | UNCHANGED. |
| OQ-13c | UNCHANGED — cross-repo PR re-submission deferred to v0.13+ (needs ≥ 3 captures per D11 + ADR 0018 §References tightening; v0.12 unchanged). |
| OQ-13d | already `[x]` resolved at v0.10.1. |
| OQ-13e | UNCHANGED — live-mesh extraction deferred to v0.13+. |
| OQ-13g | already codified in D22 at v0.10.1; D22 hybrid pattern applied again at v0.12 (verbatim citation + lint scope expansion). |
| OQ-13i | UNCHANGED — mypy strict still deferred to v0.13+. |
| OQ-14 | UNCHANGED — FIBERGLASS_CEILING + TILE_FLOOR deferred to v0.13+. |

### §6.4 NEW OQs raised at v0.12

| OQ | Description | Resolution candidate | Deferred to |
| --- | --- | --- | --- |
| **OQ-15** | Predictor-adoption decision — should v0.13+ ship Eyring or Millington-Sette as default predictor for glass-heavy rooms? | **Resolution candidate** (depending on item D classification): if classification = sabine_approximation_effect: v0.13+ ships Eyring as parallel-but-preferred predictor for rooms where the Sabine/Eyring ratio > 1.15; if classification = coefficient_sourcing_issue: v0.13+ ships per-band glass α revision (Vorländer 2020 §11 verbatim) WITHOUT predictor change; if classification = ambiguous: v0.13+ ships mirror-image-source comparator (new library code in `roomestim/predict/image_source.py`) before re-running the residual study. **Reverse**: if spatial_engine integration request lands at v0.12+ ship time, accelerate the predictor-decision to v0.13.0 (NOT v0.13+). | v0.13+ |

### §6.5 Persistence

Per planner protocol, this plan's open questions persist to
`.omc/plans/open-questions.md`:

- OQ-13b marked `[x]` resolved (conditional) OR status-update appended (if ambiguous).
- OQ-13a / OQ-13f / OQ-13h status-update annotations appended.
- OQ-15 NEW appended.
- All other OQs reaffirmed unchanged.

The `## v0.12-design — 2026-05-12` section in `open-questions.md` records the 1-2 resolutions + 3 status-updates + 1 new OQ.

---

## §7 Version bump

- `pyproject.toml`: `version = "0.11.0"` → `version = "0.12.0"`.
- `roomestim/__init__.py`: `__version__ = "0.11.0"` → `__version__ = "0.12.0"`. `__schema_version__` UNCHANGED `"0.1-draft"`.

**Why minor, not patch**: New ADR 0021 + new test surface (Eyring ratio characterising study) + lint-scope expansion + 2 new D-decisions + 1 new OQ exceed patch threshold. The conference disagreement_signature MAY reframe (structural change per D22 + ADR 0021 NEW). SemVer minor bump is the correct lane.

**Why not 0.11.1 ADD-ONLY patch**: A hypothetical v0.11.1 patch could ship only the ADR 0019 §Status-update + ADR 0020 §Status-update (no library change). The planner considered this and rejected it: the conference characterising study (item D) is the highest-leverage v0.12 item and is NOT a patch-class change — it lands a NEW ADR 0021. Splitting items C+K+L+D into v0.11.1 + v0.12.0 would create artificial scope boundaries.

---

## §8 Schema marker decision

`__schema_version__` UNCHANGED at `"0.1-draft"` at v0.12.

**Rationale**:

1. ADR 0016 §Reverse-criterion binds the schema flip to A10b in-situ capture (in-situ ALWAYS overrides substitute).
2. ADR 0018 §Reverse-criterion further tightens this to require ≥ 2 SoundCam rooms (lab + conference) returning to A11 PASS-gate under paper-faithful material maps AND ≥ 3 captures per D2.
3. v0.12 satisfies neither: conference is in disagreement-record (item D characterises it but does NOT close it under ±20 %); capture count is unchanged at 2 substitute rooms.
4. **Concrete invariant**: `__schema_version__ == "0.1-draft"` byte-equal under v0.12.

**Reverse-criterion**: unchanged from v0.11 §0.1. The Stage-2 re-flip is bound to A10b in-situ capture (lab_real.usdz fixture) + ≥ 3 captures. v0.12 does NOT pre-empt that decision.

---

## §9 D-decision list

### §9.1 D26 NEW — Predictor-adoption deferral policy

(Already drafted in §2.6.1; cross-referenced from `.omc/plans/decisions.md` D26 NEW entry.)

### §9.2 D27 NEW — Verbatim-pending closure cadence

(Already drafted in §2.6.2; cross-referenced from `.omc/plans/decisions.md` D27 NEW entry.)

---

## §10 Linear execution sequence (executor-facing)

This section is the executor-facing condensation of §2; the executor runs §10.0 → §10.11 sequentially. Each step has explicit verification commands and acceptance criteria.

### §10.0 Pre-flight

(Per §2.1; copy verbatim into executor working notes.)

### §10.1 Verbatim Vorländer 2020 lookup

Perform the textbook lookup. Record verbatim page + row + panel-thickness column + α₅₀₀ value in working notes. Determine whether value = 0.85 (no model.py change) OR ≠ 0.85 inside [0.80, 0.95] (model.py update) OR outside [0.80, 0.95] (STOP and escalate per §0.4 STOP rule #5).

### §10.2 Edit `roomestim/model.py` (conditional on §10.1 outcome)

ONLY IF verbatim ≠ 0.85: update `MaterialAbsorption[MaterialLabel.MELAMINE_FOAM]` row. Update `MaterialAbsorptionBands[MaterialLabel.MELAMINE_FOAM][2]` symmetrically if α₅₀₀ row changes. Otherwise BYTE-EQUAL.

### §10.3 Edit `docs/adr/0019-melamine-foam-enum-addition.md`

Append `## §Status-update-2026-05-12 (v0.12.0) — verbatim Vorländer citation` block after §References. Record verbatim citation. If §10.2 made a change, also annotate §Decision line.

### §10.4 Run lab A11 (conditional on §10.2 change)

ONLY IF §10.2 changed α₅₀₀: re-run `pytest tests/test_a11_soundcam_rt60.py::test_a11_soundcam_lab_band_record -v -s`. Capture predicted + rel_err. If rel_err leaves PASS-gate [-0.20, +0.20]: STOP per §0.4 STOP rule #7.

### §10.5 §2.4 Executor decision-point (Eyring ratio)

Apply edits to `tests/test_a11_soundcam_rt60.py` adding Eyring computation + ratio + classification field (without yet writing the classification string). Run `pytest tests/test_a11_soundcam_rt60.py::test_a11_soundcam_conference_disagreement_record -v -s`. Capture sabine_predicted, eyring_predicted, ratio. Apply classification per §2.3.5 thresholds. Write classification string + NEW companion tests.

### §10.6 Write `docs/adr/0021-conference-sabine-shoebox-residual-study.md`

NEW ADR per §3 + §2.3.4. ~120 lines target; ≤ 180 lines per §0.4 STOP rule #13.

### §10.7 Expand `scripts/lint_tense.py` scope

Add `docs/perf_verification_*.md`, `docs/architecture.md`, `README.md` to scope. Run lint. Capture first-run flag count. If > 5 files flagged: STOP per §0.4 STOP rule #6.

### §10.8 Append `docs/adr/0020-ci-lint-tense-policy.md` §Status-update

Record lint scope expansion + first-run flag count.

### §10.9 Update tests

Add 4 NEW tests per §0.2.A row 4-5, 8 (eyring_ratio + classification + verbatim_citation_complete + scope_includes_expanded_files). Confirm `pytest -m "not lab" --collect-only -q tests | tail -1` returns "128 tests collected".

### §10.10 Update `.omc/plans/open-questions.md` + `.omc/plans/decisions.md`

OQ-13b `[x]` (conditional); OQ-13a/f/h status-updates; OQ-15 NEW; §Status-update-2026-05-12 block in `open-questions.md`. D26/D27 NEW in `decisions.md`.

### §10.11 Version bump + RELEASE_NOTES + cumulative verification

```bash
# 1. Lint
/home/seung/miniforge3/bin/ruff check . 2>&1 | tail -3                    # "All checks passed!"
/home/seung/miniforge3/bin/python -m mypy roomestim 2>&1 | tail -3        # pre-existing baseline
python scripts/lint_tense.py                                              # exit 0 on EXPANDED 5-family scope

# 2. Test count
/home/seung/miniforge3/bin/python -m pytest -m "not lab" --collect-only -q tests 2>&1 | tail -3
# expect: "128 tests collected"

# 3. Test pass
/home/seung/miniforge3/bin/python -m pytest -m "not lab" tests -q 2>&1 | tail -5
# expect: "128 passed"

# 4. ACE corpus E2E (gated)
/home/seung/miniforge3/bin/python -m pytest -m e2e tests/test_e2e_ace_challenge_rt60.py -v 2>&1 | tail -10
# expect: byte-equal pass

# 5. Version + schema invariants
/home/seung/miniforge3/bin/python -c "import roomestim; print(roomestim.__version__, roomestim.__schema_version__)"
# expect: "0.12.0 0.1-draft"

# 6. Diff envelope check
git diff --stat eee3014 | tail -1
# expect: ≤ 600 added, ≤ 40 removed (per §0.4 STOP rule #1)

# 7. Byte-equality of locked-out file families (per §0.3)
git diff --stat eee3014 -- roomestim/ | grep -vE "__init__.py|model.py"   # expect: empty
git diff --stat eee3014 -- tests/ | grep -vE "test_a11_soundcam_rt60.py|test_room_acoustics_octave.py|test_misc_soft_furniture_budget.py|test_lint_tense.py"
# expect: empty (model.py changed only if §10.2 fired; misc_soft_budget changed only if envelope test pinned to specific value)
git diff --stat eee3014 -- proto/ docs/perf_verification_*.md .omc/research/                    # expect: empty
git diff --stat eee3014 -- docs/adr/ | grep -vE "0019|0020|0021"               # expect: empty

# 8. Pre-mortem grep guards (§5)
grep -c "MELAMINE_FOAM.*0\." roomestim/model.py                            # value M from model.py
grep -c "verbatim.*0\." docs/adr/0019-melamine-foam-enum-addition.md       # value V from ADR
# M MUST equal V (pre-mortem §5.1 mitigation)
grep -c "sabine_eyring_ratio.*> 1\.0" tests/test_a11_soundcam_rt60.py     # ≥ 1 (pre-mortem §5.2)
grep -c "ADR 0018" docs/adr/0021-conference-sabine-shoebox-residual-study.md  # ≥ 1 (pre-mortem §5.3)
grep -c "# noqa: lint-tense" README.md docs/perf_verification_*.md docs/architecture.md  # ≤ 5 sum (pre-mortem §5.4)
```

If any verification fails, STOP and re-plan per §0.4 STOP rules.

---

## §11 Hand-off

### §11.1 Pipeline

`planner` (this plan) → `executor` (mechanical implementation per §10) → `code-reviewer` (lint + diff-envelope + byte-equality of locked-out files + ADR 0021 review) → `verifier` (full pytest + ACE E2E + pre-mortem grep guards + diff-envelope check) → user (final review + commit + local tag `v0.12.0`).

Per project memory: non-trivial work goes through OMC pipeline; authoring + review separated; planner does NOT self-approve; executor MUST hand off to code-reviewer before commit. v0.12 is non-trivial (new ADR + new test surface + lint-scope expansion).

### §11.2 Commit subject (executor-facing template)

```
roomestim v0.12.0 — Vorländer verbatim α₅₀₀ closure (ADR 0019 §Status-update) + conference Eyring residual study (ADR 0021) + lint scope expansion (ADR 0020 §Status-update) + OQ-13f re-examination + D26/D27
```

(Body: standard release-notes summary; reference `RELEASE_NOTES_v0.12.0.md`.)

### §11.3 Tag policy

Local tag `v0.12.0` per D11 (tag-local-only policy unchanged). NOT pushed to remote.

### §11.4 What v0.12 does NOT do (explicit forbid list)

The plan §0.4 STOP rules explicitly forbid:

- FIBERGLASS_CEILING + TILE_FLOOR enum additions (OQ-14 unchanged; deferred to v0.13+).
- `MISC_SOFT` row coefficient changes in `MaterialAbsorption{,Bands}`.
- Predictor / CLI / adapter logic changes (including Eyring predictor body).
- Schema file (`proto/`) edits.
- Cross-repo PR re-submission (D11 + ADR 0018 §References tightening; needs ≥ 3 captures).
- A10b in-situ ACTUAL capture (protocol DOC already shipped at v0.11; capture commitment unchanged).
- Live-mesh extraction (OQ-13e; UNCHANGED at v0.12).
- Mypy strict cleanup (OQ-13i; UNCHANGED at v0.12).
- Living_room re-introduction (ADR 0018 §Reverse-criterion; UNCHANGED at v0.12).
- ADR 0018 edits (STOP rule #11; cross-link from ADR 0018 → ADR 0021 lands at v0.13+ if needed).
- Default predictor change (D26 NEW: characterise first, decide second; ADR 0022 deferred to v0.13+).
- New library code in `roomestim/predict/*` (mirror-image-source, Millington-Sette; deferred to v0.13+).
- `__schema_version__` re-flip to `"0.1"` (bound to A10b in-situ capture + ≥ 3 captures per ADR 0016 §Reverse-criterion + D2; v0.12 satisfies neither).

---

## §12 References

- v0.11.0 commit: `eee3014` — MELAMINE_FOAM enum (ADR 0019) + lab A11 PASS-gate recovered + CI tense-lint (ADR 0020) + in-situ A10b protocol DOC stub + D23/D24/D25.
- v0.11.0 design plan: `.omc/plans/v0.11-design.md` (1123 lines; DELIBERATE-mode precedent).
- v0.10.1 design plan: `.omc/plans/v0.10.1-patch.md` (863 lines; SHORT-mode precedent).
- v0.10 design plan: `.omc/plans/v0.10-design.md`.
- v0.11.0 release notes: `RELEASE_NOTES_v0.11.0.md`.
- ADR 0009 (D9) — Eyring parallel predictor; runtime invariant `eyring ≤ sabine + 1e-9`.
- ADR 0016 — Stage-2 schema flip via SoundCam substitute (FIRED at v0.10; §Reverse-criterion binds schema flip to A10b in-situ).
- ADR 0017 — A10-layout deferred-with-classification (byte-equal under v0.12).
- ADR 0018 — Substitute-disagreement record (v0.10 + v0.10.1 §Status-update-2026-05-10b; byte-equal under v0.12 per STOP rule #11).
- ADR 0019 — MELAMINE_FOAM enum addition (v0.11; §Status-update-2026-05-12 appended at v0.12).
- ADR 0020 — CI lint policy (v0.11; §Status-update-2026-05-12 appended at v0.12).
- ADR 0021 NEW (v0.12) — Conference Sabine-shoebox residual characterising study.
- D2 (≥3 captures requirement for Stage-2 lock) — unchanged.
- D11 (cross-repo PR tag-local-only policy) — applies to v0.12.
- D14..D25 — byte-equal under v0.12.
- D26 NEW (v0.12) — Predictor-adoption deferral policy.
- D27 NEW (v0.12) — Verbatim-pending closure cadence.
- OQ-11 — UNCHANGED at v0.12.
- OQ-12a — UNCHANGED at v0.12 (capture commitment unchanged; protocol DOC already shipped at v0.11).
- OQ-12b/c — UNCHANGED.
- OQ-13a — Already `[x]` at v0.11; v0.12 §Status-update annotation (verbatim citation closed).
- OQ-13b — RESOLVED `[x]` at v0.12 (conditional on §2.4 classification being non-ambiguous; status-update if ambiguous).
- OQ-13c/e — UNCHANGED.
- OQ-13d — already `[x]` at v0.10.1.
- OQ-13f — Already `[x]` at v0.11; v0.12 re-examination annotation (NO firing).
- OQ-13g — already codified in D22; D22 hybrid applied at v0.12.
- OQ-13h — Already `[x]` at v0.11; v0.12 reverse-criterion firing annotation (lint scope expanded).
- OQ-13i — UNCHANGED.
- OQ-14 — UNCHANGED at v0.12.
- OQ-15 NEW (v0.12) — Predictor-adoption decision deferred to v0.13+ per D26 policy.
- Vorländer 2020, *Auralization*, §11 / Appendix A — primary coefficient source for MELAMINE_FOAM α₅₀₀; verbatim citation closed at v0.12.
- ADR 0009 — Eyring parallel predictor (D9); used at v0.12 as conference disagreement-study comparator.
- arXiv:2311.03517v2 (NeurIPS 2024 D&B, SoundCam) — paper-retrieved RT60 + dims for lab + conference; byte-equal under v0.12.
- `RELEASE_NOTES_v0.12.0.md` (NEW).
- v0.11 weekly progress report: `docs/weekly_progress_report_2026-05-11.md` (byte-equal under v0.12).

---

## §13 Open-questions persistence note

Per planner protocol, this plan's open questions persist to `.omc/plans/open-questions.md`:

- OQ-13b marked `[x]` resolved at v0.12 (conditional on non-ambiguous classification) OR status-update appended (if ambiguous).
- OQ-13a / OQ-13f / OQ-13h status-update annotations appended.
- OQ-15 NEW appended.
- All other OQs reaffirmed unchanged.

The `## v0.12-design — 2026-05-12` section in `open-questions.md` is the canonical landing page for the resolution + 3 status-updates + 1 new OQ.

---

## §14 Planner-locked default answers to parent-prompt open questions

The parent orchestrator prompt asked 5 questions; each gets a single planner-locked default with rationale + reverse-criterion:

### §14.1 Q1: Item C standalone or bundled?

**LOCKED**: Bundled with K + L + D (4-item bundle). Standalone would ship ~50 lines (ADR 0019 §Status-update only) — too small for its own minor release; the project's 4-item bundling precedent at v0.11 amortises release cycles efficiently. Reverse-criterion: if item D characterising study is dropped mid-flight (e.g., Eyring monotonicity invariant breaks; per §0.4 STOP rule #7), downgrade v0.12 scope to 3-item (C + K + L) and ship as v0.11.1 patch.

### §14.2 Q2: Bundle item L (OQ-13f reverse re-examination)?

**LOCKED**: YES, bundle. L is pure bookkeeping (no test/library changes); near-zero cost; ensures the v0.11 OQ-13f resolution-candidate's "structural-sign assertion may become soft FAIL gate" reverse-criterion is explicitly re-examined at v0.12 (the next minor release after lab returned to PASS-gate). Outcome: NO firing per §0.1 row "Item L". Reverse-criterion: if v0.13+ critic argues margin is too large (e.g., lab `rel_err < 0.20` is too loose given v0.11 +2.40 % empirical), tighten in v0.13+ under successor ADR — NOT at v0.12.

### §14.3 Q3: Item D (Sabine-shoebox residual) vs Item F (live-mesh)?

**LOCKED**: Item D (conference Sabine-shoebox residual characterising study). D closes a v0.11-still-OPEN disagreement signature using in-repo Eyring (zero library cost); F requires 3-5 days + SoundCam mesh download access + alpha-shape / RANSAC / Hough library code (~300-500 lines new library) — way over v0.12 envelope. Defer F to v0.13+ under OQ-13e (UNCHANGED). Reverse-criterion: if v0.12 Eyring ratio is in the ambiguous [1.10, 1.15] zone, the characterising study lands as inconclusive — but it still lands; v0.13+ would then take the OQ-13e live-mesh path (or mirror-image-source) for additional discrimination. The two items are NOT mutually exclusive; they are sequenced by minimum-leverage at v0.12 → multi-comparator at v0.13+.

### §14.4 Q4: Branch decision (minor vs patch)?

**LOCKED**: v0.12.0 minor. Per §7 — new ADR 0021 + new test surface (Eyring ratio characterising study) + lint scope expansion + 2 new D-decisions + 1 new OQ + potential conference signature reframe. Patch threshold would require library byte-equality, which v0.12 satisfies ONLY conditionally (model.py byte-equal if verbatim α₅₀₀ = 0.85; otherwise +1/-1 line). Even at minimum library change, the ADR 0021 NEW + 4 NEW tests exceed patch threshold. Reverse-criterion: if item D is dropped mid-flight (§0.4 STOP rule #7), downgrade to v0.11.1 patch with OQ-13b re-deferred to v0.13+.

### §14.5 Q5: Schema marker?

**LOCKED**: Stays `"0.1-draft"`. Stage-2 re-flip is bound to ADR 0016 §Reverse-criterion + A10b in-situ capture + ≥ 3 captures per D2 (and ADR 0018 §References tightening); v0.12 satisfies none of these (no capture; conference still in disagreement). Per §8. Reverse-criterion: unchanged from v0.11 §0.1 + §8.

---

**END of v0.12 design plan.**
