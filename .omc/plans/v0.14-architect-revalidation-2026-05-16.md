# v0.14.0 architect re-validation memo — 2026-05-16

- **Author**: oh-my-claudecode:architect (READ-ONLY re-validation pass)
- **Predecessor planner artefact**: `/home/seung/mmhoa/roomestim/.omc/plans/v0.14-design.md` (805+ lines, written 2026-05-12; last edit 2026-05-12 10:58)
- **Interim release shipped**: v0.12-web.1 (commit `0bef198`, 2026-05-16 01:54) on the parallel web-demo track
- **Today's date**: 2026-05-16 (4 days after planner-write; 0 days after v0.12-web.1 ship)
- **Scope**: re-validate v0.14-design.md currency, hard-wall lock, and executor-readiness given v0.12-web.1 interim.

## Verdict: YELLOW — 4 amendments needed; absorb then executor

The planner-locked DEFAULT (path γ honesty-leak fallback under ADR 0022 NEW) STILL HOLDS. The substantive Item A / B / C / D framing is currency-unaffected by v0.12-web.1 (acoustics-track core was byte-equal except `polycam.py` → `mesh.py` rename per D33). However, four mechanical / cross-reference items in v0.14-design.md require absorption before executor takeoff:
1. CRITICAL: §1.2 LOCKED test count `131 → 145` is now physically impossible to verify with `pytest -m "not lab"` because v0.12-web.1 added 37 web-lane tests under `tests/web/` that the filter does not exclude (live repo at HEAD: `pytest -m "not lab"` collects **176**, not 131). The §5.1 + §5.8 acceptance commands will trip §0.4 STOP rule #3 every executor run.
2. MAJOR: ADR-numbering collision: v0.14-design.md repeatedly cites "ADR 0022 NEW" and "ADR 0023 RESERVED" but the v0.12-web.0/web.1 cycle has already burned ADR 0024 (web demo), 0025 (binaural), 0026 (HRTF), 0027 (mesh-format). v0.14 must rename to "ADR 0028 NEW" and "ADR 0029 RESERVED" or the audit-trail honesty discipline breaks.
3. MAJOR: §6.1 "v0.10 → v0.14 5-release window" narrative is incomplete: omits the parallel v0.12-web.0 / v0.12-web.1 track entirely. The cadence read (DELIBERATE-DELIBERATE-DELIBERATE-SHORT-DELIBERATE) is now incorrect-as-written because two interim parallel-track DELIBERATE-equivalent releases also shipped.
4. MINOR: D32 (tempdir lifecycle) + D33 (MeshAdapter rename) + OQ-17/18/19/20/21/22 (web-track follow-ups) are not cross-referenced in §3.1 / §3.2 / §7. The "OQ-17 NEW (Polygon ISM v0.15+)" planner-locked numbering at §3.1 + §8 now COLLIDES with already-shipped OQ-17 (HUTUBS subject-id stability per `.omc/plans/open-questions.md:185`).

Nothing in the v0.12-web.1 surface invalidates D27 hard-wall cadence, path γ default-safe lock, the [0.80, 0.95] envelope, MELAMINE_FOAM α₅₀₀ = 0.85 byte-equality claim, lab A11 PASS-gate (rel_err = +2.40 %), or the ISM Item B library plan. Amendments are mechanical, not substantive.

---

## §1 Currency check

### §1.1 v0.12-web.1 vs path γ closure dependencies — NO INVALIDATION

I confirmed at file:line level that v0.12-web.1's `0bef198` commit touches NONE of the path γ closure dependencies:

- **MELAMINE_FOAM enum + α₅₀₀ = 0.85**: commit `0bef198` body confirms "NO change to MaterialLabel enum (10 entries; MELAMINE_FOAM α₅₀₀ = 0.85 byte-equal)" (commit message verbatim). `roomestim/model.py` is NOT in the `git show 0bef198 --stat` file list (only `roomestim/adapters/__init__.py`, `roomestim/adapters/mesh.py`, `roomestim/adapters/polycam.py` touch `roomestim/`).
- **Lab A11 PASS-gate**: `tests/test_a11_soundcam_rt60.py` is NOT in the `0bef198` file list. `_LAB_EXPECTED` byte-equal to v0.13.0.
- **ace_challenge adapter**: `roomestim/adapters/ace_challenge.py` is NOT in the `0bef198` file list. `_geom_float` mypy narrowing from v0.13.0 OQ-13i closure stays intact.
- **ISM library lock at `roomestim/reconstruct/image_source.py`**: directory `roomestim/reconstruct/` (`/home/seung/mmhoa/roomestim/roomestim/reconstruct/`) currently contains `floor_polygon.py`, `__init__.py`, `listener_area.py`, `materials.py`, `walls.py`. No `image_source.py` (correct — v0.14 ships it). No `predict/` package created (correct — v0.14-design.md §0.0 row "Library location lock" + CC-1 still applies byte-equal).
- **Vorländer / Bies & Hansen / NRC verbatim search**: `grep -rin "Bies\|NRC datasheet\|Vorländer verbatim" /home/seung/mmhoa/roomestim/docs/` returns ONLY references inside ADR 0019 §References + §Status-update blocks (lines 114, 116, 165, 173, 180, 182-183, 193, 198 of `/home/seung/mmhoa/roomestim/docs/adr/0019-melamine-foam-enum-addition.md`). NO new verbatim α₅₀₀ value has landed between v0.13.0 and 2026-05-16. Path α / β remain OPPORTUNISTIC; path γ remains default-safe.

**Conclusion**: v0.14-design.md §0.0 Item A row + §2.A Item A "What is explicitly NOT changing" claims are still BYTE-TRUE in the live repo.

### §1.2 v0.12-web.1 file overlap vs v0.14 §1.1 file table — NO OVERLAP except `tests/test_mypy_strict_baseline.py`

`git show 0bef198 --stat` touched 26 files. Cross-referenced against v0.14-design.md §1.1 (19-22 rows):

| v0.14 §1.1 row | Path | v0.12-web.1 touched? |
| --- | --- | --- |
| 1 | `docs/adr/0022-hardwall-closure-and-ism-adoption.md` NEW | No |
| 2 | `roomestim/reconstruct/image_source.py` NEW | No |
| 3 | `roomestim/reconstruct/__init__.py` MOD | No |
| 4 | `tests/test_image_source.py` NEW | No |
| 5 | `tests/test_a11_soundcam_rt60.py` MOD | No |
| 6 | `tests/test_e2e_ace_challenge_rt60.py` MOD | No |
| 7 | `tests/test_room_acoustics_octave.py` MOD | No |
| 8 | `docs/adr/0019-melamine-foam-enum-addition.md` MOD | No |
| 9 | `docs/adr/0021-sabine-shoebox-residual-study.md` MOD (conditional) | No |
| 10 | `.omc/plans/open-questions.md` MOD | YES (+49 lines, OQ-20/21/22 NEW; v0.14 plan envelope ~30 added must absorb that this file already grew) |
| 11 | `.omc/plans/decisions.md` MOD (conditional) | YES (+35 lines, D32/D33 NEW; v0.14 plan must account for the larger baseline) |
| 12 | `pyproject.toml` MOD | No (commit notes "pyproject.toml version stays 0.13.0") |
| 13 | `roomestim/__init__.py` MOD | No (only `roomestim_web/__init__.py` touched at web-track) |
| 14 | `RELEASE_NOTES_v0.14.0.md` NEW | No (web-track shipped `RELEASE_NOTES_v0.12-web.1.md`) |
| 15 | `scripts/lint_tense.py` MOD | No |
| 16 | `tests/test_lint_tense.py` MOD | No |
| 17 | `tests/test_mypy_strict_baseline.py` (UNCHANGED) | YES — `tests/test_mypy_strict_baseline.py` was MOD'd at v0.12-web.1 (+10 lines, importlib.util.find_spec skipif gate); v0.14-design.md §1.1 row 17 says "(UNCHANGED)" — incorrect after v0.12-web.1. |
| 18 | `docs/adr/0023-predictor-default-switch.md` NEW (conditional) | No |
| 19 | `docs/perf_verification_ism_v0_14.md` NEW (conditional) | No |

**Conclusion**: Only ONE row of v0.14 §1.1 has a stale "UNCHANGED" annotation — row 17 (`tests/test_mypy_strict_baseline.py`). This is MINOR (the test still exists and gates the mypy strict baseline; v0.12-web.1's edit added a `pytest.importorskip("mypy")` skipif gate so the test no longer fails on hosts without `[dev]` extras). v0.14 row 17 should be reframed from "(UNCHANGED)" to "(MOD'd at v0.12-web.1 commit `0bef198`; v0.14 inherits the skipif gate byte-equal)".

### §1.3 D32 / D33 impact on D27 cadence accounting + path γ closure — NONE

- **D32** (tempdir lifecycle: `_EvictingDeque(maxlen=8)` + atexit 4 h reaper) is fully scoped to `roomestim_web/app.py`. NO acoustics-track dependency. Does NOT touch ADR 0019 / D27 cadence / path γ closure.
- **D33** (MeshAdapter rename: `PolycamAdapter` → `MeshAdapter` as canonical; `PolycamAdapter` retained as deprecated subclass alias) is fully scoped to `roomestim/adapters/{mesh.py,polycam.py}`. Convex-hull-of-XY-projection geometry preserved byte-equal across `.obj/.gltf/.glb/.ply`. NO MaterialLabel enum touch. NO α value touch. The v0.12-web.1 commit message explicitly states "NO change to default WALL_PAINTED/WOOD_FLOOR/CEILING_DRYWALL material triple; NO change to v0.13.0's α₅₀₀ values or D27 hard-wall cadence (v0.14 SECOND-AND-LAST re-deferral preserved)".

**Cross-check**: `/home/seung/mmhoa/roomestim/.omc/plans/v0.12-web.1-design.md:21` reads "D27 hard wall at v0.14 unaffected; OQ-13a / OQ-16 untouched". And `.omc/plans/v0.12-web.1-design.md:88,505` reads "D27 hard wall (v0.14 Vorländer α₅₀₀) — orthogonal; acoustics-track only" and "D27 hard wall at v0.14 (Vorländer α₅₀₀) unchanged". The v0.12-web.1 planner explicitly carved D27 OUT of scope.

**Conclusion**: D27 cadence accounting in v0.14-design.md §6.1 stays CORRECT for the substantive content (v0.11 NEW pending → v0.12 FIRST re-deferral → v0.13 SECOND-AND-LAST → v0.14 HARD WALL). However the §6.3 narrative cadence ("DELIBERATE-DELIBERATE-DELIBERATE-SHORT-DELIBERATE") OMITS v0.12-web.0/web.1 as parallel-track interim releases. See AMENDMENT 3.

### §1.4 OQ-20/21/22 vs v0.14 Item C / Item B — NO OVERLAP, but NUMBERING COLLISION

- **OQ-20** (glTF binary `.glb` byte-equal reproducibility across trimesh versions): scoped to `roomestim_web/` + `MeshAdapter`. NO interaction with Item C (per-band glass α revision) or Item B (ISM analytic shoebox validation). The fixtures `lab_room.gltf/.glb/.ply` are in `tests/fixtures/` (per the `git show 0bef198 --stat` file list) but they're consumed by `tests/test_adapter_mesh.py`, NOT by `tests/test_image_source.py`. Independent.
- **OQ-21** (PLY no-faces degenerate case): mesh-adapter scope only. NO acoustics interaction.
- **OQ-22** (`_TEMP_REAPER` 4 h `atexit` window tightening): web-track only. NO acoustics interaction.

**CRITICAL FINDING (numbering collision)**: v0.14-design.md §3.1 (`/home/seung/mmhoa/roomestim/.omc/plans/v0.14-design.md:580`), §3.1 line 581, line 582, §7 line 798, and §8 lines 808/810/812 all reference "OQ-17 NEW (Polygon ISM v0.15+)" / "OQ-18 NEW (Predictor-default switch)" / "OQ-19 NEW (Per-band glass α revision)". These numbers were planner-locked at v0.13-design pass (2026-05-12), but `.omc/plans/open-questions.md` now records:
- **OQ-17** = HUTUBS subject-id stability (v0.12-web.0; `open-questions.md:185`)
- **OQ-18** = HF Spaces cold-start budget (v0.12-web.0; `open-questions.md:197`)
- **OQ-19** = Binaural WAV byte-exact reproducibility (v0.12-web.0; `open-questions.md:208`)
- **OQ-20** = glTF `.glb` byte-equal (v0.12-web.1; `open-questions.md:223`)
- **OQ-21** = PLY no-faces (v0.12-web.1; `open-questions.md:240`)
- **OQ-22** = `_TEMP_REAPER` tightening (v0.12-web.1; `open-questions.md:256`)

Therefore v0.14 cannot register its three NEW OQs as OQ-17/18/19 — they must be renumbered. The next available slot is **OQ-23 / OQ-24 / OQ-25**. This is MAJOR (D28-P1 audit-trail discipline forbids silent re-numbering; the planner must explicitly renumber + cite the v0.12-web.0/web.1 OQ allocation).

---

## §2 Hard-wall verification

### §2.1 v0.14-design.md §0.0 Item A — RE-CONFIRMED

I re-read `/home/seung/mmhoa/roomestim/.omc/plans/v0.14-design.md` line 127 (Item A row): "**γ — honesty-leak fallback under ADR 0022 NEW (default-safe lock)**. ADR 0022 NEW: α₅₀₀ DEMOTED to 'envelope-bounded mid-value with no verbatim source acquired through D27 cadence exhaustion (v0.11 NEW pending → v0.12 FIRST re-deferral → v0.13 SECOND-AND-LAST re-deferral → v0.14 hard-wall closure under γ)'. Enum MELAMINE_FOAM RETAINED; α₅₀₀ = 0.85 BYTE-EQUAL; lab A11 PASS-gate BYTE-EQUAL."

Live-repo verification (path γ default-safe condition at 2026-05-16):
- **MELAMINE_FOAM α₅₀₀ = 0.85** STILL byte-equal (v0.12-web.1 commit body verbatim: "MELAMINE_FOAM α₅₀₀ = 0.85 byte-equal").
- **Lab A11 PASS-gate (rel_err = +2.40 %)** STILL byte-equal (commit body: "NO change to v0.13.0's α₅₀₀ values or D27 hard-wall cadence").
- **OQ-13a status** at `.omc/plans/open-questions.md:171` (v0.13.0 ship-time annotation): "Vorländer page + row + panel-thickness column verbatim citation **STILL PENDING** ... Re-deferred under D27 SECOND-AND-LAST permitted re-deferral. v0.14 = HARD WALL." No v0.14-ship annotation yet (correct — v0.14 has not shipped).

### §2.2 §0.4 STOP rule #7 — STILL NOT FIRED

STOP rule #7 condition: "Verbatim Vorländer OR Bies & Hansen OR NRC datasheet α₅₀₀ becomes available mid-cycle AND lands inside [0.80, 0.95] envelope".

Live-repo search at 2026-05-16:
```
grep -rin "Bies\|NRC datasheet\|Vorländer verbatim" /home/seung/mmhoa/roomestim/docs/
```
Returns 10 lines, ALL inside `/home/seung/mmhoa/roomestim/docs/adr/0019-melamine-foam-enum-addition.md` — at §References row "Bies & Hansen (2018), Engineering Noise Control, §A — secondary cross-check" (`docs/adr/0019-melamine-foam-enum-addition.md:116`) AND inside the v0.12 / v0.13 §Status-update blocks recording the closure-attempt outcomes. NO extracted verbatim α₅₀₀ value for melamine foam has appeared between v0.13.0 (`2046681`, 2026-05-12) and 2026-05-16. The same `grep` evidence the v0.13 planner used at line 153 of `.omc/plans/open-questions.md` (`grep -rn "Bies\|Hansen" docs/ roomestim/` returning ONLY the §References row) STILL HOLDS.

### §2.3 SNU library ILL channel — NO NEW STATUS UPDATE

I read `/home/seung/mmhoa/roomestim/docs/adr/0019-melamine-foam-enum-addition.md` lines 100-209 in full. The latest §Status-update block (v0.13.0, "SECOND re-deferral", line 165) records: "External-acquisition-channel exhaustion record (Option B — HONEST-FALLBACK): At v0.13 executor-time, external acquisition channels (SNU library ILL, OA mirrors, publisher OA page) for Bies & Hansen 2018 §A were NOT investigated." (line 196-200).

No newer §Status-update block has landed (correct — v0.14 has not shipped). v0.12-web.1 (`0bef198`) did not amend ADR 0019 (verified via `git show 0bef198 --stat`: `docs/adr/0019-melamine-foam-enum-addition.md` is NOT in the file list).

**Conclusion**: At 2026-05-16, NO verbatim Vorländer / Bies & Hansen / NRC datasheet melamine α₅₀₀ has surfaced. Planner-locked DEFAULT γ STILL HOLDS. Path α/β remain OPPORTUNISTIC at executor-time per §0.4 STOP rule #7. v0.14-design.md §0.0 Item A row + §0.4 STOP rule #7 require NO amendment.

### §2.4 STOP rules #5, #6, #11 — STILL CORRECT

- **STOP rule #5** (`__schema_version__` must stay `"0.1-draft"`): UNCHANGED. v0.12-web.1 commit body: "NO change to RoomModel.schema_version ('0.1-draft')".
- **STOP rule #6** (`MaterialLabel` enum gains no new entry): UNCHANGED. v0.12-web.1 commit body: "NO change to MaterialLabel enum (10 entries; MELAMINE_FOAM α₅₀₀ = 0.85 byte-equal)".
- **STOP rule #11** (no library logic mutation in `roomestim/place/*`, `roomestim/cli.py`, `roomestim/adapters/*`, `roomestim/io/*`, `roomestim/export/*`): **PARTIALLY OBSOLETE**. v0.12-web.1 DID mutate `roomestim/adapters/*` (`polycam.py` → subclass alias; `mesh.py` NEW). The v0.14-design.md STOP rule #11 text was written BEFORE v0.12-web.1 and reflects v0.13.0 baseline. v0.14 executor will inherit `roomestim/adapters/mesh.py` + the `PolycamAdapter`-as-subclass-alias structure as the v0.14 BASELINE; the v0.14 STOP rule #11 should not interpret that pre-existing v0.12-web.1 mutation as an in-cycle violation. See AMENDMENT 4.

---

## §3 Amendments

### AMENDMENT 1 (severity: CRITICAL)
**§1.2 line 332-356 + §0.0 row "Test count target" line 137 + §0.4 STOP rule #3 line 272 + §5.1 line 663-668 + §5.8 line 752 — test count target `131 → 145` with `-m "not lab"` filter**

§0.0 row "Test count target" verbatim:
> Concrete acceptance line: `pytest -m "not lab" --collect-only -q tests 2>&1 \| tail -1` reads exactly `145 tests collected`.

§5.1 verbatim:
> ```bash
> /home/seung/miniforge3/bin/python -m pytest -m "not lab" --collect-only -q tests 2>&1 | tail -3
> # expect: "145 tests collected" ±2 (143-147 range).
> ```

**Replace with**: Either (a) tighten the executor filter to `pytest -m "not lab and not web"` to preserve the 131-baseline + 14 ISM addition = 145 target on the acoustics-track; OR (b) re-baseline to current `pytest -m "not lab"` collection of **176** (= 132 acoustics-default + 7 mesh-adapter tests + 37 web-lane) and target 176 + 14 = **190** (±2). Path (a) is recommended because the v0.14 release is acoustics-track (web-track ships under `RELEASE_NOTES_v0.12-web.N.md` parallel cadence per D30; mixing the test count gates couples them).

**Reason**: I verified live-repo at 2026-05-16:
```
/home/seung/miniforge3/bin/python -m pytest -m "not lab" --collect-only -q tests 2>&1 | tail -3
# 176/177 tests collected (1 deselected) in 2.14s
```
and
```
/home/seung/miniforge3/bin/python -m pytest -m "not lab and not e2e and not web" --collect-only -q tests 2>&1 | tail -3
# 136/177 tests collected (41 deselected) in 1.72s
/home/seung/miniforge3/bin/python -m pytest -m web --collect-only -q tests 2>&1 | tail -3
# 38/177 tests collected (139 deselected) in 1.74s
```
The v0.14-design.md §1.2 baseline of 131 was computed at v0.13.0 ship time (commit `2046681`, 2026-05-12). Between then and 2026-05-16, v0.12-web.0 added ~22 web-lane tests + v0.12-web.1 added +16 web-lane tests + 7 mesh-adapter tests in `tests/` (per the v0.12-web.1 commit message: "Web lane 37 passed + 3 skipped (was 21 + 1 at v0.12-web.0; +16 new web tests)"). The web-lane tests live in `tests/web/` and the marker `-m "not lab"` does NOT exclude `-m web`, so they are counted in the default-lane collection.

Without this amendment, the §5.1 acceptance command `pytest -m "not lab" --collect-only -q` will return `190 tests collected` (after +14 ISM), tripping §0.4 STOP rule #3 (range [143, 147]) on every executor run regardless of correct ISM implementation. Executor halts immediately. CRITICAL.

### AMENDMENT 2 (severity: MAJOR)
**Throughout v0.14-design.md — "ADR 0022 NEW" / "ADR 0023 RESERVED" naming**

v0.14-design.md cites "ADR 0022" 80+ times (§0.0 line 127, line 131, line 132, line 134, line 139, line 140; §1 line 295-298; §1.1 line 304; §2.A line 372-381; §2.B line 491; §2.C line 514-535; §3.1 line 581-582; §4 line 622-637; §5.7 line 733-743; §7 line 796-798; §10 line 892-894; etc.) and "ADR 0023 RESERVED" several times (§0.0 line 139; §2.D line 553-560; §10 line 892).

**Replace with**: Renumber all "ADR 0022" → "ADR 0028" and all "ADR 0023" → "ADR 0029" throughout v0.14-design.md. Also rename the planned file path from `docs/adr/0022-hardwall-closure-and-ism-adoption.md` → `docs/adr/0028-hardwall-closure-and-ism-adoption.md` (and 0023 → 0029 for the conditional file).

**Reason**: I ran `ls /home/seung/mmhoa/roomestim/docs/adr/` and the existing ADR slots are:
```
0001..0021 (acoustics-track sequential)
0024 (web-demo-separate-package, v0.12-web.0)
0025 (binaural-demo-stack, v0.12-web.0)
0026 (hrtf-dataset-selection, v0.12-web.0)
0027 (mesh-format-generalisation, v0.12-web.1)
```
Numbers 0022 and 0023 are unallocated (gap between acoustics-track 0021 and web-track 0024 reserved at v0.12-web.0 time), but D28-P1 audit-trail discipline + the existing ADR numbering convention (sequential across releases regardless of track) implies the next allocated number is 0028, not 0022. Shipping v0.14 with "ADR 0022" overwriting the gap would (i) create a non-sequential audit trail; (ii) leave readers unable to predict ADR ordering by date; (iii) risk silently overwriting the intent of the v0.12-web.0 author who left 0022/0023 unallocated (possibly reserved for an in-flight v0.13.x or v0.12-web.x ADR; see commit `cfea9cb` for v0.12-web.0 ADR numbering). The safe, audit-discipline-compliant choice is to allocate 0028 / 0029 at v0.14 ship time.

Note: I did NOT find an explicit D-decision or planner artefact stating "ADR numbers 0022 / 0023 are reserved for v0.14 acoustics-track" — the v0.13-design.md (line 153 of `.omc/plans/open-questions.md`) and v0.14-design.md both use "ADR 0022" forward-looking, but this was planner-locked BEFORE v0.12-web.0 allocated 0024-0027. The reverse-reading is also viable (v0.14 acoustics-track had naming intent on 0022 first; v0.12-web.0 jumped to 0024 deliberately to leave 0022/0023 for v0.14). Either way, the v0.14 planner MUST decide explicitly and record the rationale in v0.14-design.md (not silently take 0022).

### AMENDMENT 3 (severity: MAJOR)
**§6.3 line 779-789 — "v0.14 fits the 5-release window v0.10 → v0.14"**

§6.3 verbatim:
> ### §6.3 v0.14 fits the 5-release window v0.10 → v0.14
> - **v0.10**: honesty correction ... DELIBERATE.
> - **v0.11**: substantive ... DELIBERATE.
> - **v0.12**: substantive ... DELIBERATE.
> - **v0.13**: admin closure ... SHORT.
> - **v0.14** (this plan): HARD-WALL CLOSURE + ISM library bundle + conference reclassification. DELIBERATE.
>
> DELIBERATE-DELIBERATE-DELIBERATE-SHORT-DELIBERATE cadence matches the OMC project rhythm.

**Replace with**: Acknowledge v0.12-web.0 and v0.12-web.1 as parallel-track interim releases (per D29 filename routing + D30 versioning); reframe the cadence as acoustics-track-only DELIBERATE-DELIBERATE-DELIBERATE-SHORT-DELIBERATE with a parallel web-track sub-cadence (v0.12-web.0 DELIBERATE → v0.12-web.1 DELIBERATE-fix-pass). Cross-ref D29 / D30 / D32 / D33 / ADR 0024 / ADR 0027 explicitly.

**Reason**: The §6.3 narrative is currently mute about the parallel track entirely. The v0.14 critic / reader who encounters this section will read "v0.13 → v0.14 directly" and be confused when `.omc/plans/v0.12-web.1-design.md` (576 lines) + `RELEASE_NOTES_v0.12-web.1.md` exist alongside. D22 audit-trail discipline + D28-P1 honesty-discipline require the narrative to reflect all shipped releases between the start of the window and v0.14. Without this amendment, v0.14 §6.3 contains an unintentional honesty-leak (silent omission of v0.12-web.0/web.1).

### AMENDMENT 4 (severity: MINOR — but multiple sub-items)
**Multiple cross-reference adjustments**

(a) §1.1 row 17 (`tests/test_mypy_strict_baseline.py`) line 320 marked "(UNCHANGED)" — replace with "(MOD'd at v0.12-web.1 commit `0bef198`; v0.14 inherits the `pytest.importorskip('mypy')` skipif gate byte-equal)".

(b) §3.1 line 580-582 + §8 line 808/810/812 — the planner-locked OQ-17 / OQ-18 / OQ-19 NEW labels COLLIDE with already-shipped OQ-17 (HUTUBS, `open-questions.md:185`) / OQ-18 (HF Spaces cold-start, `open-questions.md:197`) / OQ-19 (binaural WAV byte-exact, `open-questions.md:208`). Renumber to **OQ-23** (Polygon ISM v0.15+), **OQ-24** (Predictor-default switch v0.15+), **OQ-25** conditional (Per-band glass α revision v0.15+). Update §3.1 / §7 / §8 / §10 OQ list accordingly. Add a one-line note explaining the v0.12-web.0/web.1 OQ allocation consumed 17-22.

(c) §0.4 STOP rule #11 line 280 (no library logic mutation in `roomestim/adapters/*`) should add the clause "outside of the v0.12-web.1 baseline (`PolycamAdapter` is a deprecated subclass alias of `MeshAdapter`; v0.14 acoustics-track does not re-touch this surface)" to clarify the boundary. Otherwise the v0.14 critic could read STOP rule #11 as a backward-looking violation flag and re-litigate D33.

(d) §7 line 795-799 + §0.0 row "D-decision additions" line 140 — cross-reference D32 + D33 (NEW at v0.12-web.1) as v0.13.x → v0.14 prior decisions in the §7 "D-decisions" list, even though v0.14 itself does not invoke them. Otherwise the v0.14 audit-trail is incomplete (D22 + D28-P1 require enumeration of prior decisions in the cross-ref index).

**Reason**: Each of (a)/(b)/(c)/(d) is a single-line cross-reference correction. (b) is the highest-priority of the four because OQ-numbering collision would silently overwrite existing OQ-17/18/19 entries in `.omc/plans/open-questions.md` at v0.14 ship time — that IS an audit-trail honesty leak (D22 / D28-P1 violation). (a)/(c)/(d) are documentation-completeness corrections; (b) is borderline MAJOR but bundled here for absorption efficiency.

---

## §4 Executor readiness

### Verdict: YELLOW — 4 amendments needed; absorb then executor

Substantive plan is currency-correct. All four amendments are mechanical / cross-reference adjustments; none rewrite Item A / B / C / D scope, the path γ default-safe lock, the LOC envelope, the ISM API shape, the analytic shoebox validation framing, the §0.4 STOP rules #1/2/4/5/6/7/8/9/10/12/13/14/15 (only #3 + #11 need adjustment), or the §1.2 +14 test-net count delta. The hard-wall closure path γ is still the planner-locked DEFAULT.

### Recommended absorption sequence
1. Planner round (1 pass; ~30 min):
   - AMENDMENT 1: Decide path (a) tighten filter to `not lab and not web` OR path (b) re-baseline to 176 → 190. Recommend (a) for parallel-track cleanliness.
   - AMENDMENT 2: Renumber ADR 0022 → 0028 + 0023 → 0029 throughout. Explicitly document the rationale in v0.14-design.md §0.0 row "ADR list" + new §0.0 row "ADR numbering" tracking the v0.12-web.0 0024-0027 allocation.
   - AMENDMENT 3: Add 2-3 paragraphs to §6.3 acknowledging v0.12-web.0/web.1 parallel-track sub-cadence; cross-ref D29 / D30 / D32 / D33 / ADR 0024 / 0027.
   - AMENDMENT 4: Apply (a) line edit; (b) re-number OQ-17/18/19 → OQ-23/24/25; (c) STOP rule #11 clarification; (d) add D32 / D33 to §7 cross-ref index.
2. Critic round (optional; planner discretion):
   - Critic-lean if amendments expand v0.14-design.md by > 50 lines.
   - Critic-skip if amendments stay surgical (likely; estimate +15-25 line net).
3. Executor takeoff with absorbed plan.

### Why YELLOW (not GREEN)
GREEN would require no amendments. AMENDMENT 1 alone (CRITICAL) breaks the §5.1 acceptance gate on the first executor command. AMENDMENT 2 (MAJOR) creates the ADR-numbering audit-trail leak if shipped unchanged. AMENDMENT 4(b) creates the OQ-numbering audit-trail leak. These are not optional polish; they are pre-conditions for executor takeoff under D22 / D28-P1 audit-trail discipline.

### Why NOT RED
RED would require a full re-plan. v0.14-design.md's substantive content (Items A/B/C/D, the path γ default-safe lock, the ISM library API at `roomestim/reconstruct/image_source.py`, the analytic shoebox validation framing, the conference + lab + Office_1 ratio plan, the ADR 0019 §Status-update + ADR 0021 §Status-update conditional pattern, the §0.4 STOP rules excluding #3/#11) is BYTE-CORRECT against the v0.12-web.1 baseline. The amendments are mechanical; the plan does not need re-derivation from scratch. A single planner round absorbs all four cleanly.

### Estimated time to executor takeoff
Planner absorption: 30-45 min (most of which is AMENDMENT 1 path decision + AMENDMENT 2 renumber-find-replace). Executor takeoff: same day.

---

## §5 Cross-reference index

- **Planner artefact (re-validated)**: `/home/seung/mmhoa/roomestim/.omc/plans/v0.14-design.md` (954 lines per `wc -l`)
- **Interim release manifests**:
  - `/home/seung/mmhoa/roomestim/.omc/plans/v0.12-web.1-design.md` (576 lines; D32 / D33 / OQ-20/21/22 sources; D27 hard-wall carve-out at lines 21, 87-88, 491-492, 505)
  - `/home/seung/mmhoa/roomestim/RELEASE_NOTES_v0.12-web.1.md`
- **Audit-trail dependencies**:
  - `/home/seung/mmhoa/roomestim/.omc/plans/decisions.md` (D27 at line 1119; D28 at line 1130; D32 at line 1225; D33 at line 1243)
  - `/home/seung/mmhoa/roomestim/.omc/plans/open-questions.md` (OQ-13a at line 71; OQ-13b at line 73; OQ-15 at line 143; OQ-16 at line 153; OQ-17 at line 185; OQ-18 at line 197; OQ-19 at line 208; OQ-20 at line 223; OQ-21 at line 240; OQ-22 at line 256)
  - `/home/seung/mmhoa/roomestim/docs/adr/0019-melamine-foam-enum-addition.md` (§References at line 104-122; v0.12 §Status-update at line 123-163; v0.13 §Status-update at line 165-209)
  - `/home/seung/mmhoa/roomestim/docs/adr/0027-mesh-format-generalisation.md` (NEW at v0.12-web.1; full file)
- **Live-repo geometry**:
  - `/home/seung/mmhoa/roomestim/roomestim/reconstruct/` directory listing (no `image_source.py` yet; correct — v0.14 ships it; `roomestim/predict/` does NOT exist; CC-1 correction in v0.14-design.md still applies byte-equal)
  - `/home/seung/mmhoa/roomestim/roomestim/adapters/` directory listing (`mesh.py` NEW at v0.12-web.1; `polycam.py` MOD'd to deprecated subclass alias per D33)
  - `/home/seung/mmhoa/roomestim/docs/adr/` directory listing (0001..0021 + 0024..0027; numbers 0022 / 0023 currently unallocated)
- **Live-repo test collection (verified at 2026-05-16)**:
  - `pytest -m "not lab" --collect-only -q tests` → **176/177 tests collected**
  - `pytest -m "not lab and not e2e and not web" --collect-only -q tests` → **136/177 tests collected**
  - `pytest -m web --collect-only -q tests` → **38/177 tests collected**
- **Verbatim citation search at 2026-05-16**: `grep -rin "Bies\|NRC datasheet\|Vorländer verbatim" /home/seung/mmhoa/roomestim/docs/` → 10 hits, ALL inside `docs/adr/0019-melamine-foam-enum-addition.md`; NO new external-source verbatim has surfaced; path γ default-safe lock holds.
