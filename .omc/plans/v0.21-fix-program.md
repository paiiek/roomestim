# roomestim post-audit fix program (started 2026-05-28)

Autonomous "fix everything the v0.20.0 multi-perspective audit found" program.
**Checkpoint protocol**: each cycle lands as its own committed + green + tagged
state; the OQ status in `open-questions.md` flips to CLOSED on land. If interrupted,
resume from the first non-CLOSED OQ below — the repo is always at a clean committed
green checkpoint.

Baseline at program start: HEAD = 8cb693b (v0.20.0, pushed). Gates: default 277/6,
web 72/1, mypy 0/38, ruff clean, lint_tense 0, RT60 `1.9190766987173207`.

## Checkpoints

- [x] **Cycle A — v0.21.0 (OQ-43 + OQ-44, edit/predict correctness)** — gates GREEN 2026-05-28
  - OQ-43: shared `wall_surfaces` / `surface_index_for_wall` resolver in
    `roomestim/model.py`; the four predictor walls-only filters +
    `roomestim_web/viewer.py` route through `wall_surfaces`;
    `evolve_room_material` / `_bulk` full-list-index semantics byte-identical
    (additive only — no shipping caller passes a walls-relative index there yet);
    characterization test `tests/test_surface_index_frame.py` pins the bridge
    across two adapter orderings (RoomPlan + inline synthetic trailing-floor).
  - OQ-44: wall_index upper-bound at reader + `object_add._on_add_object` +
    `RoomPlanAdapter` (adapter emits objects → guard is live); predictor fallback
    rationale surfaces the offending index (rationale-string-only, `rt60_s`
    byte-equal); `evolve_surface` band-promotion gated on `absorption_bands is not
    None` (scalar α stays unconditional; load-bearing test split per D70).
  - Gates: default 285/6, web 74/1, mypy 0/38, ruff clean, lint_tense 0, RT60
    `1.9190766987173207` byte-equal. Both versions `0.21.0` / `0.17-web.0`.
  - OQ-43/44 → CLOSED; D68/D69/D70 landed; ADR 0037 + ADR 0031 §Status-update-v0.21.0.
  - **Pending**: commit + tag v0.21.0 (separate review+verify pass).

- [ ] **Cycle B — OQ-45 (web public-deployment hardening, code-level)**
  - mesh upload size + vertex-count cap (`MeshAdapter` + gradio `max_file_size`);
    scrub absolute paths / raw exceptions from web-facing error strings;
    tempdir reaper per-PID namespacing; `on_apply_overrides` list-input type guard.
  - dep-CVE pinning: assess in planner — prefer declarative `pyproject` floor bumps +
    lockfile/CI `pip-audit` recommendation over force-upgrading the canonical env
    (which would risk all gates). Bump `roomestim_web` version (web source changes).
  - Land: commit + tag; OQ-45 → CLOSED (or partial w/ residual noted).

## Out of scope (unchanged): PolycamAdapter alias removal (BREAKING, D33).
