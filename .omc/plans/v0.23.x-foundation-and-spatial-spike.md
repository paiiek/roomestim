# v0.23.x — 토대 강화(#3/#2) + 영상→geometry 실현가능성 스파이크(#1) (2026-06-01)

> 북극성(메모리 project_roomestim_north_star): roomestim = **설치공간 캡처 → 정확한 공간추론 + 스피커 레이아웃**, 단독 유닛(또는 spatial_engine 앞단). vid2spatial_v2(사운드소스 추적)와 **완전 별개**. RIR은 수단(청취충실도 아님) → Phase B/C·OQ-47 저우선.
> 사용자 선택(2026-06-01): **B = 토대 먼저 + #1 스파이크 병행**. 모드: 자율 execute→verify→repeat. OMC 오케스트레이션.

## RESUME POINTER
- **현재**: 병렬 조사 ✅ 완료. (1) **HRTF 좌우반전 CONFIRMED**(tracer high-conf, 양 경로) — 증거: KEMAR 실측 ILD az=90°→LEFT louder(39.5×), coords.py:pipeline_to_ambix(az)=-az 가 변환 권위인데 binaural.py 미적용. (2) #1 리서치 ✅ → `.omc/research/image-to-geometry-feasibility-2026-06-01.md` 저장(핵심: ADR 0001 COLMAP 브랜치 연속, ScaleAnchor 이미 존재, 360-pano→HorizonNet 1차).
- **#3 HRTF 수정 ✅ 완료**: executor(coords.pipeline_to_ambix 경유, 양 경로 통합, README honesty) → code-reviewer APPROVE(0 blocking) → verifier APPROVE(controlled revert로 load-bearing 증명: pre L=1889/R=47 → post 정상). 게이트 GREEN(default 300p/5s, web 86p/4s, ruff/mypy/tense EXIT0). 버전 0.23.1, D80. **커밋/푸시 진행**.
- **#1 Phase-0 스파이크 ✅ 완료**(2026-06-01): **VERDICT = GREENLIGHT(qualified)**. HorizonNet(HF mirror `gum-tech/horizonnet-resnet50-rnn`, st3d) GPU 추론 ~0.25s/img, 출력이 RoomModel 계약에 1:1(corners→floor_polygon, z0/z1→ceiling_height_m), metric 스케일=카메라높이 1스칼라(기존 ScaleAnchor known_distance). 공식 real-data 정확도 corner-err ~0.67%/3D-IoU ~84%. **단 conclusive 못 함**: 원저자 Google Drive GT 데이터셋 dead/locked → 합성 방으로만 테스트(domain-gap로 floor boundary 과대, cm오차 13-72cm는 비대표적·비관적). scratch=`/home/seung/mmhoa/spike-image-geometry/`(repo 밖). roomestim 코드 무변경.
- **real GT 검증 ✅ 완료**(166 real rooms, LGT-Net 미러 PanoContext+Stanford2D3D): **VERDICT = FALLBACK(conditional)**. 표준 S2D3D 5.09%CE/62.6%IoU, PanoContext 8.46%CE/61.5%IoU(out-of-domain, st3d 체크포인트). metric cm: nominal cam_h median 35-57cm, perfect ScaleAnchor median 18cm(≤15cm 43-45%방), ±10cm cam_h 32-38cm. 오차분해=scale ~34-40cm(ScaleAnchor 해소가능)+shape ~18cm(불가역 corner). 단일-파노 st3d는 ≤15cm gate 신뢰성 미달(vs RoomPlan ~8.5cm). in-domain 체크포인트(panos2d3d, 접근불가)면 borderline.
- **결정**: 사용자 "둘 다" → ADR 0045 작성 中. 권고 framing: [vision] image adapter = **"rough-estimate/assisted-measure" tier**(provenance=reconstructed, NOT measured), 단일-파노=quick-start, 다중뷰 MASt3R/VGGT=first-class 정확도 경로. honesty infra(provenance/confidence/Manhattan-flag) 필수. conclusive 인증 = in-domain 체크포인트 + 보정 cam_h로 ≲15cm 재확인(blocking gate).
- **스파이크 산출물**: `/home/seung/mmhoa/spike-image-geometry/`(repo 밖, 재현가능: venv+weights+eval). roomestim 코드 무변경 확인.
- **ADR 0045 사이클 ✅ 완료·커밋·푸시 `fef5afa`**: executor 작성 → critic ACCEPT-WITH-RESERVATIONS(§E 인용오류 등 수정) → tense-lint EXIT0 doc-only 커밋. ADR 0045 PROPOSED + D81 + OQ-52~58 + architecture.md 인덱스. (research 파일은 .omc/research gitignore → 로컬 보존, ADR 자체완결.)
- **이 트랙(v0.23.x) 상태**: #3 HRTF 수정(v0.23.1 `4cb87e0`) ✅ + #1 스파이크/ADR 0045 ✅. **남은 것**: #2 비-shoebox 강건성(B6 live-mesh corner, `floor_polygon.py` stub) = 별도 사이클. #1 후속 = OQ-52(in-domain checkpoint)/OQ-53(다중뷰 MASt3R/VGGT spike)가 다음 실증 단계(blocking gate). 사용자 in-situ 측정 협의 결과 대기(OQ-50/A10b).
- **게이트 baseline**: default 300p/5s, web 84p/4s, ruff/mypy --strict/tense EXIT0. canonical `/home/seung/miniforge3/bin/python`.
- **미커밋**. 커밋은 사용자 요청 시.

## 작업 항목
### #3 (즉시, 저위험)
- **HRTF 좌우반전 확정**: `hrtf_io.py:40` SOFA "az 0..360 CCW from front" → CCW면 +90°=왼쪽. 렌더러 DOA `az=atan2(side,front)`가 오른쪽 소스→+90° 가정(D75)이면 `nearest_hrir` 룩업이 좌우 반전. 자기참조 테스트(`test_binaural_renderer.py:148`)가 갭 은폐. → **증거 기반 확정 후** 수정 + 데이터-근거 L/R 테스트. blast radius: render_binaural_demo + synthesize_brir 둘 다.
- **README honesty 부채**: stale 게이트 카운트(287/67) → 실제 300/84, "현재상태(2026-05-29)" 날짜, feature-expansion Phase 1 미체크 박스.

### #2 (다음, 토대)
- 비-shoebox/불완전 mesh 강건성. B6 live-mesh corner 추출(`floor_polygon.py` NotImplementedError stub, 합성-shoebox tautology) — 별도 사이클.

### #1 (병행 리서치 → ADR seed, 구현 별도)
- 설치공간 **영상/사진 → room geometry(floor polygon + 치수 + 재질 단서)** 실현가능성. 현 roomestim은 깨끗한 스캔만 입력 = 미구현 frontier. 어떤 입력 모달(단일사진/다중뷰/영상), 어떤 접근(photogrammetry/SfM/monocular reconstruction), borrow-vs-build, roomestim 출력(adapter 경계)과의 접합점.

## 평가 사실(재확인 불요)
- v0.22.2(`2eae5eb`) + v0.23.0 RIR Phase A(`fa7c48d`) 커밋·푸시 완료. 게이트 GREEN.
- 3-lane 평가(architect/explore/critic) 완료: synthesize_brir UI 미연결(라이브러리만), HRTF L/R 의심, web mypy 미게이트(21에러), Phase B/C 격하.
